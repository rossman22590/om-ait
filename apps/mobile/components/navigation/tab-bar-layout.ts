/**
 * Geometry of `FloatingTabCapsule` (`FloatingTabBar.tsx`), the segmented
 * capsule the switcher sheets pin at their bottom (`PinnedBar`). The root tab
 * bar that first used it is gone (COR-161), and with it the tab-screen
 * clearance helpers.
 */

/** Height of the floating capsule (icon over label). */
export const FLOATING_BAR_HEIGHT = 60;
