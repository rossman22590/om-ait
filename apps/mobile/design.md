# Kortix Mobile — design approach

Read this before building or restyling any screen in `apps/mobile`. It records
the patterns Jay has signed off on, with the exact values. `CLAUDE.md` in this
folder holds the primitive rules (which component to use); this file holds the
layout and visual decisions (how a screen looks).

When a rule here conflicts with an older screen, the rule wins. Convert the
screen when you touch it.

## 1. Settings screens

Every settings-style screen uses one layout, built from
`@/components/kortix/settings-list`: the Account page, the `(settings)`
sub-pages (Sounds, Notifications, Language, Delete account), Accounts,
Billing, and Plans. Do not hand-roll headers, rows, dividers, or group titles.

There is **one settings page**: the Account page
(`components/settings/AccountPage.tsx`). The Account tab renders it, and the
project sidebar's avatar opens the same page inside the project stack at
`/projects/[id]/account`. There is no `(settings)` index screen and no General
page.

| Export | Use |
| --- | --- |
| `SettingsHeader` | Back button + title + optional trailing action. `(settings)` screens get it from `_layout.tsx`; root-stack screens (`/accounts`, `/billing`) render it themselves with `Stack.Screen options={{ headerShown: false }}`; tab roots pass `showBack={false}`; project pages (Sessions, Account) pass `onOpenMenu`, which replaces Go back with the hamburger (`MenuButton`); `largeTitle` moves the title below the control row as an `h3` (Sessions) |
| `SettingsPage` | Scroll body. Tab roots pass `paddingBottom={useTabBarClearance()}` and `contentInsetAdjustmentBehavior={TAB_SCROLL_INSET_ADJUSTMENT}` |
| `SettingsGroup` | Titled, borderless rounded card with full-width separators |
| `SettingsRow` | icon (or `leading`) · label · trailing |
| `AppearanceRow` | The Appearance row: current mode as its value; opens a dialog with System, Light, Dark (icon · label · check) |

```tsx
import { SettingsGroup, SettingsPage, SettingsRow } from '@/components/kortix/settings-list';

<SettingsPage>
  <SettingsGroup title="Preferences">
    <AppearanceRow />
    <SettingsRow icon={Volume2} label="Sounds" onPress={() => go('/(settings)/sounds')} />
  </SettingsGroup>
  <SettingsGroup title="Help">
    <SettingsRow icon={BookOpen} label="Docs" external onPress={openDocs} />
  </SettingsGroup>
</SettingsPage>
```

### Page

| Property | Value |
| --- | --- |
| Body | `SettingsPage` — `ScrollView`, `bg-background`, no scroll indicator |
| Side margin | `px-5` (20pt) |
| Space between groups | 18pt |
| Bottom padding | safe-area inset + 28pt |
| Content above groups | `header` prop (e.g. the profile photo on the Account page) |
| Pinned bottom action | render the pill below `SettingsPage` in a `bg-background px-5 pt-3` view with `max(insets.bottom, 16)` bottom padding, and pass `paddingBottom={24}` to the page (Plans) |
| Full-bleed hero | `hero` prop (Billing). The hero scrolls with the page; the body sits on a `rounded-t-3xl bg-background` sheet that overlaps the hero’s bottom 24pt, so the hero keeps ≥ 24pt bottom padding |

### Header

| Property | Value |
| --- | --- |
| Component | `SettingsHeader` from `settings-list` — one header for every settings-style screen |
| Go back | `PlatformButton` — native SwiftUI button on iOS (`chevron.left`, plain style on the `secondary` fill), `Button variant="secondary"` icon pill on Android |
| Back behaviour | `router.back()`; if there is no history, `router.replace('/projects')` |
| Title | screen name, sentence case |
| Trailing action | `right` prop — e.g. `PlatformButton label="New" systemImage="plus"` on Accounts |
| Hero header | `align="center"` + `transparent` over a `SettingsPage` hero. Pair a centred title with icon-only actions; an empty side gets a 40pt spacer |

Register a new `(settings)` screen in `_layout.tsx` with
`header: () => <SettingsHeader title="…" />`.

### Group

| Property | Value |
| --- | --- |
| Title | `Text variant="muted"`, `mb-2 px-4`, **sentence case** ("Sound pack", not "SOUND PACK"). Never uppercase, never letter-spaced eyebrows |
| Surface | **Each row is its own tile** (Jay, 2026-09-22): `bg-card`, **no border**, `overflow-hidden` (it clips the row's pressed fill). The group's outer corners are `rounded-2xl` (16pt: first row top, last row bottom); the corners between two rows are `rounded-md`. A one-row group is one fully `rounded-2xl` tile. `SettingsGroup`'s `className` overrides the tile surface (`bg-secondary` inside a sheet) |
| Dividers | No hairline. A 2pt gap between tiles (`ROW_GAP` in `settings-list.tsx`), set by `SettingsGroup`: what is behind the group shows through it — `bg-background` on a page, the sheet colour in a sheet. Rows never draw their own divider |
| Empty group | renders nothing — conditional rows (`{cond && <SettingsRow/>}`) are safe |

### Row

A row is **icon · label · trailing**. Nothing else.

| Property | Value |
| --- | --- |
| Padding | `px-4 py-3` (py one step below px — see §2 Spacing) |
| Icon | Phosphor from `@/lib/icons` at the app weight (no `weight` prop), `size={18}`, `text-foreground/80`, centred in a leading slot at least 20pt wide (`min-w-5`) so every label aligns. A row with no `icon` and no `leading` drops the slot and its `mr-3` gap, so its label starts at the card padding. A loading row keeps the slot with a `<Skeleton>` so its label does not shift |
| Custom leading | `leading` prop instead of `icon` — flag emoji (`Text variant="large"`) on Language, `Avatar variant="custom" size={28}` on Accounts |
| Label | `Text` default variant, one line, sentence case ("Edit profile", "Delete account") |
| Long label | `multiline` prop wraps the label instead of truncating it. Only for list content that is the label itself: plan features on Plans and the upgrade sheet (`Check` icon · feature). Never a way to add a description |
| Description | **none**. Do not add a second line under the label |
| Chevron (navigates in-app) | `CaretRightIcon`, `size={16}`, `text-muted-foreground/70` — shown automatically when the row has `onPress` |
| External link (opens browser) | `external` prop → `ArrowUpRightIcon`, same size |
| Read-only value | `value` prop → muted text on the right, max 60% width, one line (e.g. the account email) |
| Inline control | `right` prop → `Switch`, `ToggleGroup`, check mark, icon button. Replaces the chevron |
| Selected option (picker) | `checked` prop → primary check mark. Picker rows also pass `right={null}` so no chevron shows (Sound pack, Language) |
| Active item that still navigates | `checked` + default chevron (the active account on Accounts) |
| Appearance | always `<AppearanceRow />` — a `Dialog` (not a page, not an inline toggle) listing System, Light, Dark in that order, each with its icon (`Monitor`, `Sun`, `Moon`) and a check on the active mode, in a `SettingsGroup className="bg-secondary"` on the `bg-popover` dialog; choosing applies and closes. Dialog width is explicit — `min(window width − 32, 420)` — because `w-full` collapses to content inside the native overlay wrappers |
| Destructive | `destructive` prop → icon and label `text-destructive` (Delete account, Sign out) |
| Badge | `badge` prop → small destructive pill after the label (e.g. "Scheduled") |
| Plan badge | `PricingTierBadge` in `right` (Account page email row, Billing Current plan). Legacy tiers (Basic, Plus, Pro, Ultra) render their SVG artwork. Any other plan name renders a pill in the artwork's shape: full radius, `THEME.light.border` fill and `THEME.light.foreground` text in both themes, label at 55% of the pill height. Never leave the slot empty |
| Press feedback | background highlight `active:bg-accent`. Never scale a row inside a card |

### Account tab and account screens

| Screen | Structure |
| --- | --- |
| Account page (`AccountPage`) | `presentation="tab"` (Account tab): page title "Account" (`Text variant="h3"` in a 40pt row, part of the scroll content — no header bar, no logo, page background). `presentation="project"` (`/projects/[id]/account`, from the project sidebar avatar): `SettingsHeader title="Account"` with the hamburger (`onOpenMenu` → the project drawer), no Go back, no h3. Both then: profile header (80pt `ProfilePicture`, tap → system photo picker; 28pt camera badge `bg-secondary` with a 2pt `border-background` ring at the bottom-right; `KortixLoader size="small"` over the photo while uploading; display name `Text variant="large"` centred below) → one untitled group (no icon · email · `PricingTierBadge size="md"` for the active account's plan, always shown: `plan.label` from the API, else `tier_display_name`; no icon · Edit profile → `EditProfileSheet`) → Preferences (Appearance, Sounds, Notifications, Language) → Workspace (Accounts, Billing) → Help → **Advanced** (Delete account, Sign out — destructive, last) |
| Edit profile | `EditProfileSheet`: `Sheet` + `SheetHeader` "Edit profile" + `SheetTextInput` (display name, max 100) + `Button size="lg" rounded-full` Save, disabled until the name changes. Save closes the sheet; a failure is a toast |
| Delete account (`/(settings)/account-deletion`) | Groups: What gets deleted (icon · label, read-only) → When (picker: In 30 days ✓, Immediately) → destructive `Button size="lg" rounded-full`. The pill opens ONE `AlertDialog` with two steps, for both timings (`lib/account-deletion/confirm-flow.ts`): (1) title + consequence line, auto-focused `<Input>` "Type DELETE to confirm", destructive Continue disabled until the word matches (trimmed, any case); (2) "Are you sure?" + "Everything in your account is deleted…", destructive Delete that stays disabled for 1s after the step appears, so a double tap on Continue cannot delete. The steps swap inside the same dialog — never two overlays. Scheduled deletion: one row (Calendar · Scheduled for · date) + secondary pill Cancel deletion (no confirmation — it is safe) |
| Plans (`/plans`) | `SettingsHeader "Plans"` → Plan picker group (plan icon · name · price or "Current" · check) → seat total row for Team with more than one member → "{plan} includes" group (`Check` · feature, `multiline`) → pinned primary pill. The pill opens kortix.com (arrow icon): Upgrade to Team / Switch to Free → web billing, Contact sales → `/contact`; disabled "Current plan" or "Ask an account owner to upgrade". Logic: `lib/billing/plan-action.ts` |
| Upgrade sheet (`GlobalUpgradeSheet`) | Centred "Kortix Team" (`large`) · price (`h1`, tabular) · "per seat / month" (`muted`) · gate message (`muted`) → Includes group (`multiline`) → seat total row → primary pill "Upgrade to Team" (→ Plans) or an Ask an account owner row → secondary pill Not now |
| Projects header | Kortix logo · search · New. No settings/avatar button — the Account tab owns settings. Search shows only when the account has at least one project (hidden while loading and when empty) |
| Projects empty state | Plain page, centred between header and tab bar: "No projects yet" (`Text variant="large"`) · 24pt gap · primary `Button size="lg" rounded-full` "Create project" (owners and admins only). No card, border, fill, icon, or description |
| New project sheet | `NewProjectSheet` (Jay, 2026-09-22): `KortixBottomSheetModal` at 70%, `SheetTitleRow` "New project" with the X, then `px-4` content with 16pt gaps — the name `SheetTextInput` (auto-focused, Return creates), one `SettingsGroup className="bg-secondary"` of an "Account" row (`UserIcon`, `value` = the account's name, chevron; **only when the user can create in two or more accounts** — owner or admin, `creatableAccounts` in `lib/projects/landing.ts`, web `/new`'s `AccountPicker` rule; it pushes an "Account" view of picker rows — 28pt `Avatar` · name · check — and a tap picks and returns) and an "Import from GitHub" row (`GithubLogoIcon`, chevron), and an `lg` "Create project" pill (disabled until a name is typed; "Creating…" while pending). The sheet opens on the Projects page's active account (else the first creatable one); after a create the Projects page switches to the project's account before it opens the project. No description, no info card, no Cancel. The row pushes the GitHub view in (`sheet-push`; Back in the close button's slot): a `bg-secondary` group of GitHub account picker rows (`GithubLogoIcon` · owner · check) ending in "Add GitHub account" (external), a "Search repositories" field, a `bg-secondary` group of repository picker rows (`owner/repo`, `description` = default branch · Private), "Project name (optional)", and an `lg` "Import repository" pill. Loading: `KortixLoader size="small"`. No installation: the empty-state shape — `Text variant="large"` "Connect GitHub" · 24pt · an `lg` "Connect GitHub" pill that opens the install page |
| Account detail (`/accounts/[id]`) | `SettingsHeader title={account name}` + tab switcher (Members, Groups, Git, Audit, Settings). No avatar/name/"1 member · 1 project" block |
| Tab content | `SettingsPage`; primary action (Invite member, Create group, …) as the first row group; lists as titled groups of avatar/icon · name · value · chevron; row actions on the detail screen or an action sheet, never inline icon buttons |
| Detail screens (member, group) | `SettingsHeader title={name}`; destructive action alone at the bottom |
| Billing (`/billing`) | `SettingsPage hero={<BillingHero …/>}`: warm gradient (`THEME.accent.red` → `.orange` at low alpha over the theme background, diagonal sheen), centred transparent header with a help icon (Credits explained), balance label (`Text variant="large"`) + balance (`h1`, tabular), breakdown rows (label · semibold tabular value, no icons), one primary pill (`Button size="lg" rounded-full`, label left; chevron right, or an arrow when it opens the browser): Buy credits (→ web billing) when the plan can buy credits, else Change plan (→ Plans). Loading: `KortixLoader` in an `h-64` box, no skeleton bars. Sheet: Scheduled change (New plan, Starts on, Keep current plan → `AlertDialog`) → Subscription (Current plan badge or name, Next billing, Annual commitment, Cancels on, Change plan when the hero shows Buy credits, Manage on kortix.com — external). Reads the active account (`useActiveAccount`), the same one as the Account page badge; refetches on pull to refresh and when the app returns to the foreground |

No subtitles, helper paragraphs, or meta lines anywhere on these screens.

| Pattern | Value |
| --- | --- |
| Accounts list | one untitled `SettingsGroup` — the header already says "Accounts" |
| Row ⋯ menu (Projects) | `ProjectActions`: bottom `Sheet` with the item's avatar + name and one untitled `SettingsGroup` of actions (Open project; Archive project, destructive, managers only). A destructive action confirms in an `AlertDialog` that opens after the sheet has closed — never two overlays at once |
| Android / web tab bar | `FloatingTabBar`, mirrors the iOS bar: 60pt capsule (`FLOATING_BAR_HEIGHT`), icon (20pt) over a 12px label, `bg-secondary` pill thumb behind the active tab, foreground icon + label on every tab. Light: `bg-background` + soft shadow; dark: `bg-card` + border. Behind it, a scroll-edge fade (theme background, transparent 36pt above the capsule → opaque at the screen edge), like the iOS 26 bar. Hides on React Native `Keyboard` events, not keyboard-controller values. Android nav bar: `expo-navigation-bar` `enforceContrast: false` (no 3-button scrim in builds) + `NavigationBar.setStyle` matching the status bar |
| Project stack | `/projects/[id]` is a nested stack: `app/projects/[id]/_layout.tsx` renders `ProjectScreen`, `index` is project home, `view` is the open page, thread, or connecting session, `sessions` is the Sessions page, `files` is the Files page, and `account` is the Account page (`components/session/ProjectRoutes`, route names in `lib/session/project-stack.ts`). The stack is `[index]` or `[index, X]` — never deeper than one screen over project home. **Every project page shows the hamburger and no Go back** (Jay, 2026-09-16): project home, a thread, and a connecting session show `FloatingMenuButton`; every project page uses one 16pt side edge (`px-4`; Jay, 2026-09-16): `PageHeader` `px-4`, `FloatingMenuButton` `left-4`, the home composer `px-4`, `SearchListHeader` `px-4` by default, and `SettingsHeader`/`SettingsPage` `gutter="project"` on Sessions and Account (the Account tab and settings screens keep 20pt `gutter="page"`); tool-page bodies still set their own inline padding (mostly 16pt) — not yet converted; every hamburger is `MenuButton` (`components/kortix/menu-button.tsx`), the same on every platform — no native Liquid Glass circle on iOS (Jay, 2026-09-17): `Button variant="ghost" size="icon" className="-ml-2.5 rounded-full bg-background"` (page colour, so a floating hamburger hides the content scrolling under it) (the −10pt margin puts the icon glyph on the page's padding edge, parallel to the title and list), static `MenuIcon` 20pt `text-foreground` (`components/icons/menu-icon.tsx`: three left-aligned bars, 24/11/13 wide; Jay, 2026-09-17), 10pt hit slop, label "Open menu" — transparent at rest, `active:bg-accent` while pressed, no open/close animation (Jay, 2026-09-16); tool pages and Files show `PageHeader`'s hamburger; Sessions and Account show `SettingsHeader onOpenMenu`. The drawer opens from every project route (hamburger or left-edge swipe). Drawer moves (`drawerRouteMove`, `returnHomeMove`): Sessions / Files / avatar push over home, replace a covering route, and do nothing when that route is already on top; a session row only updates the tab store (home pushes `view`, an open `view` swaps its content, a covering route replaces itself with `view` — `useCoveringRoute`); New session resets the store and pops to home (`StackActions.popTo('index')`); All projects `router.replace('/projects')` (the logomark is not tappable). Never replace `view` with `view`: the old view's cleanup would close the session that just opened. Back: Android back closes an open drawer, else pops a covering route to project home (`androidBackMove`), else on home is left to the system. iOS has no swipe-back in the project stack (`gestureEnabled: false`, `fullScreenGestureEnabled: false`): the left edge opens the drawer on every project route — one edge gesture, one meaning. `projects/[id]` itself has `gestureEnabled: false` and always opens with `router.replace`, so no screen sits under a project. The Projects list opens only from the project sidebar. A project always opens on project home: the tab store neither restores nor persists the active page or thread |
| App start | `app/index.tsx` is the door (web `/projects/start`): signed out → `/auth`; signed in → the last project this user opened (`useLastProjectStore`, keyed by user id), else the first project (`lib/projects/landing.ts`: selected account, then owned/admin, then member accounts), via `router.replace`. The Projects list only when no account has a project. Failure: 2 retries, then a centred message (`large` title, `muted` line) chosen by `lib/projects/start-failure.ts`: unreachable → "Can't reach Kortix" / server error → "Could not open your project", each with `size="lg"` pills Try again (primary) · All projects (secondary, opens `/projects`) · Sign out (ghost); a 401/403 → "Your session has ended" with one primary pill Sign in again. Never a dead end, never an automatic fall back to the list. Every implicit redirect (sign-in, back with no history, leaving an account) goes to `/`, never `/projects` |
| Push transition | Every stack (root, `(settings)`, `auth`, project) is expo-router's native `Stack` with the platform default push/pop on iOS and Android — no custom animation. The root `index` redirect has `animation: 'none'`. Swipe-back is iOS-only, and off in the project stack (its left edge opens the drawer); Android uses the system back gesture. |
| Tab root headers | The Account tab has no screen header — the profile block is the top of the page (status-bar inset: automatic on iOS, `insets.top + 16` on Android) |
| Confirmations | `AlertDialog` (`rounded-3xl` content — `Dialog` and `AlertDialog` are borderless in the primitives, on `bg-popover` — the same surface as bottom sheets — over a `bg-black/70` overlay; call sites never set a background; footer: destructive `Button size="lg" rounded-full` + `AlertDialogCancel asChild` secondary pill). Never `Alert.alert`. Sign out, Delete account, and Keep current plan keep the dialog open while they run and show a failure in the description. Delete account always asks for a typed confirm word and then a second question with a delayed Delete button (both timings); taps alone never delete. Keep current plan confirms with the buttons alone, and a safe action (Cancel deletion) does not confirm |
| Results | Success of an action that leaves the page in place → `useToast().success`; a failure outside a dialog → `useToast().error`. Never `Alert.alert` |

### Copy

- Labels name the destination or setting in 1–2 words, sentence case.
- Group titles group by user intent: Preferences, Profile, Account, Help,
  Advanced, Sound pack, Sound events, Feedback.
- Destructive actions live in their own group at the bottom (Advanced / Account).

## 2. Spacing

Padding is written as plain Tailwind classes in each component — no shared spacing module. Mobile uses stock Tailwind spacing (1 unit = 4pt: `px-4` = 16pt). The values below are the only ones to use.

Jay's rule (2026-09-16): **vertical padding is one Tailwind step below horizontal padding** on every padded row, list item, and inset block. Pick the side padding for the surface, then take one step off for the top and bottom.

| Pair | Pt | Use |
| --- | --- | --- |
| `px-3 py-2` | 12 / 8 | Edge rows: project sidebar session rows and Previous chats, inside an `mx-4` column so the highlight box starts on the 16pt edge and the content on the 28pt edge |
| `px-4 py-3` | 16 / 12 | Rows inside a card or group (`SettingsRow`, Sessions page rows) — the default |
| `px-5 py-4` | 20 / 16 | Roomy blocks and page sections |

- Never set py below px − 1 step (`px-4 py-1.5` reads cramped) or equal to px (`px-4 py-4` reads loose).
- Side edges: project pages use a 16pt edge (`px-4`, Jay 2026-09-16 — 12pt was too tight); the Account tab and settings screens use 20pt (`px-5`).
- A control's icon sits on the padding edge, not the control's box: `MenuButton` pulls itself left by `-ml-2.5` so the hamburger glyph lines up with the title and list below it.
- Fixed-height controls (`Button` sizes, 40/44pt inputs) keep their size variants; this rule is for padded content, not for `Button` boxes.

## 3. Buttons

| Situation | Use |
| --- | --- |
| Primary header action (e.g. projects "New") and screen back buttons | `PlatformButton` — native iOS SwiftUI button (plain style, `secondary` fill, no glass), design-system pill on Android |
| The project hamburger | `MenuButton` — the design-system ghost icon button on every platform. No native Liquid Glass circle on iOS (Jay, 2026-09-17) |
| Everything else | `Button` from `@/components/ui/button` with `variant` + `size` |
| Pill shape | `className="rounded-full"` — the only visual class allowed on a `Button` |
| Action under a chat message (Copy, Edit message, turn details ⋯) | `Button variant="ghost" size="icon-sm"` — a 28pt box (web: 26px), with `hitSlop={TURN_ACTION_HIT_SLOP}` (8pt above and below → 44pt tall target). Glyph sizes are unchanged (15pt user row, 17pt assistant row). Never `size="icon"` there: a 40pt box per action reads oversized (Jay, 2026-09-21). `icon-sm` is for these actions only |
| Never | `h-*`, `w-*`, padding, or text size/weight classes on a `Button` or its `Text` |

`PlatformButton` needs the `ExpoUI` native module (`@expo/ui`). A binary built
without it falls back to the design-system button, so OTA updates stay safe.
Adding or upgrading native modules requires a new binary (`npx expo run:ios`
locally, EAS for stores).

### Inputs

| Rule | Value |
| --- | --- |
| Border | **none**, on every input. Fields are filled surfaces |
| Surface | `bg-secondary` (the `secondary` token) |
| Text | 16pt Roobert Regular — `INPUT_FONT_SIZE` / `INPUT_FONT_FAMILY` from `pill-input.tsx`, `font-roobert text-base` on `<Input>` |
| Placeholder | same size and weight as the text, `muted-foreground` colour (never a lighter opacity) |
| Which field | `<Input>` (rounded-xl, 44pt) for forms; `PillInput` (pill, 44pt) on full screens like auth; `SheetTextInput` inside bottom sheets; `SearchBar` / `SearchListHeader` for in-page search; `SearchHeader` for header search mode |
| Never | local `border-*`, `text-[NNpx]`, `font-*`, or height classes on an input |

## 4. Auth screens

| Element | Value |
| --- | --- |
| Welcome | full-bleed `KortixCurrents` hero, logomark, provider pills as `PlatformFullWidthButton` (`size="xl"`, 48pt — the only `xl` Buttons in the app; Jay, 2026-09-17): native SwiftUI capsules on iOS (Jay, 2026-09-17), design-system `Button` `rounded-full` elsewhere, same variants on both (Google and Apple `default`, email `outline`), provider icon pinned to the pill's left edge. No legal links (Jay, 2026-09-17) |
| Email screen | back `PlatformButton`-style header, `PillInput` fields (44pt, matches `Button size="lg"`), primary pill, bottom pinned secondary pills. No legal links (Jay, 2026-09-17) |
| Fields | `PillInput` on full screens, `SheetTextInput` inside bottom sheets — never `<Input>` restyled by class |

## 5. Project home

`/projects/[id]` with no chat open (`components/session/ProjectHome.tsx`,
COR-34). It matches the web project home: simple and clean, nothing extra.

| Element | Value |
| --- | --- |
| Hero | `ProjectHero` (`components/session/ProjectHero.tsx`): the Kortix symbol drawn as liquid metal (`MetalKortixLogo`, below) **alone — no sentence, no project name** (Jay, 2026-09-21; the "Give {project name} something real to work on." line and `lib/session/project-greeting.ts` are deleted — do not add them back). Centred on the screen; centred in the area above the keyboard while typing. `accessibilityRole="image"`, label "Kortix" |
| Hero metal | `components/kortix/MetalKortixLogo.tsx` draws Paper's `Heatmap` shader (`lib/effects/heatmap-sksl.ts`, an SkSL port; Paper's own build is WebGL and cannot run in React Native) with Skia, the same code on iOS and Android (Jay, 2026-09-21). **Dark is Paper's export exactly** (`Heatmap`: contour 1, inner glow 0.89, angle 51, scale 0.9, white and near-black, transparent back, Paper's exported frame at rest); do not alter the shader's output (Jay, 2026-09-22: removing the outline and body was rejected). **Light is its own palette on the same shader, not a copy of dark**: near-black streaks over a light-gray body (Paper's white vanishes on a white page). **It moves only while the phone moves and costs nothing at rest**: no frame loop and no idle drift. Reanimated's rotation sensor (built in, not `expo-sensors`) feeds a reaction that retargets a critically damped spring (`SPRING`: stiffness 35, damping 12, about 1 s to settle) only when the target changes; a spring keeps its velocity between the 25 readings a second, so the motion is continuous, and when it settles nothing draws. A full move is 2.5 shader-seconds (`HEATMAP_SWEEP_SECONDS`) over about 29 degrees (`TILT_FULL_RAD`). Movement is measured against a baseline that follows the phone with a 1.5 s time constant (`lib/effects/heat-tilt.ts`, tested), so holding the phone upright is rest and the highlight eases back to rest when it is held still. Reduce Motion, an unfocused screen and a backgrounded app draw the rest frame and slow the sensor to 1 Hz. Falls back to the flat `KortixLogo` while the texture loads or if the shader does not compile. The texture `assets/brand/kortix-heatmap.png` is generated: `pnpm bake:heatmap`. It is three gray planes (contour, wide blur, inner blur) and must stay gray: Skia color-manages an RGB texture and mixes its channels, which drew a halo around the symbol on a device. `scripts/check-sksl.ts` compiles the shader; `scripts/render-sksl.ts` renders it to PNG without a device (`SWEEP=1` for the frames a move can reach; `TONE=light LIGHT=<name>` for light palettes) |
| Hero size | Resting size = `heroLogoSize(window width)` (`lib/session/project-hero.ts`, tested): 30% of the width, min 88pt, max 150pt — 121pt on a 402pt iPhone, 113pt at 375pt, 150pt on a tablet (Jay, 2026-09-22: bigger again; it was 64pt, then 96pt). While the keyboard is up the symbol scales to `HERO_KEYBOARD_SCALE` (0.625 → 76pt on a 402pt phone). The scale reads `useReanimatedKeyboardAnimation().progress` on the UI thread, so it moves 1:1 with the keyboard and reverses with it — no timing, no spring of its own. Only `transform` animates; the symbol renders at the resting size and scales down, so it is sharp at both ends |
| Hero colours (hidden) | **A press on the symbol held for 5 seconds** (`LOGO_SHEET_HOLD_MS`, `Pressable delayLongPress`; Jay, 2026-09-22, shortened from 10s the same day) opens `LogoPaletteSheet` — nothing opens sooner, and there is no hint and no progress: it is not a feature to find by accident. A shorter press only closes the keyboard, as a tap around the symbol does. On open: `haptics.success()`. Project home's hero wrapper is `pointerEvents="box-none"` so only the symbol takes touches. **The sheet** (`components/session/LogoPaletteSheet.tsx`): `KortixBottomSheetModal title="Kortix"`, `PickerSheet`'s layout (16pt sides, 16pt between groups). First a **Style** `SettingsGroup` (`lib/effects/logo-style.ts`: Dither, the default · Heatmap — one Paper shader each, `MetalKortixLogo`, both driven by the same tilt, `hooks/useTiltMotion.ts`; switching crossfades over `LOGO_STYLE_CROSSFADE_MS`, so the shaders never cut. A third style, liquid metal, shipped and was removed the same day, Jay: keep only heatmap and dither), then one **Colour** `SettingsGroup className="bg-secondary"` (Graphite, the default · Gold · Copper · Ruby · Amethyst · Cobalt · Emerald — metal finish only, Jay, 2026-09-22: pastel removed) of `SettingsRow`s: `leading` = a 20pt round swatch (the palette's body colour, its highlight as the centre dot) · name · check, `right={null}`. **Predefined colours only, never a free colour picker** (Jay): every palette is one brand accent (`THEME.accent.*`), derived in `lib/effects/logo-palette.ts` (tested) — no new colour literal; on a white page the outline stays mid-dark because the shader maps low heat to transparent. A tap applies at once with `haptics.selection()` and **the sheet stays open** (compare, like the model sheet); it takes at most 50% of the screen with a 0.2 backdrop (a deliberate override) so the symbol behind it is the live preview. The style and the palette are both kept on the device (`stores/logo-palette-store.ts`, zustand `persist`; an unknown saved id falls back to Dither / Graphite). `MetalKortixLogo` takes the style as `style` and the palette as `palette` (heatmap) / `front` (dither); the flat fallback symbol keeps the theme colour |
| Chat input | `Composer` (`components/kortix/composer.tsx`) — **one card for project home and the thread** (Jay, 2026-09-21; the thread's `SessionChatInput` wraps it): `rounded-3xl`, **the page colour `bg-background` in both themes** (Jay, 2026-09-21: never `bg-card` or any second shade — measured on the simulator in dark mode: page and card both rgb 11,11,11). Edge: a hairline `border border-border` in both themes. **No shadow** (Jay, 2026-09-21): `LIGHT_SHADOW` put a grey halo around the card in light mode, so the input read darker than the page. Text field on top (16pt Roobert, plain "Ask anything" placeholder, no animation). Row below, **36pt controls** (Jay, 2026-09-21: 40pt read oversized): add (`Button variant="secondary" size="icon-md"`, 18pt `+`) · model pill (`Button variant="secondary" size="sm"`, model name + 14pt `CaretDownIcon` `text-muted-foreground`) · spacer · send (`Button size="icon-md"`, 18pt arrow, secondary when empty, primary when there is something to send). All `rounded-full`; every control passes `hitSlop={COMPOSER_CONTROL_HIT_SLOP}` (4pt), so its touch target stays 44pt. Nothing else in the row: no agent, no settings, no Queue pill |
| Thread additions | Same card, `px-4`, `pb-3`. `header` slot above the text: the queue panel and the staged slash command (a `bg-secondary` pill: `TerminalIcon` · `/name` · remove). Pill label `composerPillLabel`: "{model}" or "{model} · {level}". While the agent works: Stop (`secondary` `icon-md`, 12pt `StopIcon`) alone; with a draft, Stop + Send, and Send queues the message. `+` opens the Add sheet (`AttachSheet`, see Add below); under the tiles the thread adds one `SettingsGroup className="bg-secondary"`: **Recent files** (`StackIcon`, the three-layer glyph) and, when the project has AutoContinue commands, AutoContinue with its state as the row value. Each row closes the Add sheet and then opens its own sheet (`closeThen`). Project home has no session, so it shows the tiles alone. **Recent files sheet** (`SessionFilesSheet`, Jay 2026-09-22): full height (`snapPoints ['100%']`, `topInset` = the status bar), `KortixBottomSheetModal title="Recent files"` (close button · centred title), `SheetTextInput` "Search files" (hidden while the session has no file), then the files this session produced — web's Outputs card rules, ported to `lib/session/session-files.ts` (tested): completed write / edit / `apply_patch` / `show` items / generated image, video, deck; one row per file, the later call wins; the latest run first, then shown · PDF · spreadsheet · document · deck · page · image · media. Two `SettingsGroup className="bg-secondary"`: untitled (what the user came for; row = file-type glyph from `showFileTypeIcon` · name · kind as the value: PDF, Spreadsheet, Document, Slides, Web page, Image, Video) and "Other files" (`Text variant="muted"`, `mb-2 px-2`; source and config; no value — "File" says nothing). Rows keep the chevron: a tap opens another sheet. Search matches name, `show` title, path, and kind. A tap (`haptics.tap()`) opens the **file preview sheet**, a second `KortixBottomSheetModal` pushed over the list (`stackBehavior="push"`; Jay, 2026-09-22): full height, title = the file name, body = `FilePreview` (`components/files/FilePreviewRenderers` — markdown, HTML, CSV, JSON, code, text, image), loaded by `useFilePreviewData` (`components/files/`, the hook `FileViewer` uses too, so size limits and fetch rules are shared). **A file the sheet does not render fetches nothing** (Jay, 2026-09-22): `previewsInline(name)` (`lib/session/session-files.ts`, tested) is false for a PDF, an Office file (doc/docx/ppt/pptx/key/xls/xlsx/od*), an archive, audio and video; the hook gets `enabled: false` and the body is the file card — 40pt file-type glyph `text-muted-foreground` · name (`Text variant="large"`, 2 lines) · kind (`muted`). `enableContentPanningGesture={false}`: only the handle and title bar drag the sheet, so the document scrolls inside it. Loading: `KortixLoader size="large"` centred. **Failure says why** (`previewFailure`, `lib/files/preview-failure.ts`, tested): 404 → "This file is no longer in the sandbox. It was moved or deleted."; 401/403 → "You do not have access to this file."; any other status, a network error, or no sandbox URL → "The sandbox did not answer. It may be asleep or starting up." / "The sandbox is not connected. …" with a secondary `Button size="lg" rounded-full` "Try again" (`retry()` refetches). A stopped sandbox keeps its transcript but serves no file read, so this is the common failure. **Copy is in the title row, far right** (Jay, 2026-09-22): `KortixBottomSheetModal titleTrailing` → `SheetTitleRow trailing` (`components/kortix/sheet.tsx`; the slot mirrors the close button's, `-mr-2.5`, so the title stays centred). `Button variant="ghost" size="icon" rounded-full`, 20pt `CopyIcon` → `CheckIcon` for 1.5 s, `haptics.success()`, 10pt hit slop. It shows only when the file's text has loaded — never for an image, a file that is not rendered, or a failed load. **Pinned bar — the project drawer's bottom bar, copied** (Jay, 2026-09-22: "copy that exact same approach"; `components/kortix/pinned-bar.tsx` `PinnedBar`, the drawer's values): the document fills the sheet to its bottom edge and scrolls **under** the bar; the bar is `absolute inset-x-0 bottom-0`, height = safe-area bottom + 16 (`PINNED_BAR_BOTTOM_GAP`) + control height + 36 (`PINNED_BAR_FADE_ABOVE`); behind the controls a `LinearGradient` of the surface, alpha 0 → 0.85 at 45% → 1, `pointerEvents="none"`, and the bar itself is `box-none` so the transparent top passes touches to the document. The controls sit 16pt above the safe-area edge. **Not a solid footer, and no separate fade strip over the document's end** (the first build, rejected). The content ends `usePinnedBarInset(40)` above the edge (safe area + 16 + 40 + 16 `PINNED_BAR_CONTENT_GAP`): `FilePreviewBottomInsetContext` (`FilePreviewRenderers.tsx`, default 0 so `FileViewer` is unchanged) pads the markdown, text and CSV scroll views, adds `padding-bottom` to the generated code / JSON HTML and hides their language badge (it would sit under the bar), and sets the HTML WebView's `contentInset` (iOS only; on Android a page's last ~106pt can rest under the bar). The file card, the loader and the failure state centre above the bar with the same inset. **No fade under the title row** (Jay, 2026-09-22: not on a text file, not on a page in a WebView — it veils the first lines). The renderers paint the page background, so this sheet's surface is `THEME.*.background` too (`backgroundStyle`, the one deliberate chrome override here) and the bar fades to it: title row, document and bar are one plane in both themes. **Controls: two equal cells** (`PinnedBar className="gap-2 px-4"`), both `Button` **default size** (`h-10`, never `sm`) `flex-1 rounded-full`, icon 18pt + label: **Download** (`secondary`, `DownloadSimpleIcon`, `KortixLoader size="small"` in the icon's place while it runs; `downloadOpenCodeFileToCache` streams to disk, then **the device opens the file — never the share sheet** (Jay, 2026-09-22): `openFileOnDevice` (`lib/files/open-on-device.ts`). **Android:** a `VIEW` intent on the file's content URI with the read grant (`expo-intent-launcher` + `FileSystem.getContentUriAsync`, MIME type from `mimeTypeForFile`, `lib/files/mime-type.ts`, tested) — the phone starts its PDF, slides, spreadsheet or text app; part of the Expo SDK, so it works in Expo Go, where Jay tests (Samsung S24). The launch promise resolves only when the user returns, so it is raced against 500 ms and the button never stays busy. **iOS:** Quick Look (`@react-native-documents/viewer`, a custom native module: a development or store build has it, Expo Go does not). No app for the type (`isNoAppError`, `lib/files/no-app-error.ts`, tested) → `toast.info` "File downloaded. No app on this device can open it." A binary without the needed module (each is required lazily, OTA-safe like `PlatformButton`) → `toast.info` "File downloaded. Update the app to open it." **Never the share sheet and never an in-app viewer as a fallback** — Jay rejected both. The file opened but failed to launch → `toast.error` "File downloaded, but it did not open. Try again." Download failure = `toast.error`; disabled for a missing file) · **Add to chat** (primary, `PlusIcon`; disabled when a rendered file did not load; enabled for a file that is not rendered). It fires `haptics.selection()`, closes both sheets, and the draft gets `@<workspace-relative path> ` at its end (`appendFileMention`; tracked as a file mention; a file already mentioned is not added twice). Closing the preview returns to the list with the search text kept. The preview body is a plain `View className="flex-1"`, not `BottomSheetView`, which sizes to content and collapses the preview. Empty: `Text variant="muted"` "No files in this session yet" / "No matching files". The body mounts only while the sheet is open, so the memoized composer never subscribes to streamed messages. An active AutoContinue mode shows a `secondary` `icon-md` button (18pt `InfinityIcon`, `text-kortix-purple`) after the model pill. Onboarding hides `+` and the model pill |
| Chat input position | Pinned to the bottom with `px-4`. Bottom gap = `pb-3` (12pt) on both screens — one step below the `px-4` edge (§2; Jay, 2026-09-21: "if px-4 make pb-3, if px-6 make pb-5"): at rest `insets.bottom + 12`, and 12pt above the keyboard while typing. Home: `COMPOSER_BOTTOM_GAP` / `KEYBOARD_GAP` = 12, the composer follows the keyboard by `translateY`. Thread: `SessionChatInput`'s wrapper is `px-4 pb-3`, and the bottom area's `insets.bottom` padding collapses with the keyboard's progress (`useReanimatedKeyboardAnimation`), so the home-indicator inset is not added above the keyboard. Text, files, and model stay until the send succeeds |
| Add (`+`) | Opens `AttachSheet` (`components/session/`) on project home and in a thread — never a native action sheet or `Alert` (Jay, 2026-09-21). No title. One row of three equal tiles, `gap-2`: Camera (`CameraIcon`) · Photos (`ImageIcon`) · Files (`FilesIcon` — a document glyph, never a folder; Jay, 2026-09-21), data in `lib/session/attach-sources.ts` (tested). Tile: `bg-secondary rounded-2xl min-h-24`, 24pt icon over `Text variant="small"`, press = `usePressScale(0.96)` + `haptics.tap()`. `PickerSheet`'s layout: 16pt sides, 4pt under the handle, 16pt between blocks, bottom `max(insets.bottom, 16) + 8`. A tile closes the sheet and its source opens from the sheet's `onDismiss` (`closeThen`) — never two overlays, no timer: Camera → the camera, Photos → the system photo picker (multi-select, no permission prompt), Files → the system file browser (multi-select). Camera refused → `AlertDialog` "Camera access is off" · Cancel · Open Settings (`Linking.openSettings`). A source that fails to open is a `toast.error`. Both platforms get all three. Files show as chips (thumbnail · name · remove). They upload after the new session connects |
| Model list | **One list per project, the same on web, project home, and a thread** (Jay, 2026-09-21; rules ported from web's `model-selector.tsx` into `lib/session/model-picker.ts`). LLM gateway on: the project's `/model-picker` catalog (`useProjectModelCatalog`), `enabled !== false`, without the `auto` alias; every model runs through the `kortix` provider. In a thread the sandbox's copy of a model wins when it exists (it carries the thinking levels). Gateway off (no catalog): the sandbox's own providers without `kortix`, newest per family (`filterToLatestModels`); project home then has no list and hides the pill. Groups = the real upstream provider (`provider`, else the wire id prefix) — never the raw provider name, which is always "Kortix" under the gateway. Group order `PICKER_PROVIDER_ORDER` (Kortix first), unknown providers after it by title; rows by name. "ChatGPT subscription" rows drop their "(ChatGPT)" suffix; search still matches it. The label and order tables are copies of `@kortix/llm-catalog` (Metro cannot resolve it), pinned by `model-picker.test.ts` |
| Model pill | Label = the pick, else the project default (home, sent as `opencode_model`) or the resolved model (thread, "{model} · {level}"). Hidden while the list loads. **"Connect model"** when the list has loaded and the project offers no model (web's "No model connected"). Opens `ModelPickerSheet` |
| Model sheet | `ModelPickerSheet` → `PickerSheet` (`components/session/`). Layout: **16pt sides** — the project edge, `px-4` (Jay, 2026-09-21; the model sheet, the agent sheet, and the thread's Add sheet all use it, the app's other action sheets stay 20pt) — 4pt under the handle, 16pt between blocks, bottom `max(insets.bottom, 16) + 8`; the sheet sheet has two stops, like the Customize sheet (Jay, 2026-09-22): it opens at its content's height (at most 70% of the screen; 85% when the list is long enough for the search field), and a drag up takes it to full screen under the status bar. Its title is `KortixBottomSheetModal title`: the close button at the far left and the title centred, in the handle area above the scrolling list, the same row as the Customize sheet. Every group title (`Text variant="muted"`, `mb-2`) keeps the `px-2` inset — a 24pt line; `PickerSheet` renders its own group titles because `SettingsGroup`'s are `px-4`, which Jay rejected here (2026-09-21). `SheetTextInput` "Search models" when the list has more than 8 rows (`PICKER_SEARCH_THRESHOLD`; the sheet then opens at a fixed 85% so it does not resize while filtering; else it sizes to content, max 70%), then one `SettingsGroup className="bg-secondary"` per provider of picker rows: label · check, no chevron, no description, no model id. Search matches name, provider, and model id. A model applies with `haptics.selection()` and **the sheet stays open** (`closeOnSelect={false}`; Jay, 2026-09-21): the check moves, the Thinking slider switches to the new model's levels, and the user closes with a swipe or a tap outside. Only the model sheet does this — the agent sheet closes on a pick. No result: `Text variant="muted"` "No matching models". No models at all: "No models available" (`large`) · 24pt · primary `Button size="lg" rounded-full` "Connect provider" — a gateway project opens web's `/projects/:id/customize/models` in the browser (mobile has no gateway model screen), any other project opens the LLM Providers page |
| Thinking | **Project home and the thread** (Jay, 2026-09-21), in the model sheet above the list, only when the active model has levels. Levels: the thread reads the sandbox model's `variants`; a gateway catalog sends `variants: {}`, so `catalogThinkingLevels` (`model-picker.ts`, web's rule) reads `reasoning_options` — the `effort` knob's values, else low/medium/high for `budget_tokens`, else none — and a sandbox model with no levels takes the catalog's. The level is stored per model in `useLocalConfigStore.modelVariants["kortix/<modelID>"]`, the store the thread reads, so home and thread always show the same level; the pill reads "{model} · {level}" on both. First message: a level cannot ride `initial_prompt`, so a text-only home send with a level creates the session with `pending_prompt { text, agent: null, model, variant }` (web's channel; the server delivers it), and a send with files adds `model` + `variant` to its `prompt_async` body. No level set → the send is unchanged (`initial_prompt`). A **stepped slider** (Jay, 2026-09-21: "the slider approach like ChatGPT"), not a segmented control: header row `px-2` — "Thinking" (`muted`) left, the stop under the thumb (`small`) right, `items-baseline`; track 44pt `rounded-full bg-secondary overflow-hidden`; stops = Default, then each level, evenly spaced, each a `size-1.5 bg-muted-foreground/50` dot; fill `bg-foreground/10` from the left edge to the thumb; thumb 36pt `bg-primary` circle, 4pt inside the track. The thumb follows the finger 1:1 with `haptics.selection()` on each stop crossed; release snaps to the nearest stop with `withSpring({ duration: 300, dampingRatio: 1 })` carrying the drag velocity (no bounce) and applies the level; a tap on the track jumps there. Only `transform` animates. Reduced motion: no spring. The pan fails on a 12pt vertical move, so the sheet still scrolls and closes. `accessibilityRole="adjustable"` with increment / decrement. The sheet stays open. Stop math: `stopOffset` / `nearestStop` (`composer-config.ts`, tested). There is no separate Thinking tab, sheet, or toolbar button. The 3-tab "Configuration" sheet is deleted — do not add it back |
| Agent | **Header, right side, on project home and in a thread — never in the composer** (Jay, 2026-09-21). `AgentPill` sits inside `ProjectHeaderActions`, `FloatingMenuButton`'s child; the row is `justify-between`, so the hamburger holds the left edge and the actions the right. The `···` button follows the pill and holds the edge, so the pill passes `edge={false}`: `Button variant="ghost" className="rounded-full bg-background"`. Alone in a row (`edge`, the default) it adds `-mr-2.5` (mirrors `MenuButton`'s `-ml-2.5`), agent name (capitalised) + 14pt `CaretDownIcon`. It opens a `PickerSheet` titled "Agent" (label · check; search above 8 agents; a pick applies and closes). Agents = `pickableAgents` (primary or all — a missing mode counts as all — not hidden, not `enabled: false`). Hidden with fewer than two. Thread: the sandbox's agents, the pick is `useLocalConfigStore.selectedAgent`. Project home has no sandbox: the project config's agents (`useProjectDetail` → `/detail`), active = `homeAgentName` (web's order: the pick made on home, else the project's `default_agent`, else the last-used agent; null → no agent is sent and the pill reads "Agent"). A home pick also becomes the store's last-used agent, so the thread's pill shows it. Sent on create as `agent_name` (plus `pending_prompt.agent`, or `agent` in the files path's `prompt_async` body) |
| Chrome | One floating 40pt row, three controls (Jay, 2026-09-21): hamburger (left) · `AgentPill` · `···` (right). It belongs to the project screen, not to the home: project home, a thread and a connecting session share it. `ProjectHeaderActions` renders the pill and the `···` button: `Button variant="ghost" size="icon" className="-mr-2.5 rounded-full bg-background"`, 20pt `DotsThreeIcon`, 10pt hit slop, always visible (the pill hides with fewer than two agents). No `···` in onboarding. It dismisses the keyboard and opens the project sheet |
| New header controls (design law) | A control added to any header or chrome row — the project chrome (Chrome, above), a `PageHeader`, or anywhere else a button gets introduced — prefers floating over a gradient fade of the surface behind it, not a flat `bg-background` icon button sitting directly on the page (Jay, 2026-09-22). The recipe already exists: `PinnedBar` (`components/kortix/pinned-bar.tsx`) — clear → 85% at 45% → solid, ending on the real surface colour, never `transparent` (a grey band on Android). The drawer's own New session · avatar row (`ProjectLeftDrawer`) is the reference example Jay pointed at: the identical gradient, hand-rolled inline there because it predates the `PinnedBar` extraction and has not been migrated onto the component. Reuse `PinnedBar` (or its exported `PINNED_BAR_FADE_ABOVE` / `PINNED_BAR_BOTTOM_GAP` constants for a custom shape) instead of a second hand-rolled `LinearGradient`. This is a preference for controls added from here on — it does not mean retrofitting the existing flat controls documented above (`ProjectHeaderActions`'s `···`, `PageHeader`'s control row) unless asked |
| Project sheet | `CustomizeSheet`, data in `lib/session/dock-menu.ts` (`CUSTOMIZE_SHEET_GROUPS`, pinned by `dock-menu.test.ts`). `PickerSheet`'s layout: 16pt sides, 16pt between groups, scrolls. Two stops (Jay, 2026-09-22): it opens at its content's height (the dynamic snap point, at most 85% of the screen), and a drag up takes it to full screen under the status bar. **Every sheet does this** (Jay, 2026-09-22): `KortixBottomSheetModal` adds the `'100%'` stop after the stops a call site passes (`withFullDetent`, `lib/ui/sheet-detents.ts`) and defaults `topInset` to the safe-area top, so a sheet opens at the size its call site asked for and always expands to full screen. Title row (Jay, 2026-09-22): `KortixBottomSheetModal title="Customize"` (web's name for the section). The component draws it for every titled sheet, in the handle area so it stays put while the content scrolls: a close button at the far left (`Button variant="ghost" size="icon"`, `-ml-2.5 rounded-full`, 20pt `XIcon`, label "Close"), the title centred (`Text variant="large"`, role header), a 30pt spacer that balances the button. Its paddings, the 20pt top corners, the backdrop and the handle are `SHEET_DEFAULTS` in `components/kortix/sheet.tsx`: one place for every sheet in the app. Two `SettingsGroup`s of `SettingsRow`s (icon · label · chevron). Group one, untitled, the core sections: Agents, Skills, Schedules, Review, Secrets. Models has no row and no page (Jay, 2026-09-22): mobile has no models screen at all; `LlmProvidersPage`, `ProviderLogo`, and `lib/kortix/custom-provider-config.ts` are deleted, and `page:llm-providers` no longer exists. Group two, "More": Files, Webhooks, Members, Terminal. Channels and Sandbox have no row and no page (Jay, 2026-09-22): `ChannelsNavPage`, `ChannelsPage`, `SandboxPage`, `page:channels`, `page:channels-nav` and `page:sandbox` are deleted. Commands has no row and no page (Jay, 2026-09-21): web deprecated commands, so `CommandsPage`, `page:commands` and the `command` configure prompt are deleted. Dev has no row (Jay, 2026-09-21): `DevPage` is web's "Develop on your own machine" guide, `git clone` and terminal commands for a laptop; it stays in the repo, unreachable. A row closes the sheet and calls `navigateToPage`, so the page renders in the `view` route and the stack stays `[index, X]`. **Back from a page returns to the thread it was opened over** (Jay, 2026-09-21): a page and a thread share the `view` route and opening a page clears the store's active thread, so `ProjectScreen` remembers it (`returnThreadForPage`) and Android back or a page's own back control reopens it (`pageBackMove`, both in `lib/session/project-stack.ts`). The sandbox did not change, so the thread shows at once. A page opened from project home goes home. iOS has no back gesture in the project stack (the left edge opens the drawer), and these pages show the hamburger, so on iOS the way back is the drawer's session row. **Connectors have no row: mobile leaves connectors to web** (`ConnectorsPage` stays in the repo, unreachable). Review opens `ReviewPage` and shows the count of items that wait for the user (`countReviewItemsBySegment(...).needs_you`) as its `value`. Changes has no row and no other entry point (Jay, 2026-09-21: one page for changes, Review): open change requests are review items, Review's `+` opens one, and Review's history button opens the branches in a sheet. `ChangesPage` stays in the repo for its `OpenCRSheet` and `BranchRow`.  The same sheet opens from a tool page's `PageHeader` `···` |
| Tool page header | `PageHeader` (`components/kortix/page-header.tsx`), on every project tool page (Agents, Skills, Schedules, Review, Secrets, Webhooks, Members, Terminal, …). The Sessions page's layout (§7), so every project page reads the same (Jay, 2026-09-21). Control row: `px-4 pb-3`, top padding `max(inset, 10) + 6`, 56pt min height, `justify-between` — hamburger left (`MenuButton`, `-ml-2.5`); right, in this order: the page's own `rightActions`, then `+` (`onAdd`, the page's one create action, `addLabel` e.g. "New agent"), then `···` (opens the project sheet). Every control is `Button variant="ghost" size="icon" className="rounded-full"` with a 20pt icon; the group carries `-mr-2.5`, so the last glyph sits on the 16pt padding edge. Title row below: `h-10 px-4 pb-1`, `Text variant="h3"` (24pt Roobert semibold, foreground), role header, one line. **The title is never a grey 16pt label beside the hamburger, and `+` never sits beside the search field**: `SearchListHeader` on these pages takes no `onAdd`, so the field spans the full width under the title |
| Agents and Skills | `AgentsPage` and `SkillsPage` are thin wrappers over `ConfigEntriesPage` (`components/pages/ConfigEntriesPage.tsx`), one list and one detail for both (Jay, 2026-09-21/22). List: `PageHeader` with `onAdd` (starts a configure session), full-width `SearchListHeader`, then one `SettingsGroup` of `SettingsRow`s in a `px-4` column (Jay, 2026-09-22; the app's list, never `ListRow`). **No icon tile on a row.** A row is two lines: the name (`label`), then the description on one line below it (`SettingsRow description`, the one list-row exception to icon · label · trailing); an agent's second line starts with its mode (`Primary · description`). The default agent carries a filled 14pt `StarIcon` (`THEME.accent.orange`) before the chevron. Loading: three `Skeleton` rows. Empty: one muted line ("No agents yet" / "No matching agents"). Detail: the entry's name is the `PageHeader` title, hamburger only — **no Go back button** (Jay, 2026-09-22): the detail is local state, not a pushed screen, and the project screen's left edge is already claimed by the drawer's swipe, so a back gesture here would fight it. Android hardware back closes the detail (`BackHandler`, registered in `ConfigEntriesPage`); iOS has no back gesture either, so the hamburger + drawer is the way elsewhere, same as the top-level project pages. Header `rightActions`: Copy (`CopyIcon`, a `CheckIcon` for 1.5 s after a copy) and Edit (`PencilIcon`, starts a configure session), before `···`. **No file path.** Below the title: the mode as `Badge variant="secondary"`, the description as `Text variant="muted"`, then one `SettingsGroup` with one row — "Set as default agent" (`StarIcon`, `right={null}`), or "Default agent" with the filled star and a check on the current default; hidden for a subagent and on Skills. Then the file's markdown body (`SelectableMarkdownText`, frontmatter stripped). Both scroll views fade at both ends (`components/kortix/scroll-fade.tsx`, the drawer's fades as components, no touches): `BottomFade` always — the page background over the last 56pt plus the safe-area inset, and the content pads by the same height; `TopFade` — 24pt under the search field or the header, invisible while the list rests at its top and coming in over the first 24pt of scroll (`useScrollFade` on an `Animated.ScrollView`, `scrollEventThrottle={16}`), so a row passing under the header dissolves instead of being cut. Both gradients run to the SAME colour at zero alpha, never to `transparent` (black at zero alpha: a grey band on Android) |
| Schedules and Webhooks | `PageList` of one `SettingsGroup` of `SettingsRow`s (Jay, 2026-09-22; no icon tile): name, then `schedule · last run · agent` as the `description` (webhooks: `Signed/Unsigned · last run · agent`); a 6pt `StatusDot` before the chevron (green active, grey paused). **No "N of M active" line** (Jay, 2026-09-22). The create and detail sheets open at full screen (`snapPoints={['100%']}`, one stop; Jay, 2026-09-22: never a lower first stop). Detail sheet (schedules): the body sits in `SheetFill` (`components/kortix/sheet.tsx`) so the Fire now · Pause · Delete row is a `PinnedBar` floating over a fade of the sheet with the settings groups scrolling under it, the agent and skill detail's fade (Jay, 2026-09-22) — never a solid bar. `SheetTitleRow` with the trigger's name — **no status badge, no slug, no icon tile**; a scroll body of `SettingsGroup className="bg-secondary"` rows (Runs · Cron · Time zone), the prompt as a **read-only tile** (`PromptPreview`: muted `px-4` title, a `rounded-2xl bg-secondary` tile with up to four lines and a chevron) — **never an editable field in the sheet's scroll view**: a multiline input there takes the touch, so a scroll that starts on it focuses it and raises the keyboard (Jay, 2026-09-22). A tap pushes `PromptEditView` in from the right (`sheet-push`): title "Prompt" with a Back chevron, an auto-focused `SheetTextInput` (160pt min), the placeholder line, and a pinned Save pill; a saved prompt pops back to the detail. Both live in `components/pages/TriggerPromptField.tsx`, shared by the schedule and the webhook sheets. Then the Agent and Model dropdowns (their titles use the same muted `px-4` style as "Prompt"), then Last fired · Source rows; and a **pinned action bar** under the scroll view, always on screen: Fire now (`lg`, flex-1) · Pause/Resume (`secondary lg`) · Delete (`secondary icon`, destructive glyph), on the sheet colour with the safe-area inset below. Agent and Model are the app's dropdown (`@/components/ui/select`, trigger `h-11 rounded-xl border-0 bg-secondary shadow-none`) with `portalHost={OVERLAY_PORTAL_HOST}`: the host mounted after `BottomSheetModalProvider` in `app/_layout.tsx`, because the default host sits under an open sheet on Android. Model options come from `/model-picker` (8–13 enabled models), never `/llm-catalog` (thousands of rows froze the sheet) |
| Secrets | `PageList` of one `SettingsGroup` of `SettingsRow`s (Jay, 2026-09-22): the name, then `Required/Optional · status · scope` as the `description`; an orange 6pt dot when a required secret has no value; no footer (Jay, 2026-09-22: mobile has no models screen to link to). Detail sheet: `SheetTitleRow` with the secret's name, then `SettingsGroup className="bg-secondary"` groups — "Use in my sessions" (Shared value / My value as picker rows with a check, only when there is a choice), "My value", "Shared value" — never a segmented pill or uppercase labels. A value form **pushes in** over the detail with the activity sheet's motion (`components/kortix/sheet-push.tsx`: `PUSH_IN` from the right, `POP_IN` back from the left, 250ms strong ease-out, none under reduced motion); the pushed form's title row shows `SheetBackButton` in the close button's slot |
| Settings | `SettingsNavPage` (`page:settings`), opened only from the drawer's gear button, top right of the logo (Jay, 2026-09-22) — no dock row, no `PageHeader onBack`. `PageList` of `SettingsGroup`s **with no group title** (Jay, 2026-09-22: just the rounded card, no "General"/"Repository"/"Danger zone" label above it — the gap between groups is the only separation). Every sheet renders through `KortixBottomSheetModal` directly (Jay, 2026-09-22: not the `<Sheet>` wrapper), the Schedules/Webhooks/Secrets shape. **General** — Project name (value = current name, tap opens a rename sheet: `KortixBottomSheetModal title` + one `SheetTextInput` + Save, the app's edit-a-field pattern — never an inline input on the page). **Repository** — Repository (icon Github when a GitHub URL resolves else `GitBranch`, value = `owner/repo`, external + tap opens it), Default branch and Manifest path (value rows, tap opens the same rename sheet, `mono`), Add collaborator (managed repos + managers only, opens a small invite sheet: GitHub username field, Can edit/Can view toggle, Add). No experimental-features row (Jay, 2026-09-22: `useUpdateExperimentalFeature` stays in `lib/projects/hooks.ts`, unused by this page). **Danger zone** (managers only) — Delete project, `destructive`, a two-step confirm (Jay, 2026-09-22, GitHub's repo-delete shape): a sheet asks to type the exact project name to enable `Continue` (`variant="destructive"`, disabled until the typed text matches `project.name`), then **the app's `AlertDialog`** (`@/components/ui/alert-dialog`; Jay, 2026-09-22 — never `Alert.alert` for a confirm/cancel decision, only for a one-button acknowledgement) "Are you sure?" opens after that sheet dismisses — the first consumer of `AlertDialog` in the app. `AlertDialogAction` needs an explicit destructive override (`className="bg-destructive active:bg-destructive/90 dark:bg-destructive/60 text-white"`): its own component always renders the `default` button variant. Confirming calls `archiveProject` (there is no hard delete; "Delete project" is the live label web itself shows over that call) and redirects to `/projects`. Every edit row is read-only (no `onPress`, no chevron) for a non-manager |
| Members | `MembersNavPage`: no in-page heading, no description, no bordered cards (Jay, 2026-09-22). `PageHeader` with `onAdd` = Invite member (managers only) → the invite sheet (email `SheetTextInput`, a "Role" `SettingsGroup` of picker rows, an `lg` Invite pill; success is a toast). The page is titled `SettingsGroup`s with both scroll fades: "Pending invites" (mail icon · email · role or "Expired"; a tap opens a sheet with Resend and Revoke rows), "People" (28pt avatar · email · role as `value`; a chevron only when the viewer can edit that member — account owners and admins hold Manager by their account role), "Groups" (group · role, plus an "Attach a group" row for managers). Loading: three `Skeleton` tiles |
| Files | `FilesNavPage` (`/projects/[id]/files`, the drawer's Files pill; also a Customize sheet row): the read-only git browser. `PageHeader` with the title and `···` only; a full-width search; a breadcrumb of chips on a 40pt line (never clipped) only inside a folder. Entries show their name without the extension (`package`, not `package.json`; `displayNames` keeps the extension on names that would collide once bare, e.g. `kortix.html` beside `kortix.yaml`; the full name is the file sheet's title) and a filled glyph: a folder in its tone (`lib/files/folder-tone.ts`: one of the six `THEME.accent`s by a stable hash of the name, case and leading dot ignored); a file by its type (`lib/files/file-icon.ts` → `components/files/file-icons.tsx`: one glyph per web language — TSX, TS, JS, JSX, HTML, CSS, Vue — plus PY, RS, C, C++, SQL, MD, TXT, CSV, INI, PNG, JPG, SVG, PDF, DOC, XLS, PPT, ZIP; the Kortix symbol in the theme's colour at 0.72× the glyph size on `kortix.*`, since the symbol fills its box and the glyphs do not; the git glyph on `.git*`), filled, in the muted foreground — filled reads lighter than the app's bold outline. List: `SettingsGroup`s "Folders" and "Files" of `SettingsRow`s (22pt glyph · name · size as value). The grid is the default view (Jay, 2026-09-22). Grid: 2-up `bg-card` tiles — 36pt glyph, `Text variant="small"` name, `text-xs` muted size on a file, no "Folder" line. Inside a folder the title is the folder's name and the hamburger stays — **no Go back in its place, on any page** (Jay, 2026-09-22); a crumb goes up, and Android back goes up one folder (registered after ProjectScreen's handler), never project home. **No press animation** (Jay, 2026-09-22): rows and tiles use the pressed fill only. All controls live in a `PinnedBar` at the bottom, the drawer's bar: version pill (`GitBranchIcon` · ref, opens the Version sheet, a `SettingsGroup` of picker rows) at the left, then sort and download (`DownloadSimpleIcon`) as `secondary icon` buttons with the list/grid toggle between them: the app's `Tabs` (`TabsList` as a 40pt `bg-secondary` pill, two icon `TabsTrigger`s, the active one on the page background), never two icon buttons. No refresh button: a pull on the list refetches the files and branches (`RefreshControl`, tint `mutedForeground`), as on every other list. A file opens in a `KortixBottomSheetModal` at full height with `enableContentPanningGesture={false}` and the page background: the file name as the title, `CopyContentButton` as `titleTrailing` once the text has loaded, `FilePreview` as the body under `FilePreviewBottomInsetContext`, and a `PinnedBar` with Download · History (two equal `secondary` cells) — the recent-files sheet's layout. History pushes in (`sheet-push`) as a `SettingsGroup` of commit rows, and a commit pushes its `PatchDiffView` in after it; Back walks back. Never a `Modal`, never a hand-animated sheet |
| Review | `ReviewPage` (`page:review`): web's Review Center without bulk select, keyboard layer, or per-session grouping. `PageHeader` with `rightActions` = one `icon` ghost button and `onAdd`, so the control row reads history · `+` · `···` (Jay, 2026-09-21): `ClockCounterClockwiseIcon` "Versions" → a sheet that lists the project's branches (`BranchRow`; title "Versions"; scrolls, at most 80% of the screen; the branches load when the sheet first opens), then `onAdd` ("Open a change request") → `OpenCRSheet` at 88% (on success: Needs you segment, list refetch, success toast). Versions is a sheet, not a tab. Segments: `ToggleGroup type="single" variant="outline"`, three equal items — Needs you, Waiting, Done (label plus count, "Needs you 2"). Rows: `ListRow`, kind icon (`components/review/review-icons.ts`) in the item's tone (`reviewItemTone`: an open item takes web's kind colour — change blue, approval orange, output purple, decision yellow, batch green; a finished item takes its outcome — approved or done green, rejected red, changes requested orange, dismissed muted), title, one subtitle line `summary · agent · age · risk`. Empty segment: one muted line, no icon, no button. A failed background refetch keeps the list. The item model and its mapping come from `@kortix/sdk` (`mapApiReviewItem`, `reviewSegmentForStatus`, `reviewItemTarget`); copy and rules are `lib/review/review-meta.ts`, the verdict call is `lib/review/review-verdict.ts` (both under `bun test`). `ReviewDetailSheet`: `PickerSheet`'s layout, at most 90% of the screen; title, kind and risk `Badge`s, agent; one body per kind (change: what changed, impact, conflicts, checks, requested changes, `PatchDiffView`; approval: each call's arguments, or "You do not have access" when the API withholds them; decision: options as `SettingsRow`s, a tap answers; output: note, text preview, external preview row; batch: children with `value`). Verdicts only while `status === 'needs_you'`, in the sheet's pinned footer (`BottomSheetFooter`, sheet background, 12pt above, safe-area below; the scroll content pads by its height), so they stay on screen over a long change. Two verdicts share one row, 50/50, the primary on the right: a change is Request changes · Merge (**no Close on a change**, Jay, 2026-09-21), an approval is Deny · Approve. Three verdicts stack as full-width `lg` pills, first `default`, Request changes `secondary`, the rest `ghost`. Request changes swaps the pills for a `SheetTextInput` + Cancel / Send. Merge a change and Approve or Deny a connector call confirm in an `AlertDialog` that opens after the sheet has closed. A Change Request or a connector call never reaches `/act`: `cr:` ids merge / close / request changes, `call:` ids go to `resolveApproval` |
| Thread top fade | `FloatingMenuButton fade` (the thread only: project home and the connecting state do not scroll). A header strip of the page background behind the button, `pointerEvents="none"`: solid from the screen edge to the bottom of the button (`insets.top + 8 + 40`), then a 24pt `expo-linear-gradient` (alpha 1 → 0) below it — the gradient starts under the header row, never inside it (Jay, 2026-09-21). The strip ends at `insets.top + FLOATING_MENU_CLEARANCE` (72pt: 8 offset + 40 button + 24 fade). The list's top inset is the same clearance, so the first turn and the pinned newest turn rest below the fade and are never dimmed; turns that scroll above it fade out, then are hidden behind the header row and the status bar. Mirrors the 24pt fade above the chat input |
| Never | A greeting sentence, the project name, starter chips, recent-session lists, cards, widgets, subtitles |
| New chat, no messages | `SessionPage`'s `FreshSessionHero` renders the same `ProjectHero` (same size, same keyboard scale) |

## 6. Project sidebar

The left drawer on `/projects/[id]` (`components/session/ProjectLeftDrawer.tsx`).
It opens from every project page: the hamburger, or an edge swipe (80pt edge,
`swipeEnabled` while the project screen is focused) on any project route.
A swipe closes it (no close button). Every action calls `onClose()` before it
navigates. Navigation from the drawer never deepens the project stack beyond
one screen over home:

| Action | Project home on top | A covering route (`view`, `sessions`, `files`, `account`) on top |
| --- | --- | --- |
| Sessions / Files / avatar | push that route | replace the top route with it; nothing when it is already on top |
| Session row | tab store update → home pushes `view` | `view`: the row of the session on screen only closes the drawer; another row updates the store and the view swaps content. `sessions`/`files`/`account`: store update, the route replaces itself with `view` (`useCoveringRoute`) |
| New session | go to project home | go to project home + `popTo('index')` |
| All projects | `router.replace('/projects')` | same |

```
┌──────────────────────────────────────────┐  full screen width
│ [Kortix logomark]                    (⚙) │  gear opens Settings, Jay 2026-09-22
│   ▢ Sessions                              │  box edge 16pt, icon edge 28pt
│   ▢ Files                                 │
│   ▢ All projects                          │
│  ◌ Session title                          │  scrolls; 20 newest
│  ● Session title                          │
│  Previous chats                           │
│ ░░░░░░░░░░░░░░░░ fade ░░░░░░░░░░░░░░░░░░ │
│ [+ New session]                   (photo) │  pinned, 16pt above safe area
└──────────────────────────────────────────┘
```

| Element | Value |
| --- | --- |
| Drawer | `react-native-drawer-layout`, `drawerType="slide"`, width `100%`, transparent overlay, no shadow (`ProjectScreen`) |
| Drawer close | Faster than open (Jay, 2026-09-22): `closeSpringConfig={DRAWER_CLOSE_SPRING}` — `stiffness: 400, damping: 40, mass: 1`, critically damped, no overshoot: 90% of the travel in ~195ms, settled in ~330ms. Hamburger, session row, and swipe-release closes all use it; a swipe keeps its velocity. Open keeps the library spring (`1000 / 500 / 3`). The prop comes from `patches/react-native-drawer-layout+4.2.10.patch` (the library hardcodes one spring); without the patch `tsc` fails on the prop — run `npx patch-package`, then restart Metro with `--clear` |
| Surface | `bg-chrome-background` (the web `--sidebar` token), `insets.top` top padding. **One straight left line at 20pt** (Jay, 2026-09-16): the Kortix logo, the Sessions / Files / All projects icons, the session status marks, and Previous chats all start at 20pt — the header is `px-5`; every row is `px-3` inside a `px-2` column |
| Header row | `px-5 py-2`, `justify-between` (Jay, 2026-09-22: was logo-only). Logo on the 20pt line: `KortixLogo variant="logomark" size={18}` (90 × 18pt: the logomark is 5:1) in a 44pt-tall `View`, **not tappable** (`accessibilityRole="image"`, label "Kortix"). Far right, on the padding edge (`-mr-2.5 rounded-full`, matching the project header's `···`): a `GearSixIcon` `Button variant="ghost" size="icon"`, label "Project settings" — the only entry point to the project Settings page (`page:settings`). Only the All projects row opens the Projects list; the logo never navigates (Jay, 2026-09-16). No close button (Jay, 2026-09-16): an edge swipe closes the drawer |
| Nav rows | `NavPill` rows in a `px-2 pb-2` column, no gap: `Pressable` `flex-row items-center gap-3 rounded-xl px-3 py-2.5 active:bg-accent` (44pt), icon `size={18}` `text-foreground` on the 20pt line, label `Text` `font-medium`, one line. Order: Sessions (`ChatsTeardropIcon`, → `sessions`), Files (`FoldersIcon`, → `files`), All projects (`CustomizeIcon`, web's Customize glyph from `components/icons/customize-icon.tsx`, → `router.replace('/projects')`, the only way to the Projects list). Sessions and Files go through ProjectScreen's `navigateProjectRoute` (push or replace, see the table above) |
| Double-tap guard | Sessions, Files, All projects, and the avatar navigate once: the first tap sets `navigatingRef`, later taps do nothing. The guard resets when the drawer's visibility flips — `useDrawerProgress()` crosses 0.01 (fully closed, or visible again), read with `useAnimatedReaction`. No timer. The drawer content stays mounted while closed, so mount cannot reset it |
| Row style | Menu rows, not free-standing buttons: no resting fill, no border; the pressed `bg-accent` box and the open session's resting `bg-accent` box sit on the parent's `px-4` edge with the same `rounded-xl` radius |
| Session list | `ScrollView` under the nav rows, no indicator, `paddingTop` 4. Rows in a `px-2` column (status marks on the 20pt line): `Pressable` `flex-row items-center gap-3 rounded-xl px-3 py-2 active:bg-accent`. Open session: the row of the session on screen (an open thread or a connecting session, `shownProjectSessionId`) is `bg-accent` at rest with `accessibilityState={{ selected: true }}`, same font weight; tapping it only closes the drawer (`drawerSessionRowMove`), so the thread is not remounted or reconnected. Row = `SessionStatusMark` (the same marks as the Sessions page, see §7) · title (`Text` default, one line, `sessionDisplayTitle`). Label "{title}, {status}" (`sessionStatusLabel`). Rows are **every session of the project** (Jay, 2026-09-21 — no 20-row cap), newest `sessionLastActivityAt` first, from `useProjectSessionsPaged`: a `useInfiniteQuery` over the API's keyset pages (`listProjectSessionsPage`, 50 rows, `x-next-cursor`). The list is an `Animated.FlatList` (virtualised): `onEndReached` at 0.6 loads the next page (`shouldLoadMoreSessions`: one page at a time, never during a pull), a `KortixLoader size="small"` `py-4` shows under the rows while it loads, then Previous chats. Pull to refresh: `RefreshControl` (tint `mutedForeground`) refetches the loaded pages; a background poll never shows the spinner. The Sessions row opens the same list with search and groups |
| Loading | `KortixLoader size="small"` centred, `py-8` |
| Empty | `Text variant="muted"` "No sessions yet", `px-3 py-2` (aligned with a row's title) |
| Previous chats | `LegacyChatsSection` after the rows, `mt-2`, inside the scroll content. In a `px-2` column; its header and chat rows are `px-3 py-2`, so they start on the 20pt line (§2). Renders only when the account has pre-OpenCode chats |
| Session list top fade | A 24pt `expo-linear-gradient` of the drawer surface (`THEME.*.chromeBackground`, alpha 1 → 0) over the top of the list, `pointerEvents="none"`, so rows fade out under the nav pills instead of a hard edge. Its opacity follows the scroll offset (Reanimated, UI thread): 0 at rest, 1 after 24pt of scroll, so the first row is never dimmed |
| Bottom bar | Absolute at the bottom, full width, `pointerEvents="box-none"`. Background: an `expo-linear-gradient` fade of the drawer surface (`THEME.*.chromeBackground`) — alpha 0 → 0.85 → 1 at locations 0 / 0.45 / 1, `pointerEvents="none"` — from 36pt above the controls to the screen edge (the same fade as the floating tab bar). No solid fill. Controls: a `flex-row justify-between px-5` row (the New session pill on the 20pt line) whose bottom is `insets.bottom + 16` (16pt above the home indicator). The list's bottom padding is that offset + 44 + 16, so its last row rests above the controls and earlier rows fade out under them |
| New session | `Button size="lg" className="rounded-full"` — the default (primary) variant, 44pt, `NavigationArrowIcon` flipped horizontally (`scaleX: -1`), tip up-right (web's New session glyph, `size={20}`) + "New session", content width. `haptics.tap()` → `onClose()` → `onNewSession()` (ProjectScreen `returnHome`: reset the store and pop a covering route, so project home's composer starts the session) |
| Avatar | `ProfilePicture size={11}` (44pt, the `lg` pill height) from `useProfileEditor()` — the Account page's photo and name; initial when there is no photo. `Pressable rounded-full active:opacity-70`, 2pt hit slop, label "Account". No name or email. → `/projects/[id]/account` through `navigateProjectRoute` (push over home, or replace a covering route) |
| Haptics | `haptics.tap()` on every button and pill. Session rows: one `haptics.tap()`, fired by ProjectScreen (`handleOpenProjectSession`, or the close-only path for the open session's row); the drawer adds none |

Intentionally absent — do not add back:

- Search row and the command palette (deleted).
- The user card footer (name, email, plan, update dot). The avatar is the only account entry.
- A New session row at the top. New session lives only in the bottom bar.
- A Review button in the drawer. Review is a row of the project sheet (§5 → Project sheet), opened by the header's `···`.
- The sandbox-scoped Projects tree, and any collapsible section header or chevron over Sessions.
- Row descriptions, timestamps, or counts.

## 7. Project sessions page

`/projects/[id]/sessions` (`components/session/ProjectSessionsPage.tsx`),
opened by the sidebar's Sessions row (pushed over project home, or replacing
the covering route). It lists every
session of the project. Title, status, grouping, search, and relative time
come from `lib/session/session-list.ts` (unit-tested).

| Element | Value |
| --- | --- |
| Header | `SettingsHeader title="Sessions" largeTitle gutter="project" onOpenMenu`: the hamburger (opens the project drawer) alone in the control row, no Go back; the title "Sessions" below it as `Text variant="h3"` (`accessibilityRole="header"`) in an `h-10 px-4 pb-1` row, above the search field |
| Search | `SearchListHeader` (16pt `px-4`, its project default), placeholder and label "Search sessions". Shown only after loading and only when the project has at least one session; when the list becomes empty the query is cleared. Trimmed, case-insensitive match on the display title (`filterSessionsByTitle`). Clear button label "Clear search" |
| Groups | Today · Yesterday · This week (2 to 7 local calendar days before today) · Older, by `sessionLastActivityAt`, newest first (`groupSessionsByActivity`). Empty groups are omitted. Headers show only when more than one group has sessions (`showHeaders`): `Text variant="muted"`, `mb-2 px-4`, role header, 18pt above every header but the first |
| Data | `useProjectSessionsPaged` — the drawer's paged list (50 rows a page). `onEndReached` at 0.6 loads the next page, with a `KortixLoader size="small"` footer while it loads. Search and groups work on the rows loaded so far; a search with few matches leaves the list short, so it reaches its end and keeps loading pages until the matches fill the screen or the list ends. `useProjectSessions` (one page, flat) stays for lookups only |
| List | `FlatList`, **one item per group** (Jay, 2026-09-22): a `SettingsGroup` of `SettingsRow`s, the settings screens' layout, so the page follows `SettingsGroup`'s look (row tiles, 2pt gaps) with no copy of it. The group's `title` shows only when more than one group has sessions; 18pt between groups (the settings screens' gap). No indicator, `paddingHorizontal: 16` (the project edge), top padding 4, bottom padding `usePinnedBarInset(44)` so the last row rests above the New session button, keyboard dismisses on drag. A group is not virtualised row by row; pages stay 50 rows |
| Row surface | `SettingsGroup`'s. Never a hand-rolled `bg-card` row with `rounded-t-2xl` / `Separator` (the first build: it missed the 2026-09-22 tile redesign) |
| Row | `SettingsRow`: `leading` = `SessionStatusMark` · `label` = title (`sessionDisplayTitle`, one line) · `value` = time (`shortRelative`: now, 5m, 2h, 3d, 2mo, 1y) · `right={null}` (no chevron: the time holds the right edge). `onLongPress` + `longPressLabel="Session actions"` + `accessibilityLabel` / `accessibilityHint` are `SettingsRow` props added for this row (a screen reader gets the long press as a named action). The clock re-renders every 60s and on each refetch. No description |
| New session | **Pinned at the bottom right, the project drawer's button** (Jay, 2026-09-22): `PinnedBar controlHeight={44} className="justify-end px-5"` over a fade of the page background; `Button size="lg" rounded-full`, `NavigationArrowIcon` 20pt flipped (`scaleX: -1`) + "New session". `haptics.tap()` → `newSession()` from `useProjectRoute` (ProjectScreen's `returnHome`: reset the store, pop this covering route), so project home's composer starts the session. The list scrolls under it. Hidden while the page loads |
| Tap | Opens the session once per page (`useCoveringRoute`'s `leavingRef`). The view route replaces this page (`StackActions.replace`), so the stack stays one screen over project home. The page fires no haptic; ProjectScreen's `handleOpenProjectSession` fires one `haptics.tap()`. If the store opens a session without a tap (notification, deep link), the page replaces itself with the view the same way |
| Long press | `haptics.medium()`, then the **session options sheet** — `KortixBottomSheetModal` directly, never the `Sheet` wrapper (Jay, 2026-09-22). `SheetTitleRow title={session title} hideClose`: **no X button**, the title centred; a swipe down or a tap outside closes it. `enableDynamicSizing` + `snapPoints={['100%']}` + `topInset`: it opens at its content height and **drags up to full height**. One untitled `SettingsGroup` on the 16pt edge (`px-4`, the title row's inset). Actions: Rename (`Pencil`) · Share (`Share`, only when `can_manage_sharing !== false`) · Restart (`RotateCcw`) · Stop (`Square`, only while running) · Delete (`Trash2`, destructive, `haptics.warning()`). Restart, Stop, and Delete need `can_manage_lifecycle !== false`. Chevron rule: Rename and Share keep the chevron; Restart, Stop, and Delete pass `right={null}` |
| Rename and Share | **Both push inside the options sheet — one sheet for the whole flow** (Jay, 2026-09-22). Share was a second sheet that opened after the first had closed, with an X and no way back; it now follows Rename. The sheet's body is one `BottomSheetScrollView` (dynamic sizing needs a single scrollable child, and Share's member list can be taller than the screen). **Rename snaps the sheet to full height** (`snapToPosition('100%')` on push; Jay, 2026-09-22): the field sits at the top, clear of the keyboard, and the sheet does not resize as the keyboard moves; Back snaps to the content height again (`snapToIndex(0)`). Share stays at its content height. Rename: pushed inside the options sheet (`components/kortix/sheet-push.tsx`, the app's in-sheet push): the options swap for `SessionRenameForm` with `PUSH_IN`; the title row becomes "Rename session" with `SheetBackButton` in the leading slot, and **Back returns to the options** (`POP_IN`, only after a push). No second sheet, no close-then-open. The form (`components/session/SessionRenameForm.tsx`): `SheetTextInput` — the one text field for sheets; never a hand-styled `BottomSheetTextInput` with a border (the old sheet: icon tile, description, bordered field, `ActivityIndicator`, all removed) — auto-focused, max 120, placeholder = the automatic title, then primary `Button size="lg" rounded-full` "Save" ("Saving…" while it runs). An unchanged name closes the sheet; an empty name reverts to the automatic title. Failure: `haptics.warning()` + `toast.error` "Unable to rename the session. Try again." |
| Sheet follow-ups | Only Delete follows the sheet: its `AlertDialog` opens after the options sheet has dismissed — never two overlays. Rename and Share are views of the options sheet itself. Restart and Stop close the sheet and run at once: `toast.success` "Session restarting" / "Session stopped", or `toast.error` "Unable to restart the session. Try again." / "Unable to stop the session. Try again.". A repeat of the same action on the same session is ignored while one runs. Share failures are `toast.error` |
| Share | `SessionShareForm` (`components/session/SessionShareForm.tsx`), pushed into the options sheet: title row "Share session" with `SheetBackButton` (Back returns to the options; no X). 16pt sides (`px-4`, the title row's inset), 16pt between blocks. One `SettingsGroup className="bg-secondary"` of picker rows (icon · label · check, `right={null}`): Only you (`LockIcon`) · Whole project (`GlobeIcon`) · Specific people (`UsersIcon`) — web's `SESSION_SHARING_COPY` labels, no descriptions, no header icon tile. Specific people adds a "People" group (`Text variant="muted"`, `mb-2 px-4`) of `Avatar variant="custom" size={28}` · email (" (you)" on the viewer) · check; a tap toggles with `haptics.selection()`. Members shared with at open sort first and the order then stays fixed, so a row never moves under the finger. Loading: `KortixLoader size="small"`; none: `Text variant="muted"` "No other members yet". Primary `Button size="lg" rounded-full` "Save" ("Saving…" while it runs), disabled while Specific people has no pick — no warning line. The form mounts when pushed: it seeds from the session once, pins the session id, and fetches members only while it shows. Save closes the sheet; failure: `haptics.warning()` + `toast.error` "Unable to update sharing. Try again." |
| Delete dialog | `AlertDialog`, title "Delete session", description `Delete “{title}”? Its sandbox is destroyed. This cannot be undone.` Footer: `AlertDialogCancel asChild` secondary `size="lg"` pill "Cancel" · destructive `size="lg"` pill "Delete session" ("Deleting…" while it runs). Both disabled while the delete runs, and the dialog stays open until it settles. Failure: `haptics.warning()`, the description becomes "Unable to delete. Check your connection and try again." in `text-destructive`, dialog stays open. Success: the session's tab closes, `haptics.success()`, `toast.success` "Session deleted", dialog closes. The title stays in the description while the dialog animates closed |
| Loading | `KortixLoader` centred in the page, no search field |
| Empty / no matches / error | Centred `Text variant="muted"` (`px-8`, `text-center`): "No sessions yet"; "No matching sessions" when the search hides every row; "Unable to load sessions. Pull to refresh." when the load failed and nothing is cached |
| Pull to refresh | `RefreshControl` (tint `mutedForeground`) refetches the project's sessions |
| Accessibility | Row label "{title}, {status}, {spoken time}", e.g. "Fix login, Running, 5 minutes ago" (`spokenRelative`: "5m" would read as "5 meters"). Hint "Opens the session". Actions: activate, and longpress labelled "Session actions" (opens the sheet). The mark is hidden from screen readers |

Status marks (`components/session/SessionStatusMark.tsx`), shared with the
project sidebar. Each state differs in shape, not colour alone:

| Display status | Source statuses | Mark |
| --- | --- | --- |
| `running` | running | Filled dot, `size-2.5`, `bg-kortix-green` |
| `stopped` | stopped, completed | Hollow ring, `size-2.5`, `border-[1.5px] border-muted-foreground` |
| `starting` | queued, branching, provisioning | Ring `size-2.5` `border-[1.5px] border-kortix-yellow` with a `size-1` `bg-kortix-yellow` centre dot, opacity pulse 1 → 0.35 (800ms, ease-in-out, repeats). Static under reduced motion |
| `failed` | failed | Diamond: `size-2 rotate-45 rounded-[1px] bg-destructive` |
| `needs-you` | pending review (not reachable on mobile yet) | `size-2` `bg-kortix-blue` dot inside a `size-4` `bg-kortix-blue/20` halo |

## 8. Colour and theme

- Every screen follows the resolved colour scheme (NativeWind `useColorScheme`).
  No screen is "always dark".
- Colours come from tokens (`bg-card`, `text-muted-foreground`, `THEME.*`).
  See `CLAUDE.md` → Color for the hex/rgba bans and `withAlpha`.
- Skia and other native renderers cannot parse `hsl(0 0% 100%)`; pass
  `withAlpha(token, 1)` (comma form) to them.

## 9. Checklist before handing off a screen

- [ ] Settings screen uses `SettingsPage` / `SettingsGroup` / `SettingsRow`
- [ ] No uppercase or letter-spaced group titles
- [ ] No row descriptions
- [ ] Chevrons `strokeWidth={2.75}`, full-width separators, borderless `rounded-2xl` cards
- [ ] Buttons follow section 2 (no sizing classes)
- [ ] Checked in light and dark mode
- [ ] `npx tsc --noEmit -p .` shows no new errors in the files you touched
