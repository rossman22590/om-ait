# Kortix Mobile — design approach

Read this before building or restyling any screen in `apps/mobile`. It records
the patterns Jay has signed off on, with the exact values. `CLAUDE.md` in this
folder holds the primitive rules (which component to use); this file holds the
layout and visual decisions (how a screen looks).

When a rule here conflicts with an older screen, the rule wins. Convert the
screen when you touch it.

## 1. Settings screens

Every settings-style screen uses one layout, built from
`@/components/kortix/settings-list`: the Account page, the `(settings)`
sub-pages (Sounds, Notifications, Language, Delete account), Accounts,
Billing, and Plans. Do not hand-roll headers, rows, dividers, or group titles.

There is **one settings page**: the Account page
(`components/settings/AccountPage.tsx`). The Account tab renders it, and the
project sidebar's avatar opens the same page inside the project stack at
`/projects/[id]/account`. There is no `(settings)` index screen and no General
page.

| Export | Use |
| --- | --- |
| `SettingsHeader` | Back button + title + optional trailing action. `(settings)` screens get it from `_layout.tsx`; root-stack screens (`/accounts/[id]`, `/billing`) render it themselves with `Stack.Screen options={{ headerShown: false }}`; tab roots pass `showBack={false}`; project pages (Sessions, Account) pass `onOpenMenu`, which replaces Go back with the hamburger (`MenuButton`); `largeTitle` moves the title below the control row as an `h3` (Sessions) |
| `SettingsPage` | Scroll body. Tab roots pass `paddingBottom={useTabBarClearance()}` and `contentInsetAdjustmentBehavior={TAB_SCROLL_INSET_ADJUSTMENT}` |
| `SettingsGroup` | Titled, borderless rounded card with full-width separators |
| `SettingsRow` | icon (or `leading`) · label · trailing |
| `AppearanceRow` | The Appearance row: current mode as its value; opens a dialog with System, Light, Dark (icon · label · check) |

```tsx
import { SettingsGroup, SettingsPage, SettingsRow } from '@/components/kortix/settings-list';

<SettingsPage>
  <SettingsGroup title="Preferences">
    <AppearanceRow />
    <SettingsRow icon={Volume2} label="Sounds" onPress={() => go('/(settings)/sounds')} />
  </SettingsGroup>
  <SettingsGroup title="Help">
    <SettingsRow icon={BookOpen} label="Docs" external onPress={openDocs} />
  </SettingsGroup>
</SettingsPage>
```

### Page

| Property | Value |
| --- | --- |
| Body | `SettingsPage` — `ScrollView`, `bg-background`, no scroll indicator |
| Side margin | `px-4` (16pt) — `SettingsPage`'s default `gutter="project"`. **One edge for every settings-style screen** (Jay, 2026-09-22): the project's Account page, the Account tab, Sounds, Notifications, Language, Billing, Plans and `/accounts/[id]`. A 20pt `gutter="page"` is available but nothing uses it |
| Space between groups | 18pt |
| Bottom padding | safe-area inset + 28pt |
| Content above groups | `header` prop (e.g. the profile photo on the Account page) |
| Pinned bottom action | render the pill below `SettingsPage` in a `bg-background px-5 pt-3` view with `max(insets.bottom, 16)` bottom padding, and pass `paddingBottom={24}` to the page (Plans) |
| Full-bleed hero | `hero` prop (no screen uses it since Billing dropped its hero, 2026-09-24). The hero scrolls with the page; the body sits on a `rounded-t-3xl bg-background` sheet that overlaps the hero’s bottom 24pt, so the hero keeps ≥ 24pt bottom padding |

### Header

| Property | Value |
| --- | --- |
| Component | `SettingsHeader` from `settings-list` — one header for every settings-style screen |
| Go back | `PlatformButton` — native SwiftUI button on iOS (`chevron.left`, plain style on the `secondary` fill), `Button variant="secondary"` icon pill on Android |
| Back behaviour | `router.back()`; if there is no history, `router.replace('/projects')` |
| Title | screen name, sentence case |
| Trailing action | `right` prop — e.g. `PlatformButton label="New" systemImage="plus"` for a create action |
| Hero header | `align="center"` + `transparent` over a `SettingsPage` hero. Pair a centred title with icon-only actions; an empty side gets a 40pt spacer |

Register a new `(settings)` screen in `_layout.tsx` with
`header: () => <SettingsHeader title="…" />`.

### Group

| Property | Value |
| --- | --- |
| Title | `Text variant="muted"`, `mb-2 px-4`, **sentence case** ("Sound pack", not "SOUND PACK"). Never uppercase, never letter-spaced eyebrows |
| Surface | **Each row is its own tile** (Jay, 2026-09-22): `bg-card`, **no border**, `overflow-hidden` (it clips the row's pressed fill). The group's outer corners are `rounded-2xl` (16pt: first row top, last row bottom); the corners between two rows are `rounded-md`. A one-row group is one fully `rounded-2xl` tile. `SettingsGroup`'s `className` overrides the tile surface (`bg-secondary` inside a sheet) |
| Dividers | No hairline. A 2pt gap between tiles (`ROW_GAP` in `settings-list.tsx`), set by `SettingsGroup`: what is behind the group shows through it — `bg-background` on a page, the sheet colour in a sheet. Rows never draw their own divider |
| Empty group | renders nothing — conditional rows (`{cond && <SettingsRow/>}`) are safe |

### Row

A row is **icon · label · trailing**. Nothing else.

| Property | Value |
| --- | --- |
| Padding | `px-4 py-3` (py one step below px — see §2 Spacing) |
| Icon | Phosphor from `@/lib/icons` at the app weight (no `weight` prop), `size={18}`, `text-foreground/80`, centred in a leading slot at least 20pt wide (`min-w-5`) so every label aligns. A row with no `icon` and no `leading` drops the slot and its `mr-3` gap, so its label starts at the card padding. A loading row keeps the slot with a `<Skeleton>` so its label does not shift |
| Custom leading | `leading` prop instead of `icon` — flag emoji (`Text variant="large"`) on Language, `Avatar variant="custom" size={28}` on the new project sheet's Account picker |
| Label | `Text` default variant, one line, sentence case ("Edit profile", "Delete account") |
| Long label | `multiline` prop wraps the label instead of truncating it. Only for list content that is the label itself: plan features on Plans and the upgrade sheet (`Check` icon · feature). Never a way to add a description |
| Description | **none**. Do not add a second line under the label. One exception: the Account page's **profile tile** (name over email, below), the only two-line row in the app (Jay, 2026-09-23) |
| Chevron (navigates in-app) | `CaretRightIcon`, `size={16}`, `text-muted-foreground/70` — shown automatically when the row has `onPress` |
| External link (opens browser) | `external` prop → `ArrowUpRightIcon`, same size |
| Read-only value | `value` prop → muted text on the right, max 60% width, one line (e.g. the account email) |
| Inline control | `right` prop → `Switch`, `ToggleGroup`, check mark, icon button. Replaces the chevron |
| Selected option (picker) | `checked` prop → primary check mark. Picker rows also pass `right={null}` so no chevron shows (Sound pack, Language, the new project sheet's Account picker) |
| Appearance | always `<AppearanceRow />` — a `Dialog` (not a page, not an inline toggle) listing System, Light, Dark in that order, each with its icon (`Monitor`, `Sun`, `Moon`) and a check on the active mode, in a `SettingsGroup` on the `bg-popover` dialog; choosing applies and closes. Dialog width is explicit — `min(window width − 32, 420)` — because `w-full` collapses to content inside the native overlay wrappers |
| Destructive | `destructive` prop → icon and label `text-destructive`. Log out is now a neutral row (not `destructive`); Delete account is a quiet text link below the version footer, not a `SettingsRow` |
| Badge | `badge` prop → small destructive pill after the label (e.g. "Scheduled") |
| Plan badge | `PricingTierBadge` in `right` (Account page email row). Legacy tiers (Basic, Plus, Pro, Ultra) render their SVG artwork. Any other plan name renders a pill in the artwork's shape: full radius, `THEME.light.border` fill and `THEME.light.foreground` text in both themes, label at 55% of the pill height. Never leave the slot empty |
| Press feedback | background highlight `active:bg-accent`. Never scale a row inside a card |

### Account tab and account screens

| Screen | Structure |
| --- | --- |
| Account page (`AccountPage`) | The app's one settings page (COR-120 — "less is more"). `presentation="tab"` (Account tab): page title "Settings" (`Text variant="h3"` in a 40pt row, part of the scroll content — no header bar, no logo, page background). `presentation="project"` (`/projects/[id]/account`, from the project sidebar avatar): `SettingsHeader title="Settings"` with the hamburger (`onOpenMenu` → the project drawer), no Go back, no h3. Both then: **profile tile** (`ProfileTile` in `AccountPage.tsx`; Paper board 04, "Profile"; Jay, 2026-09-23), one untitled group of one tile — the row surface (`bg-card`, the group's `rounded-2xl`, `px-4 py-3`), 48pt `ProfilePicture` leading (`mr-3`), then a column: the display name (`Text` 16pt `font-roobert-semibold`, `text-foreground`, one line) over the email (`Text variant="muted"`, 14pt, one line, truncates), then the row chevron (`CaretRightIcon` 16pt `text-muted-foreground/70`, `ml-3`); pressed `active:bg-accent`; tap → `EditProfileSheet`. The one allowed two-line row (§1 Row → Description); never the email in the `value` slot → **current context**, one untitled group: a **project row** (`presentation="project"` only, hidden until the project name loads) — a 28pt tile leading (the project's first letter on `bg-foreground rounded-lg`, `text-background` label — the inverted-fill pattern, not `Avatar`), the project name as the label, `value` "Project", chevron → the project Settings page (`page:settings`), through a callback the route passes in (`onOpenProjectSettings`, `app/projects/[id]/account.tsx`) that pushes it as a **sub-page** over Settings (`openSubPage('page:settings')`, the `page` route; see **Project stack**), so back returns to Settings — the only entry point to that page since the drawer's gear button was removed (COR-124/COR-157 Task 4, **Project sidebar** below) — then an **account row**: 28pt `Avatar variant="custom"` leading, the active account's name as the label, `right` = `PricingTierBadge size="md"` (`plan.label` from `useActivePlanName`: Free, Team or Enterprise) + a hand-composed chevron (`right` replaces the row's own chevron, so the row draws one itself, `CaretRightIcon` `text-muted-foreground/70`), → `/accounts/<activeAccountId>` (Task 1's page) → Preferences (Appearance, Sounds, Notifications, Language) → Help (Docs, Support) → **Log out**, its own untitled one-row group, neutral (not `destructive`) → **version footer**, the page's last line, centred, `py-8` (32pt above and below; Jay, 2026-09-23): 14pt `KortixLogo variant="logomark"` at 50% opacity · `Text variant="muted"` "v{x.y.z}" from `Constants.expoConfig.version` (`app.json` `expo.version`; right in Expo Go too, where `nativeApplicationVersion` is Expo Go's own) → a quiet centred text link below the footer, `Text variant="muted"` + `underline`, `minHeight: 44` hit area: "Delete account" (reads "Deletion scheduled" once a deletion is pending) → `/(settings)/account-deletion`. The Workspace group (Accounts, Billing rows) and the Advanced group are gone (COR-122, COR-126, Jay, 2026-09-23) — Billing is reached from the account row's chevron via `/accounts/<id>`, not from this page directly |
| Edit profile | `EditProfileSheet`: `Sheet` + `SheetHeader` "Edit profile" + a tappable profile photo at the top (moved from the Account page's old photo header, Jay, 2026-09-23: 80pt `ProfilePicture`, tap → system photo picker; 28pt camera badge `bg-secondary` with a 2pt ring at the bottom-right — the ring colour is `useSheetBackground()`, the sheet's own `--popover` surface, not `--background`: the two tokens diverge in dark mode; `KortixLoader size="small"` over the photo while uploading) + `SheetTextInput` (display name, max 100) + `Button size="lg" rounded-full` Save, disabled until the name changes. Save closes the sheet; a failure is a toast |
| Delete account (`/(settings)/account-deletion`) | Groups: What gets deleted (icon · label, read-only) → When (picker: In 30 days ✓, Immediately) → destructive `Button size="lg" rounded-full`. The pill opens ONE `AlertDialog` with two steps, for both timings (`lib/account-deletion/confirm-flow.ts`): (1) title + consequence line, auto-focused `<Input>` "Type DELETE to confirm", destructive Continue disabled until the word matches (trimmed, any case); (2) "Are you sure?" + "Everything in your account is deleted…", destructive Delete that stays disabled for 1s after the step appears, so a double tap on Continue cannot delete. The steps swap inside the same dialog — never two overlays. Scheduled deletion: one row (Calendar · Scheduled for · date) + secondary pill Cancel deletion (no confirmation — it is safe) |
| Plans (`/plans`) | `SettingsHeader "Plans"` → Plan picker group (plan icon · name · price or "Current" · check) → seat total row for Team with more than one member → "{plan} includes" group (`Check` · feature, `multiline`) → pinned primary pill. The pill opens kortix.com (arrow icon): Upgrade to Team / Switch to Free → web billing, Contact sales → `/contact`; disabled "Current plan" or "Ask an account owner to upgrade". Logic: `lib/billing/plan-action.ts`. iOS (App Store guideline 3.1.1): `/plans` never opens — every entry point (Billing's Change plan row, the upgrade sheet's pill, every "Upgrade to unlock" `FreeTierBlock`, the agent drawer's Workers "+") is hidden or a no-op, gated by `canShowExternalPurchase` (`lib/billing/store-policy.ts`) |
| Upgrade sheet (`GlobalUpgradeSheet`) | Centred "Kortix Team" (`large`) · price (`h1`, tabular) · "per seat / month" (`muted`) · gate message (`muted`) → Includes group (`multiline`, first line "$X of usage credit per teammate, every month") → seat total row → primary pill "Upgrade to Team" (→ Plans) or an Ask an account owner row → secondary pill Not now. iOS: no price anywhere in the sheet — no header price/"per seat / month", no seat-total group, no `$` in the Includes group's first line either (it reads "Usage credit for every teammate, every month" instead, `getUpgradeSheetIncludedItems` in `lib/billing/upgrade-sheet-included.ts`) — and no pill to Plans; just the plan name, the gate message, Includes, the Ask-an-owner row when the viewer can't manage billing, and Not now |
| Projects header | Kortix logo · search · New. No settings/avatar button — the Account tab owns settings. Search shows only when the account has at least one project (hidden while loading and when empty) |
| Projects empty state | Plain page, centred between header and tab bar: "No projects yet" (`Text variant="large"`) · 24pt gap · primary `Button size="lg" rounded-full` "Create project" (owners and admins only). No card, border, fill, icon, or description |
| New project sheet | `NewProjectSheet` (Jay, 2026-09-22): `KortixBottomSheetModal` at 70%, `SheetTitleRow` "New project" with the X, then `px-4` content with 16pt gaps — the name `SheetTextInput` (auto-focused, Return creates), one `SettingsGroup` of an "Account" row (`UserIcon`, `value` = the account's name, chevron; **only when the user can create in two or more accounts** — owner or admin, `creatableAccounts` in `lib/projects/landing.ts`, web `/new`'s `AccountPicker` rule; it pushes an "Account" view of picker rows — 28pt `Avatar` · name · check — and a tap picks and returns) and an "Import from GitHub" row (`GithubLogoIcon`, chevron), and an `lg` "Create project" pill (disabled until a name is typed; "Creating…" while pending). The sheet opens on the Projects page's active account (else the first creatable one); after a create the Projects page switches to the project's account before it opens the project. No description, no info card, no Cancel. The row pushes the GitHub view in (`sheet-push`; Back in the close button's slot): a `bg-secondary` group of GitHub account picker rows (`GithubLogoIcon` · owner · check) ending in "Add GitHub account" (external), a "Search repositories" field, a `bg-secondary` group of repository picker rows (`owner/repo`, `description` = default branch · Private), "Project name (optional)", and an `lg` "Import repository" pill. Loading: `KortixLoader size="small"`. No installation: the empty-state shape — `Text variant="large"` "Connect GitHub" · 24pt · an `lg` "Connect GitHub" pill that opens the install page |
| Account detail (`/accounts/[id]`) | Mobile does not maintain settings parity with web (COR-120 — "less is more"): this is a small plan + web handoff, not the web admin tabs. `SettingsHeader title={account name}`. Untitled group: ONE Billing row (Jay, 2026-09-24 — separate Plan and Credits rows both opened `/billing`, which read as a bug), `value` = "{plan} · {balance} credits" from `useAccountState` (omitted until it loads); it opens `/billing?accountId=<this account>`. Group "On kortix.com": Members, Groups and permissions, Git, Audit log — each `external`, opening `accountHubUrl(getFrontendUrl(), accountId, tab)` (`lib/accounts/web-account-links.ts`) via `WebBrowser.openBrowserAsync` (the in-app browser). Git needs `account.write`, Audit log needs `audit.read` (`useEffectiveAccountCaps`, the same checks the old tabs used) — hidden otherwise. No avatar/name/"1 member · 1 project" block, no in-app Members/Groups/Git/Audit screens |
| Billing (`/billing`) | A plain settings page (Jay, 2026-09-24 — the gradient `BillingHero` read as sloppy and is deleted): `SettingsHeader "Billing"`, then in `SettingsPage header` a centred block — "Available credits" (`muted`), the balance (`h1`, tabular), "{plan} plan" (`muted`), and one full-width primary pill (`Button size="lg" rounded-full`): Buy credits (arrow, → web billing) when the plan can buy credits, else Change plan (chevron, → Plans). Groups, label · value rows, **no icons**: Credits (Daily with "· refreshes in Nh", Monthly, Extra) → Scheduled change (New plan, Starts on, Keep current plan → `AlertDialog`) → Plan (Current plan, Next billing, Annual commitment, Cancels on, Change plan when the pill is Buy credits, Manage on kortix.com — external) → untitled Credits explained (external). Loading: `KortixLoader` centred under the header. Reads the `accountId` route param (the account screen passes it), else the active account (`useActiveAccount`); refetches on pull to refresh and when the app returns to the foreground. iOS (App Store guideline 3.1.1 — no link to purchase outside IAP): no pill, no Change plan row, no Manage on kortix.com row. `canShowExternalPurchase` (`lib/billing/store-policy.ts`) gates all three |

No subtitles, helper paragraphs, or meta lines anywhere on these screens.

| Pattern | Value |
| --- | --- |
| Row ⋯ menu (Projects) | `ProjectActions`: bottom `Sheet` with the item's avatar + name and one untitled `SettingsGroup` of actions (Open project; Archive project, destructive, managers only). A destructive action confirms in an `AlertDialog` that opens after the sheet has closed — never two overlays at once |
| Android / web tab bar | `FloatingTabBar`, mirrors the iOS bar: 60pt capsule (`FLOATING_BAR_HEIGHT`), icon (20pt) over a 12px label, `bg-secondary` pill thumb behind the active tab, foreground icon + label on every tab. Light: `bg-background` + soft shadow; dark: `bg-card` + border. Behind it, a scroll-edge fade (theme background, transparent 36pt above the capsule → opaque at the screen edge), like the iOS 26 bar. Hides on React Native `Keyboard` events, not keyboard-controller values. Android nav bar: `expo-navigation-bar` `enforceContrast: false` (no 3-button scrim in builds) + `NavigationBar.setStyle` matching the status bar |
| Project stack | `/projects/[id]` is a nested stack: `app/projects/[id]/_layout.tsx` renders `ProjectScreen`, `index` is project home, `view` is the open page, thread, or connecting session, `sessions` is the Sessions page, `files` is the Files page, and `account` is the Account page (`components/session/ProjectRoutes`, route names in `lib/session/project-stack.ts`), and `page` is a **sub-page** (`/projects/[id]/page?pageId=…`, `ProjectSubPageRoute`). The stack is `[index]`, `[index, X]`, or `[index, X, page, …]`. **Back goes to the previous page only** (Jay, 2026-09-23): a page opened from another page is a real push — project Settings from Settings (the Account page's project row), Schedules and Secrets from project Settings (`SUB_PAGE_IDS`, `openSubPage` on `ProjectRouteValue`, `subPageOpenMove` ignores a double tap). Its `pageId` param never changes, so the screen under it keeps its content and state (scroll, open rows). The drawer never deepens the stack (below). **Every project page shows the hamburger and no Go back** (Jay, 2026-09-16), **except a pushed sub-page**, which shows Go back (`PageHeader onBack`, the `PlatformButton` chevron `SettingsHeader` uses) in the hamburger's place and has no drawer (no hamburger, no edge swipe): project home, a thread, and a connecting session show `FloatingMenuButton`; every project page uses one 16pt side edge (`px-4`; Jay, 2026-09-16): `PageHeader` `px-4`, `FloatingMenuButton` `left-4`, the home composer `px-4`, `SearchListHeader` `px-4` by default, and `SettingsHeader`/`SettingsPage` `gutter="project"` on Sessions and Account (the Account tab and settings screens keep 20pt `gutter="page"`); tool-page bodies still set their own inline padding (mostly 16pt) — not yet converted; every hamburger is `MenuButton` (`components/kortix/menu-button.tsx`), the same on every platform — no native Liquid Glass circle on iOS (Jay, 2026-09-17): `Button variant="ghost" size="icon" className="-ml-2.5 rounded-full bg-background"` (page colour, so a floating hamburger hides the content scrolling under it) (the −10pt margin puts the icon glyph on the page's padding edge, parallel to the title and list), static `MenuIcon` 20pt `text-foreground` (`components/icons/menu-icon.tsx`: three left-aligned bars, 24/11/13 wide; Jay, 2026-09-17), 10pt hit slop, label "Open menu" — transparent at rest, `active:bg-accent` while pressed, no open/close animation (Jay, 2026-09-16); tool pages and Files show `PageHeader`'s hamburger; Sessions and Account show `SettingsHeader onOpenMenu`. The drawer opens from every project route (hamburger or left-edge swipe). Drawer moves (`drawerRouteMove`, `returnHomeMove`): Search (→ `sessions`, `autoFocusSearch`) / Files / avatar push over home, replace a covering route, and do nothing when that route is already on top; under sub-pages they pop back to that route when it is the one under them (`pop-to`), else reset to `[index, route]` (`reset`, `homeAndRoute` keeps home mounted) — so a drawer move never leaves the stack deeper than one screen over home; Review goes through the tab store's `navigateToPage('page:review')` instead of a drawer route, so the same push/replace happens through `ProjectHomeRoute`/`useCoveringRoute`, not `drawerRouteMove`; a session row only updates the tab store (home pushes `view`, an open `view` swaps its content, a covering route replaces itself with `view` — `useCoveringRoute`); New session resets the store and pops to home (`StackActions.popTo('index')`); the switcher row's picked project (COR-124/COR-157 Task 4) `router.replace`s to it directly, closing the drawer with the switcher sheet — the old "All projects" row and the tappable-nothing logomark are gone (**Project sidebar**, §6). Never replace `view` with `view`: the old view's cleanup would close the session that just opened. Back: Android back closes an open drawer, else pops a sub-page one level (`androidBackMove` → `pop`, `backFromSubPage`; with nothing under it — a deep link — it replaces it with home), else pops a covering route to project home, else on home is left to the system. iOS has no swipe-back in the project stack (`gestureEnabled: false`, `fullScreenGestureEnabled: false`): the left edge opens the drawer on every project route — except a sub-page (`projectEdgeGesture` → `back`): its screen sets `gestureEnabled: true` and the drawer's `swipeEnabled` is off while it is on top, so there the left edge is the iOS swipe-back. One edge gesture, one meaning per screen. A sub-page leaves when the store leaves the home state while it is focused (`subPageLeaveMove`): a view under it is popped to and swaps its content; otherwise the stack resets to `[index, view]`. Sub-pages are pushed only from screens that show while the store is home (Settings, project Settings), and removing one never resets the store. `projects/[id]` itself has `gestureEnabled: false` and always opens with `router.replace`, so no screen sits under a project. The Projects list opens only from the project sidebar. A project always opens on project home: the tab store neither restores nor persists the active page or thread |
| App start | `app/index.tsx` is the door (web `/projects/start`): signed out → `/auth`; signed in → the last project this user opened (`useLastProjectStore`, keyed by user id), else the first project (`lib/projects/landing.ts`: selected account, then owned/admin, then member accounts), via `router.replace`. The Projects list only when no account has a project. Failure: 2 retries, then a centred message (`large` title, `muted` line) chosen by `lib/projects/start-failure.ts`: unreachable → "Can't reach Kortix" / server error → "Could not open your project", each with `size="lg"` pills Try again (primary) · All projects (secondary, opens `/projects`) · Sign out (ghost); a 401/403 → "Your session has ended" with one primary pill Sign in again. Never a dead end, never an automatic fall back to the list. Every implicit redirect (sign-in, back with no history, leaving an account) goes to `/`, never `/projects` |
| Push transition | Every stack (root, `(settings)`, `auth`, project) is expo-router's native `Stack` with the platform default push/pop on iOS and Android — no custom animation. The root `index` redirect has `animation: 'none'`. Swipe-back is iOS-only, and off in the project stack (its left edge opens the drawer) except on a sub-page (`page`), where it is on and the drawer swipe is off; Android uses the system back gesture. |
| Tab root headers | The Account tab has no screen header — the h3 "Settings" title is the top of the page (status-bar inset: automatic on iOS, `insets.top + 16` on Android) |
| Confirmations | `AlertDialog` (`rounded-3xl` content — `Dialog` and `AlertDialog` are borderless in the primitives, on `bg-popover` — the same surface as bottom sheets — over a `bg-black/70` overlay; call sites never set a background; footer: destructive `Button size="lg" rounded-full` + `AlertDialogCancel asChild` secondary pill). Never `Alert.alert`. Sign out, Delete account, and Keep current plan keep the dialog open while they run and show a failure in the description. Delete account always asks for a typed confirm word and then a second question with a delayed Delete button (both timings); taps alone never delete. Keep current plan confirms with the buttons alone, and a safe action (Cancel deletion) does not confirm |
| Results | Success of an action that leaves the page in place → `useToast().success`; a failure outside a dialog → `useToast().error`. Never `Alert.alert`. Values: §11 Toasts |

### Copy

- Labels name the destination or setting in 1–2 words, sentence case.
- Group titles group by user intent: Preferences, Profile, Account, Help,
  Sound pack, Sound events, Feedback. The Advanced group is gone (COR-122,
  COR-126) — nothing groups under that title any more.
- A destructive action lives in its own group, at the bottom of the screen
  it belongs to (e.g. Delete project on the project Settings page).

## 2. Spacing

Padding is written as plain Tailwind classes in each component — no shared spacing module. Mobile uses stock Tailwind spacing (1 unit = 4pt: `px-4` = 16pt). The values below are the only ones to use.

Jay's rule (2026-09-16): **vertical padding is one Tailwind step below horizontal padding** on every padded row, list item, and inset block. Pick the side padding for the surface, then take one step off for the top and bottom.

| Pair | Pt | Use |
| --- | --- | --- |
| `px-3 py-2` | 12 / 8 | Edge rows: project sidebar session rows and Previous chats, inside an `mx-4` column so the highlight box starts on the 16pt edge and the content on the 28pt edge |
| `px-4 py-3` | 16 / 12 | Rows inside a card or group (`SettingsRow`, Sessions page rows) — the default |
| `px-5 py-4` | 20 / 16 | Roomy blocks and page sections |

- Never set py below px − 1 step (`px-4 py-1.5` reads cramped) or equal to px (`px-4 py-4` reads loose).
- Side edges: 16pt (`px-4`) everywhere — project pages (Jay, 2026-09-16 — 12pt was too tight) AND every settings-style screen, which used to sit at 20pt and read as a different app beside the project's Account page (Jay, 2026-09-22).
- A control's icon sits on the padding edge, not the control's box: `MenuButton` pulls itself left by `-ml-2.5` so the hamburger glyph lines up with the title and list below it.
- Fixed-height controls (`Button` sizes, 40/44pt inputs) keep their size variants; this rule is for padded content, not for `Button` boxes.

## 3. Buttons

| Situation | Use |
| --- | --- |
| Primary header action (e.g. projects "New") and screen back buttons | `PlatformButton` — native iOS SwiftUI button (plain style, `secondary` fill, no glass), design-system pill on Android |
| The project hamburger | `MenuButton` — the design-system ghost icon button on every platform. No native Liquid Glass circle on iOS (Jay, 2026-09-17) |
| Everything else | `Button` from `@/components/ui/button` with `variant` + `size` |
| Pill shape | `className="rounded-full"` — the only visual class allowed on a `Button` |
| Action under a chat message (Copy, Edit message, turn details ⋯) | `Button variant="ghost" size="icon-sm"` — a 28pt box (web: 26px), with `hitSlop={TURN_ACTION_HIT_SLOP}` (8pt above and below → 44pt tall target). Glyph sizes are unchanged (15pt user row, 17pt assistant row). Never `size="icon"` there: a 40pt box per action reads oversized (Jay, 2026-09-21). `icon-sm` is for these actions only |
| Never | `h-*`, `w-*`, padding, or text size/weight classes on a `Button` or its `Text` |

`PlatformButton` needs the `ExpoUI` native module (`@expo/ui`). A binary built
without it falls back to the design-system button, so OTA updates stay safe.
Adding or upgrading native modules requires a new binary (`npx expo run:ios`
locally, EAS for stores).

### Inputs

| Rule | Value |
| --- | --- |
| Border | **none**, on every input. Fields are filled surfaces |
| Surface | `bg-secondary` (the `secondary` token) |
| Text | 16pt Roobert Regular — `INPUT_FONT_SIZE` / `INPUT_FONT_FAMILY` from `pill-input.tsx`, `font-roobert text-base` on `<Input>` |
| Placeholder | same size and weight as the text, `muted-foreground` colour (never a lighter opacity) |
| Which field | `<Input>` (rounded-xl, 44pt) for forms; `PillInput` (pill, 44pt) on full screens like auth; `SheetTextInput` inside bottom sheets; `SearchBar` / `SearchListHeader` for in-page search; `SearchHeader` for header search mode |
| Never | local `border-*`, `text-[NNpx]`, `font-*`, or height classes on an input |

## 4. Auth screens

| Element | Value |
| --- | --- |
| Welcome | full-bleed `KortixCurrents` hero, logomark, provider pills as `PlatformFullWidthButton` (`size="xl"`, 48pt — the only `xl` Buttons in the app; Jay, 2026-09-17): native SwiftUI capsules on iOS (Jay, 2026-09-17), design-system `Button` `rounded-full` elsewhere, same variants on both (Google and Apple `default`, email `outline`), provider icon pinned to the pill's left edge. No legal links (Jay, 2026-09-17) |
| Email screen | back `PlatformButton`-style header, `PillInput` fields (44pt, matches `Button size="lg"`), primary pill, bottom pinned secondary pills. No legal links (Jay, 2026-09-17) |
| Fields | `PillInput` on full screens, `SheetTextInput` inside bottom sheets — never `<Input>` restyled by class |

## 5. Project home

`/projects/[id]` with no chat open (`components/session/ProjectHome.tsx`,
COR-34). It matches the web project home: simple and clean, nothing extra.

| Element | Value |
| --- | --- |
| Hero | `ProjectHero` (`components/session/ProjectHero.tsx`): the Kortix symbol drawn as liquid metal (`MetalKortixLogo`, below) **alone — no sentence, no project name** (Jay, 2026-09-21; the "Give {project name} something real to work on." line and `lib/session/project-greeting.ts` are deleted — do not add them back). Centred on the screen; centred in the area above the keyboard while typing. `accessibilityRole="image"`, label "Kortix" |
| Hero metal | `components/kortix/MetalKortixLogo.tsx` draws Paper's `Heatmap` shader (`lib/effects/heatmap-sksl.ts`, an SkSL port; Paper's own build is WebGL and cannot run in React Native) with Skia, the same code on iOS and Android (Jay, 2026-09-21). **Dark is Paper's export exactly** (`Heatmap`: contour 1, inner glow 0.89, angle 51, scale 0.9, white and near-black, transparent back, Paper's exported frame at rest); do not alter the shader's output (Jay, 2026-09-22: removing the outline and body was rejected). **Light is its own palette on the same shader, not a copy of dark**: near-black streaks over a light-gray body (Paper's white vanishes on a white page). **It moves only while the phone moves and costs nothing at rest**: no frame loop and no idle drift. Reanimated's rotation sensor (built in, not `expo-sensors`) feeds a reaction that retargets a critically damped spring (`SPRING`: stiffness 35, damping 12, about 1 s to settle) only when the target changes; a spring keeps its velocity between the 25 readings a second, so the motion is continuous, and when it settles nothing draws. A full move is 2.5 shader-seconds (`HEATMAP_SWEEP_SECONDS`) over about 29 degrees (`TILT_FULL_RAD`). Movement is measured against a baseline that follows the phone with a 1.5 s time constant (`lib/effects/heat-tilt.ts`, tested), so holding the phone upright is rest and the highlight eases back to rest when it is held still. Reduce Motion, an unfocused screen and a backgrounded app draw the rest frame and slow the sensor to 1 Hz. Falls back to the flat `KortixLogo` while the texture loads or if the shader does not compile. The texture `assets/brand/kortix-heatmap.png` is generated: `pnpm bake:heatmap`. It is three gray planes (contour, wide blur, inner blur) and must stay gray: Skia color-manages an RGB texture and mixes its channels, which drew a halo around the symbol on a device. `scripts/check-sksl.ts` compiles the shader; `scripts/render-sksl.ts` renders it to PNG without a device (`SWEEP=1` for the frames a move can reach; `TONE=light LIGHT=<name>` for light palettes) |
| Hero size | Resting size = `heroLogoSize(window width)` (`lib/session/project-hero.ts`, tested): 30% of the width, min 88pt, max 150pt — 121pt on a 402pt iPhone, 113pt at 375pt, 150pt on a tablet (Jay, 2026-09-22: bigger again; it was 64pt, then 96pt). While the keyboard is up the symbol scales to `HERO_KEYBOARD_SCALE` (0.625 → 76pt on a 402pt phone). The scale reads `useReanimatedKeyboardAnimation().progress` on the UI thread, so it moves 1:1 with the keyboard and reverses with it — no timing, no spring of its own. Only `transform` animates; the symbol renders at the resting size and scales down, so it is sharp at both ends |
| Hero colours (hidden) | **A press on the symbol held for 5 seconds** (`LOGO_SHEET_HOLD_MS`, `Pressable delayLongPress`; Jay, 2026-09-22, shortened from 10s the same day) opens `LogoPaletteSheet` — nothing opens sooner, and there is no hint and no progress: it is not a feature to find by accident. A shorter press only closes the keyboard, as a tap around the symbol does. On open: `haptics.success()`. Project home's hero wrapper is `pointerEvents="box-none"` so only the symbol takes touches. **The sheet** (`components/session/LogoPaletteSheet.tsx`): `KortixBottomSheetModal title="Kortix"`, `PickerSheet`'s layout (16pt sides, 16pt between groups). First a **Style** `SettingsGroup` (`lib/effects/logo-style.ts`: Dither, the default · Heatmap — one Paper shader each, `MetalKortixLogo`, both driven by the same tilt, `hooks/useTiltMotion.ts`; switching crossfades over `LOGO_STYLE_CROSSFADE_MS`, so the shaders never cut. A third style, liquid metal, shipped and was removed the same day, Jay: keep only heatmap and dither), then one **Colour** `SettingsGroup` (Graphite, the default · Gold · Copper · Ruby · Amethyst · Cobalt · Emerald — metal finish only, Jay, 2026-09-22: pastel removed) of `SettingsRow`s: `leading` = a 20pt round swatch (the palette's body colour, its highlight as the centre dot) · name · check, `right={null}`. **Predefined colours only, never a free colour picker** (Jay): every palette is one brand accent (`THEME.accent.*`), derived in `lib/effects/logo-palette.ts` (tested) — no new colour literal; on a white page the outline stays mid-dark because the shader maps low heat to transparent. A tap applies at once with `haptics.selection()` and **the sheet stays open** (compare, like the model sheet); it takes at most 50% of the screen with a 0.2 backdrop (a deliberate override) so the symbol behind it is the live preview. The style and the palette are both kept on the device (`stores/logo-palette-store.ts`, zustand `persist`; an unknown saved id falls back to Dither / Graphite). `MetalKortixLogo` takes the style as `style` and the palette as `palette` (heatmap) / `front` (dither); the flat fallback symbol keeps the theme colour |
| Chat input | `Composer` (`components/kortix/composer.tsx`) — **one card for project home and the thread** (Jay, 2026-09-21; the thread's `SessionChatInput` wraps it): `rounded-3xl`, **the page colour `bg-background` in both themes** (Jay, 2026-09-21: never `bg-card` or any second shade — measured on the simulator in dark mode: page and card both rgb 11,11,11). Edge: a hairline `border border-border` in both themes. **No shadow** (Jay, 2026-09-21): `LIGHT_SHADOW` put a grey halo around the card in light mode, so the input read darker than the page. Text field on top (16pt Roobert, plain "Ask anything" placeholder, no animation). Row below, **36pt controls** (Jay, 2026-09-21: 40pt read oversized): add (`Button variant="secondary" size="icon-md"`, 18pt `+`) · model pill (`Button variant="secondary" size="sm"`, model name + 14pt `CaretDownIcon` `text-muted-foreground`) · spacer · send (`Button size="icon-md"`, 18pt arrow, secondary when empty, primary when there is something to send). All `rounded-full`; every control passes `hitSlop={COMPOSER_CONTROL_HIT_SLOP}` (4pt), so its touch target stays 44pt. Nothing else in the row: no agent, no settings, no Queue pill |
| Thread additions | Same card, `px-4`, `pb-3`. `header` slot above the text: the queue panel and the staged slash command (a `bg-secondary` pill: `TerminalIcon` · `/name` · remove). Pill label `composerPillLabel`: "{model}" or "{model} · {level}". **`#` opens a skill picker** (COR-160, `useSkillMentions.ts` + `lib/session/skill-mentions.ts`, tested): the Skills page was removed from mobile, so this is the only way to reach a skill from a thread now. Same word-start trigger detection as `@` (`useMentions.ts`), same suggestion list component (`MentionSuggestions`, `kind: 'skill'` — `Sparkle` icon, name + muted one-line description), same inline `#name ` token tracked and pruned on backspace like a mention. Filters the project's `Command[]` list `/` already fetches (`commands` prop) to `source === 'skill'` — the identical "Skills" bucket `apps/web`'s `/` menu shows — so no second network fetch. At send, the first tracked `#name` still in the text is re-resolved against the live command list: found → dispatched as a structured command through the SAME `onCommand(command, args)` path `/` uses (`SessionPage.tsx`'s `handleCommand` → `POST .../session/:id/command`), not sent as chat text; a skill deleted since it was picked falls back to plain `/name args` text, mirroring `apps/web`'s `planDraftSubmission` exactly. A draft that also carries files or `@` mentions never dispatches as a command (the command endpoint takes only name and args, so they would be lost): it sends the same `/name args` text through the normal send path, which uploads the files and keeps the mentions (`resolveSkillSubmission`'s `hasAttachments`). Send while the `#` trigger is active only dismisses the menu when the menu has rows (`suggestionMenuTakesSubmit`); a draft ending in `#word` that matches no skill draws no menu, so Send sends it. Project home has no `commands` prop and no `#` trigger — a skill is only reachable from a thread. While the agent works: Stop (`secondary` `icon-md`, 12pt `StopIcon`) alone; with a draft, Stop + Send, and Send queues the message. **Stop failure keeps the running state** (`handleStop`, `SessionPage.tsx`, COR-146): the status flips to idle optimistically, but a non-2xx `/abort` response or a network error rolls it back to what it was and shows `toast.error` "Couldn't stop. Kortix is still working." — a failed stop never silently looks like it worked. `+` opens the Add sheet (`AttachSheet`, see Add below); under the tiles the thread adds one `SettingsGroup`: **Recent files** (`StackIcon`, the three-layer glyph) and, when the project has AutoContinue commands, AutoContinue with its state as the row value. Each row closes the Add sheet and then opens its own sheet (`closeThen`). Project home has no session, so it shows the tiles alone. **Recent files sheet** (`SessionFilesSheet`, Jay 2026-09-22): full height (`snapPoints ['100%']`, `topInset` = the status bar), `KortixBottomSheetModal title="Recent files"` (close button · centred title), `SheetTextInput` "Search files" (hidden while the session has no file), then the files this session produced — web's Outputs card rules, ported to `lib/session/session-files.ts` (tested): completed write / edit / `apply_patch` / `show` items / generated image, video, deck; one row per file, the later call wins; the latest run first, then shown · PDF · spreadsheet · document · deck · page · image · media. Two `SettingsGroup`: untitled (what the user came for; row = file-type glyph from `showFileTypeIcon` · name · kind as the value: PDF, Spreadsheet, Document, Slides, Web page, Image, Video) and "Other files" (`Text variant="muted"`, `mb-2 px-2`; source and config; no value — "File" says nothing). Rows keep the chevron: a tap opens another sheet. Search matches name, `show` title, path, and kind. A tap (`haptics.tap()`) opens the **file preview sheet**, a second `KortixBottomSheetModal` pushed over the list (`stackBehavior="push"`; Jay, 2026-09-22): full height, title = the file name, body = `FilePreview` (`components/files/FilePreviewRenderers` — markdown, HTML, CSV, JSON, code, text, image), loaded by `useFilePreviewData` (`components/files/`, the hook `FileViewer` uses too, so size limits and fetch rules are shared). **A file the sheet does not render fetches nothing** (Jay, 2026-09-22): `previewsInline(name)` (`lib/session/session-files.ts`, tested) is false for a PDF, an Office file (doc/docx/ppt/pptx/key/xls/xlsx/od*), an archive, audio and video; the hook gets `enabled: false` and the body is the file card — 40pt file-type glyph `text-muted-foreground` · name (`Text variant="large"`, 2 lines) · kind (`muted`). `enableContentPanningGesture={false}`: only the handle and title bar drag the sheet, so the document scrolls inside it. Loading: `KortixLoader size="large"` centred. **Failure says why** (`previewFailure`, `lib/files/preview-failure.ts`, tested): 404 → "This file is no longer in the sandbox. It was moved or deleted."; 401/403 → "You do not have access to this file."; any other status, a network error, or no sandbox URL → "The sandbox did not answer. It may be asleep or starting up." / "The sandbox is not connected. …" with a secondary `Button size="lg" rounded-full` "Try again" (`retry()` refetches). A stopped sandbox keeps its transcript but serves no file read, so this is the common failure. **Copy is in the title row, far right** (Jay, 2026-09-22): `KortixBottomSheetModal titleTrailing` → `SheetTitleRow trailing` (`components/kortix/sheet.tsx`; the slot mirrors the close button's, `-mr-2.5`, so the title stays centred). `Button variant="ghost" size="icon" rounded-full`, 20pt `CopyIcon` → `CheckIcon` for 1.5 s, `haptics.success()`, 10pt hit slop. It shows only when the file's text has loaded — never for an image, a file that is not rendered, or a failed load. **Pinned bar — the project drawer's bottom bar, copied** (Jay, 2026-09-22: "copy that exact same approach"; `components/kortix/pinned-bar.tsx` `PinnedBar`, the drawer's values): the document fills the sheet to its bottom edge and scrolls **under** the bar; the bar is `absolute inset-x-0 bottom-0`, height = safe-area bottom + 16 (`PINNED_BAR_BOTTOM_GAP`) + control height + 36 (`PINNED_BAR_FADE_ABOVE`); behind the controls a `LinearGradient` of the surface, alpha 0 → 0.85 at 45% → 1, `pointerEvents="none"`, and the bar itself is `box-none` so the transparent top passes touches to the document. The controls sit 16pt above the safe-area edge. **Not a solid footer, and no separate fade strip over the document's end** (the first build, rejected). The content ends `usePinnedBarInset(40)` above the edge (safe area + 16 + 40 + 16 `PINNED_BAR_CONTENT_GAP`): `FilePreviewBottomInsetContext` (`FilePreviewRenderers.tsx`, default 0 so `FileViewer` is unchanged) pads the markdown, text and CSV scroll views, adds `padding-bottom` to the generated code / JSON HTML and hides their language badge (it would sit under the bar), and sets the HTML WebView's `contentInset` (iOS only; on Android a page's last ~106pt can rest under the bar). The file card, the loader and the failure state centre above the bar with the same inset. **No fade under the title row** (Jay, 2026-09-22: not on a text file, not on a page in a WebView — it veils the first lines). The renderers paint the page background, so this sheet's surface is `THEME.*.background` too (`backgroundStyle`, the one deliberate chrome override here) and the bar fades to it: title row, document and bar are one plane in both themes. **Controls: two equal cells** (`PinnedBar className="gap-2 px-4"`), both `Button` **default size** (`h-10`, never `sm`) `flex-1 rounded-full`, icon 18pt + label: **Download** (`secondary`, `DownloadSimpleIcon`, `KortixLoader size="small"` in the icon's place while it runs; `downloadOpenCodeFileToCache` streams to disk, then **the device opens the file — never the share sheet** (Jay, 2026-09-22): `openFileOnDevice` (`lib/files/open-on-device.ts`). **Android:** a `VIEW` intent on the file's content URI with the read grant (`expo-intent-launcher` + `FileSystem.getContentUriAsync`, MIME type from `mimeTypeForFile`, `lib/files/mime-type.ts`, tested) — the phone starts its PDF, slides, spreadsheet or text app; part of the Expo SDK, so it works in Expo Go, where Jay tests (Samsung S24). The launch promise resolves only when the user returns, so it is raced against 500 ms and the button never stays busy. **iOS:** Quick Look (`@react-native-documents/viewer`, a custom native module: a development or store build has it, Expo Go does not). No app for the type (`isNoAppError`, `lib/files/no-app-error.ts`, tested) → `toast.info` "File downloaded. No app on this device can open it." A binary without the needed module (each is required lazily, OTA-safe like `PlatformButton`) → `toast.info` "File downloaded. Update the app to open it." **Never the share sheet and never an in-app viewer as a fallback** — Jay rejected both. The file opened but failed to launch → `toast.error` "File downloaded, but it did not open. Try again." Download failure = `toast.error`; disabled for a missing file) · **Add to chat** (primary, `PlusIcon`; disabled when a rendered file did not load; enabled for a file that is not rendered). It fires `haptics.selection()`, closes both sheets, and the draft gets `@<workspace-relative path> ` at its end (`appendFileMention`; tracked as a file mention; a file already mentioned is not added twice). Closing the preview returns to the list with the search text kept. The preview body is a plain `View className="flex-1"`, not `BottomSheetView`, which sizes to content and collapses the preview. Empty: `Text variant="muted"` "No files in this session yet" / "No matching files". The body mounts only while the sheet is open, so the memoized composer never subscribes to streamed messages. An active AutoContinue mode shows a `secondary` `icon-md` button (18pt `InfinityIcon`, `text-kortix-purple`) after the model pill. Onboarding hides `+` and the model pill |
| Chat input position | Pinned to the bottom with `px-4`. Bottom gap = `pb-3` (12pt) on both screens — one step below the `px-4` edge (§2; Jay, 2026-09-21: "if px-4 make pb-3, if px-6 make pb-5"): at rest `insets.bottom + 12`, and 12pt above the keyboard while typing. Home: `COMPOSER_BOTTOM_GAP` / `KEYBOARD_GAP` = 12, the composer follows the keyboard by `translateY`. Thread: `SessionChatInput`'s wrapper is `px-4 pb-3`, and the bottom area's `insets.bottom` padding collapses with the keyboard's progress (`useReanimatedKeyboardAnimation`), so the home-indicator inset is not added above the keyboard. Text, files, and model stay until the send succeeds |
| Add (`+`) | Opens `AttachSheet` (`components/session/`) on project home and in a thread — never a native action sheet or `Alert` (Jay, 2026-09-21). No title. One row of three equal tiles, `gap-2`: Camera (`CameraIcon`) · Photos (`ImageIcon`) · Files (`FilesIcon` — a document glyph, never a folder; Jay, 2026-09-21), data in `lib/session/attach-sources.ts` (tested). Tile: `bg-secondary rounded-2xl min-h-24`, 24pt icon over `Text variant="small"`, press = `usePressScale(0.96)` + `haptics.tap()`. `PickerSheet`'s layout: 16pt sides, 4pt under the handle, 16pt between blocks, bottom `max(insets.bottom, 16) + 8`. A tile closes the sheet and its source opens from the sheet's `onDismiss` (`closeThen`) — never two overlays, no timer: Camera → the camera, Photos → the system photo picker (multi-select, no permission prompt), Files → the system file browser (multi-select). Camera refused → `AlertDialog` "Camera access is off" · Cancel · Open Settings (`Linking.openSettings`). A source that fails to open is a `toast.error`. Both platforms get all three. Files show as chips (thumbnail · name · remove). They upload after the new session connects |
| Model list | **One list per project, the same on web, project home, and a thread** (Jay, 2026-09-21; rules ported from web's `model-selector.tsx` into `lib/session/model-picker.ts`). LLM gateway on: the project's `/model-picker` catalog (`useProjectModelCatalog`), `enabled !== false`, without the `auto` alias; every model runs through the `kortix` provider. In a thread the sandbox's copy of a model wins when it exists (it carries the thinking levels). Gateway off (no catalog): the sandbox's own providers without `kortix`, newest per family (`filterToLatestModels`); project home then has no list and hides the pill. Groups = the real upstream provider (`provider`, else the wire id prefix) — never the raw provider name, which is always "Kortix" under the gateway. Group order `PICKER_PROVIDER_ORDER` (Kortix first), unknown providers after it by title; rows by name. "ChatGPT subscription" rows drop their "(ChatGPT)" suffix; search still matches it. The label and order tables are copies of `@kortix/llm-catalog` (Metro cannot resolve it), pinned by `model-picker.test.ts` |
| Model pill | Label = the pick, else the project default (home, sent as `opencode_model`) or the resolved model (thread, "{model} · {level}"). Hidden while the list loads. **"Connect model"** when the list has loaded and the project offers no model (web's "No model connected"). Opens `ModelPickerSheet` |
| Model sheet | `ModelPickerSheet` → `PickerSheet` (`components/session/`). Layout: **16pt sides** — the project edge, `px-4` (Jay, 2026-09-21; the model sheet, the agent sheet, and the thread's Add sheet all use it, the app's other action sheets stay 20pt) — 4pt under the handle, 16pt between blocks, bottom `max(insets.bottom, 16) + 8`; the sheet has two stops (Jay, 2026-09-22): it opens at its content's height (at most 70% of the screen; 85% when the list is long enough for the search field), and a drag up takes it to full screen under the status bar. Its title is `KortixBottomSheetModal title`: the close button at the far left and the title centred, in the handle area above the scrolling list. Every group title (`Text variant="muted"`, `mb-2`) keeps the `px-2` inset — a 24pt line; `PickerSheet` renders its own group titles because `SettingsGroup`'s are `px-4`, which Jay rejected here (2026-09-21). `SheetTextInput` "Search models" when the list has more than 8 rows (`PICKER_SEARCH_THRESHOLD`; the sheet then opens at a fixed 85% so it does not resize while filtering; else it sizes to content, max 70%), then one `SettingsGroup` per provider of picker rows: label · check, no chevron, no description, no model id. Search matches name, provider, and model id. A model applies with `haptics.selection()` and **the sheet stays open** (`closeOnSelect={false}`; Jay, 2026-09-21): the check moves, the Thinking slider switches to the new model's levels, and the user closes with a swipe or a tap outside. Only the model sheet does this — the agent sheet closes on a pick. No result: `Text variant="muted"` "No matching models". No models at all: "No models available" (`large`) · 24pt · primary `Button size="lg" rounded-full` "Connect provider" — opens `ConnectProviderSheet` (COR-125/COR-158 Task 9) instead of leaving the app directly; the model sheet dismisses first (`PickerSheet`'s `afterCloseRef`, fired from `onDismiss`) so the two sheets never overlap |
| Connect provider sheet | `ConnectProviderSheet` (`components/session/`), opened from the model sheet's "Connect provider" action, from project home's model pill, and from the thread composer's "Connect model" pill (all three share one `onConnect`/`handleConnectModel` callback per screen). Handle only, no title row. Centred: two 56pt `rounded-2xl` tiles joined by a short dashed connector (`border-t border-dashed border-border`) — a `PlugIcon` on `bg-secondary`, and the Kortix symbol on `bg-foreground` (the inverted-fill tile: `KortixLogo color={isDark ? 'light' : 'dark'}`, the opposite of the page's own theme mapping, so the symbol stays legible on the tile's swapped fill; `ConnectorAuthSheet`'s Kortix tile uses the same rule). Title "Connect a model provider" (`large`), one `muted` line "Add a provider on kortix.com. You come back here when you're done.", then a primary `Button size="lg"` "Continue" and a `variant="ghost"` "Not now". Both Buttons take `rounded-full` only; Continue's label-left, `ArrowUpRightIcon`-right spread is a `flex-1 flex-row justify-between` `View` inside the Button, never a class on it. **Continue closes the sheet first, then opens the browser** (never the sheet still open behind or after it — the user lands back on the chat or home they came from, "exactly where they were"): a `pendingContinueRef` flag set by Continue, read once by the sheet's own `onDismiss` once the close animation actually finishes, which then opens `/projects/:id/customize/models` (`lib/session/connect-model.ts`'s `projectModelsWebUrl`) with `WebBrowser.openBrowserAsync`; once that closes, refetches the project's model catalog (`useProjectModelCatalog`'s `refetch`) and shows `useToast().success('Provider connected')` only when the refetched catalog is non-empty — the sheet only ever opens from a no-model state, so any model found after the trip means a provider connected. The component itself stays mounted the whole time (a permanent sibling of the composer, like `ModelPickerSheet`), so the browser/refetch/toast sequence keeps its closures even though the *modal* has dismissed. "Not now" and swipe-to-dismiss close the sheet with no action (the flag is never set) |
| Thinking | **Project home and the thread** (Jay, 2026-09-21), in the model sheet above the list, only when the active model has levels. Levels: the thread reads the sandbox model's `variants`; a gateway catalog sends `variants: {}`, so `catalogThinkingLevels` (`model-picker.ts`, web's rule) reads `reasoning_options` — the `effort` knob's values, else low/medium/high for `budget_tokens`, else none — and a sandbox model with no levels takes the catalog's. The level is stored per model in `useLocalConfigStore.modelVariants["kortix/<modelID>"]`, the store the thread reads, so home and thread always show the same level; the pill reads "{model} · {level}" on both. First message: a level cannot ride `initial_prompt`, so a text-only home send with a level creates the session with `pending_prompt { text, agent: null, model, variant }` (web's channel; the server delivers it), and a send with files adds `model` + `variant` to its `prompt_async` body. No level set → the send is unchanged (`initial_prompt`). A **stepped slider** (Jay, 2026-09-21: "the slider approach like ChatGPT"), not a segmented control: header row `px-2` — "Thinking" (`muted`) left, the stop under the thumb (`small`) right, `items-baseline`; track 44pt `rounded-full bg-secondary overflow-hidden`; stops = Default, then each level, evenly spaced, each a `size-1.5 bg-muted-foreground/50` dot; fill `bg-foreground/10` from the left edge to the thumb; thumb 36pt `bg-primary` circle, 4pt inside the track. The thumb follows the finger 1:1 with `haptics.selection()` on each stop crossed; release snaps to the nearest stop with `withSpring({ duration: 300, dampingRatio: 1 })` carrying the drag velocity (no bounce) and applies the level; a tap on the track jumps there. Only `transform` animates. Reduced motion: no spring. The pan fails on a 12pt vertical move, so the sheet still scrolls and closes. `accessibilityRole="adjustable"` with increment / decrement. The sheet stays open. Stop math: `stopOffset` / `nearestStop` (`composer-config.ts`, tested). There is no separate Thinking tab, sheet, or toolbar button. The 3-tab "Configuration" sheet is deleted — do not add it back |
| Agent | **Header, right side, on project home and in a thread — never in the composer** (Jay, 2026-09-21). `AgentPill` sits inside `ProjectHeaderActions`, `FloatingMenuButton`'s child; the row is `justify-between`, so the hamburger holds the left edge and the actions the right. **No `···` follows the pill on project home or the connecting state** (COR-123/COR-160 Task 3: the project sheet it opened is deleted — the thread's own `···` is back, see **Chrome** below), so the pill holds the edge itself there: `edge`, the default, adds `-mr-2.5` (mirrors `MenuButton`'s `-ml-2.5`). `Button variant="ghost" className="rounded-full bg-background"`, agent name (capitalised) + 14pt `CaretDownIcon`. It opens a `PickerSheet` titled "Agent" (label · check; search above 8 agents; a pick applies and closes). Agents = `pickableAgents` (primary or all — a missing mode counts as all — not hidden, not `enabled: false`). Hidden with fewer than two. Thread: the sandbox's agents, the pick is `useLocalConfigStore.selectedAgent`. Project home has no sandbox: the project config's agents (`useProjectDetail` → `/detail`), active = `homeAgentName` (web's order: the pick made on home, else the project's `default_agent`, else the last-used agent; null → no agent is sent and the pill reads "Agent"). A home pick also becomes the store's last-used agent, so the thread's pill shows it. Sent on create as `agent_name` (plus `pending_prompt.agent`, or `agent` in the files path's `prompt_async` body) |
| Chrome | One floating 40pt row (Jay, 2026-09-21): hamburger (left) · `AgentPill` (right). It belongs to the project screen, not to the home: project home, a thread and a connecting session share it. A third control, `···`, used to follow the pill and open the project sheet; that sheet is deleted (COR-123/COR-160 Task 3). **The thread's `···` is back (COR-140 Task 5)**, re-pointed at `SessionActionsSheet` for the open thread's project session: `ProjectScreen` passes `SessionPage` an `onOpenRightDrawer` that calls `openSessionActions(activeProjectSession)`, hidden until that row has loaded. Project home and the connecting state still pass no `onOpenMore` — `ProjectHeaderActions`' prop stays optional, so the pill holds the edge there. **A thread's row also carries a centred title** (COR-140, `FloatingMenuButton`'s `title` slot, `SessionThreadTitle`) between the hamburger and the right-side controls: 16pt `font-roobert-medium` title, one line, truncates. **No status or time line under it** (Jay, 2026-09-23), so the page runs no status clock. The tap target is a `PressableSurface` filling the title column (`flex-1`, 70% opacity while pressed, `hitSlop` 10pt top/bottom), never a `Button` restyled by class. `lib/session/thread-status.ts` (pure, tested) still holds the words for a status line — "Needs you", "Working[ · N min / N hr]", "Done[ · 3 minutes ago / 2 hours ago / 4 days ago]" (`spokenRelative` buckets), and no line for a session with no message yet — but no screen renders it. Tapping the title calls `onRenamePress`, which `ProjectScreen` wires to `openSessionActions(activeProjectSession, 'rename')` — the **same** `SessionActionsSheet` Rename view the `···` menu opens, never a second rename implementation; omitted (title reads inert) until `activeProjectSession` has resolved, the same guard `onOpenRightDrawer` uses. The legacy static header bar this row used to alternate with (`chrome === 'header'`) never rendered in production — no call site passed it, nor set the `onboardingMode` that was its only other trigger — and was deleted along with the inline-rename state that belonged only to it. The `chrome` prop itself is gone from `SessionPage`: the floating row is the only chrome, the list always insets below it (`insets.top + FLOATING_MENU_CLEARANCE`) and the composer always rests above the safe area |
| Sub-agent (COR-162) | **One relation, one term, app-wide:** a *sub-agent session* is a project session spawned by another project session — `metadata.spawned_by_session`, read through the SDK's `sessionParentId` (`lib/session/sub-agents.ts`, tested). The session list nests by the same relation (§6 and §7 "Sub-agent sessions"). OpenCode's own sub-session tree (`parentID`) is not read. `ProjectScreen` computes `subAgentRelation(activeProjectSession, projectSessions)` and `subAgentsOf(...)` over the rows it already has (no extra fetch) and passes them to `SessionPage`. In the header's right-side row, before the `···`, `SubAgentHeaderChip`: an **icon-only 32pt pill** (Jay's ruling, 2026-09-23 — the title keeps the width): a `PressableSurface` `rounded-full bg-background`, 32pt tall, `hitSlop` 6 (44pt target). `child` → `ArrowElbowLeftUpIcon` alone, 32 × 32; tap opens the parent session. `parent` → `TreeStructureIcon` + the count (`Text variant="small"`), 10pt side padding; tap opens `SubAgentListSheet`. A session is never both in the header: a resolvable parent wins. The words are the `accessibilityLabel` only: "Sub-agent of {parent}" / "{N} sub-agents". `SubAgentListSheet`: `KortixBottomSheetModal title="Sub-agents"` + one `SettingsGroup` of the Sessions page's rows (`SessionStatusMark` · title · `shortRelative` time, no chevron, no description). The parent and every sub-agent open through the project-session open path (`onOpenProjectSession` → `ProjectScreen.handleOpenProjectSession`, the drawer row's path), never `navigateToSession` |
| Rows on a sheet (design law) | A `SettingsGroup` inside `KortixBottomSheetModal` (and `Sheet`, built on it) fills its rows with `SHEET_ROW_SURFACE` = `bg-secondary dark:bg-background` on its own (Jay, 2026-09-23): the sheet provides `SurfaceContext` "sheet" (`components/kortix/surface-context.ts`). Light: `secondary` 92.6% tiles on the white sheet. Dark: `background` 4.3% wells under the 7.8% sheet. The page default `bg-card` is 7.8% in dark — the sheet colour itself — so the Customize sheet's rows had 0% contrast. Never pass `className="bg-secondary"` to a group in a sheet; a `popover` dialog (Appearance) passes `SHEET_ROW_SURFACE` |
| New header controls (design law) | A control added to any header or chrome row — the project chrome (Chrome, above), a `PageHeader`, or anywhere else a button gets introduced — prefers floating over a gradient fade of the surface behind it, not a flat `bg-background` icon button sitting directly on the page (Jay, 2026-09-22). The recipe already exists: `PinnedBar` (`components/kortix/pinned-bar.tsx`) — clear → 85% at 45% → solid, ending on the real surface colour, never `transparent` (a grey band on Android). The drawer's own avatar · New session row (`ProjectLeftDrawer`) is the reference example Jay pointed at: the identical gradient, hand-rolled inline there because it predates the `PinnedBar` extraction and has not been migrated onto the component. Reuse `PinnedBar` (or its exported `PINNED_BAR_FADE_ABOVE` / `PINNED_BAR_BOTTOM_GAP` constants for a custom shape) instead of a second hand-rolled `LinearGradient`. This is a preference for controls added from here on — it does not mean retrofitting the existing flat controls documented above (`PageHeader`'s control row) unless asked |
| Project sheet | **Deleted (COR-123/COR-160 Task 3):** `CustomizeSheet` and its data, `CUSTOMIZE_SHEET_GROUPS`, no longer exist. `lib/session/dock-menu.ts` now holds only `PROJECT_CUSTOMIZE_ITEMS` (Schedules, Secrets — the project Settings page's "Customize" group, **Settings** below) and the icon keys `PageContextMenuSheet` still uses for its own unrelated Workspace prompts. Agents, Skills, Members and Terminal have no mobile page any more (**Agents, Skills, Terminal**, above, and **Members**, below — both rows deleted). Review moved into the drawer (Task 4, **Project sidebar** above), carrying the same needs-you count (`countReviewItemsBySegment(...).needs_you`, computed once in `ProjectScreen` as `reviewNeedsYouCount`) this sheet's Review row used to show. Files stays reachable from the drawer only (never had a sheet row of its own function — `FilesNavPage` mounts at `/projects/[id]/files`). Connectors, Models, Channels, Sandbox, Commands, and Dev still have no row and no reachable page, unchanged from before this task |
| Tool page header | `PageHeader` (`components/kortix/page-header.tsx`), on every project tool page (Schedules, Review, Secrets, Files, Browser, …). The Sessions page's layout (§7), so every project page reads the same (Jay, 2026-09-21). Control row: `px-4 pb-3`, top padding `max(inset, 10) + 6`, 56pt min height, `justify-between` — hamburger left (`MenuButton`, `-ml-2.5`), or, on a pushed sub-page (project Settings, Schedules, Secrets opened from a page), Go back (`onBack`, the `PlatformButton` chevron `SettingsHeader` uses) in its place; right, in this order: the page's own `rightActions`, then `+` (`onAdd`, the page's one create action, `addLabel` e.g. "New agent"). **No `···` any more** (COR-123/COR-160 Task 3): it opened the project sheet, which is deleted; `ProjectScreen`'s `pageChrome` no longer passes `onOpenRightDrawer` to any tool page, so `PageHeader` shows none. Every control is `Button variant="ghost" size="icon" className="rounded-full"` with a 20pt icon; the group carries `-mr-2.5`, so the last glyph sits on the 16pt padding edge. Title row below: `h-10 px-4 pb-1`, `Text variant="h3"` (24pt Roobert semibold, foreground), role header, one line. **The title is never a grey 16pt label beside the hamburger, and `+` never sits beside the search field**: `SearchListHeader` on these pages takes no `onAdd`, so the field spans the full width under the title |
| Agents, Skills, Terminal | Deleted (COR-123/COR-160 Task 3): `AgentsPage`, `SkillsPage`, `ConfigEntriesPage`, `lib/projects/configure-prompts.ts`, and `TerminalPage` no longer exist. `page:agents`, `page:skills`, and `page:terminal` are no longer registered page ids. Mobile authors an agent or a skill only through a session, same as web's file editor; there is no in-app terminal. A skill still RUNS from a thread — the composer's `#` trigger (COR-160 remaining part, **Thread additions** above) replaces the deleted page for invoking one. The Workspace page's `···` menu still offers **New agent** / **New skill** / **New command** prompts (`PageContextMenuSheet`) — those start a session, they do not open a deleted page, and are unaffected |
| Schedules | `PageList` of one `SettingsGroup` of `SettingsRow`s (Jay, 2026-09-22; no icon tile): name, then `schedule · last run · agent` as the `description`; a 6pt `StatusDot` before the chevron (green active, grey paused). **No "N of M active" line** (Jay, 2026-09-22). The create and detail sheets open at full screen (`snapPoints={['100%']}`, one stop; Jay, 2026-09-22: never a lower first stop). Detail sheet (schedules): the body sits in `SheetFill` (`components/kortix/sheet.tsx`) so the Fire now · Pause · Delete row is a `PinnedBar` floating over a fade of the sheet with the settings groups scrolling under it, the agent and skill detail's fade (Jay, 2026-09-22) — never a solid bar. `SheetTitleRow` with the trigger's name — **no status badge, no slug, no icon tile**; a scroll body of `SettingsGroup` rows (Runs · Cron · Time zone), the prompt as a **read-only tile** (`PromptPreview`: muted `px-4` title, a `rounded-2xl bg-secondary` tile with up to four lines and a chevron) — **never an editable field in the sheet's scroll view**: a multiline input there takes the touch, so a scroll that starts on it focuses it and raises the keyboard (Jay, 2026-09-22). A tap pushes `PromptEditView` in from the right (`sheet-push`): title "Prompt" with a Back chevron, an auto-focused `SheetTextInput` (160pt min), the placeholder line, and a pinned Save pill; a saved prompt pops back to the detail. Both live in `components/pages/TriggerPromptField.tsx`, shared by the schedule and the webhook sheets. Then the Agent and Model dropdowns (their titles use the same muted `px-4` style as "Prompt"), then Last fired · Source rows; and a **pinned action bar** under the scroll view, always on screen: Fire now (`lg`, flex-1) · Pause/Resume (`secondary lg`) · Delete (`secondary icon`, destructive glyph), on the sheet colour with the safe-area inset below. Agent and Model are the app's dropdown (`@/components/ui/select`, trigger `h-11 rounded-xl border-0 bg-secondary shadow-none`) with `portalHost={OVERLAY_PORTAL_HOST}`: the host mounted after `BottomSheetModalProvider` in `app/_layout.tsx`, because the default host sits under an open sheet on Android. Model options come from `/model-picker` (8–13 enabled models), never `/llm-catalog` (thousands of rows froze the sheet) |
| Secrets | `PageList` of one `SettingsGroup` of `SettingsRow`s (Jay, 2026-09-22): the name, then `Required/Optional · status · scope` as the `description`; an orange 6pt dot when a required secret has no value; no footer (Jay, 2026-09-22: mobile has no models screen to link to). Detail sheet: `SheetTitleRow` with the secret's name, then `SettingsGroup` groups — "Use in my sessions" (Shared value / My value as picker rows with a check, only when there is a choice), "My value", "Shared value" — never a segmented pill or uppercase labels. A value form **pushes in** over the detail with the activity sheet's motion (`components/kortix/sheet-push.tsx`: `PUSH_IN` from the right, `POP_IN` back from the left, 250ms strong ease-out, none under reduced motion); the pushed form's title row shows `SheetBackButton` in the close button's slot |
| Settings | `SettingsNavPage` (`page:settings`), a full project page, not a sheet section any more (COR-123/COR-160 Task 3). One entry point: the Account page's project row (COR-120 Task 2, reached from the drawer avatar) → a **sub-page** pushed over Settings (`openSubPage('page:settings')`; **Project stack** above). The drawer's own gear button, its other entry point, was removed in COR-124/COR-157 Task 4 (**Project sidebar** below). As a sub-page its `PageHeader` shows Go back (`onBack`, the `PlatformButton` chevron) in place of the hamburger; back returns to Settings (Jay, 2026-09-23). `PageHeader title` = the project's name (Jay, 2026-09-23; was the tab label "Settings" — that stays only as the loading fallback). `PageList` of `SettingsGroup`s **with group titles** (Jay, 2026-09-23 — reversed from the earlier "no group title" rule, now that this page also holds the project sheet's rows: "General"/"Repository"/"Danger zone" is gone, but "Customize" and "Details" separate what used to be two sheets). Every sheet renders through `KortixBottomSheetModal` directly (Jay, 2026-09-22: not the `<Sheet>` wrapper), the Schedules/Secrets shape. **Customize** (new, Jay, 2026-09-23) — Schedules and Secrets (`PROJECT_CUSTOMIZE_ITEMS`, `lib/session/dock-menu.ts`; icon · label, tap pushes the page as a sub-page over this one — `onOpenPage` → `openSubPage` — so it shows Go back and back returns here), then two `external` rows opened in the in-app browser (`lib/projects/web-project-links.ts`): Members (`UsersIcon`) → `/projects/<id>/members`, "More on kortix.com" (`GlobeIcon`) → `/projects/<id>/customize`, web's full Customize hub. This group is what is left of the project sheet (**Project sheet**, below): Agents, Skills, Members and Terminal have no mobile page any more; Review moves into the drawer (Task 4); Files stays reachable from the drawer only. **Details** (was **General** + **Repository**, merged and retitled) — Name (was "Project name", value = current name, tap opens a rename sheet: `KortixBottomSheetModal title` + one `SheetTextInput` + Save, the app's edit-a-field pattern — never an inline input on the page), Repository (icon Github when a GitHub URL resolves else `GitBranch`, value = `owner/repo`, external + tap opens it), Default branch and Manifest path (value rows, tap opens the same rename sheet, `mono`), Add collaborator (managed repos + managers only, opens a small invite sheet: GitHub username field, Can edit/Can view toggle, Add). No experimental-features row (Jay, 2026-09-22: `useUpdateExperimentalFeature` stays in `lib/projects/hooks.ts`, unused by this page). **Delete project** (managers only, its own untitled group, was "Danger zone") — `destructive`, a two-step confirm (Jay, 2026-09-22, GitHub's repo-delete shape): a sheet asks to type the exact project name to enable `Continue` (`variant="destructive"`, disabled until the typed text matches `project.name`), then **the app's `AlertDialog`** (`@/components/ui/alert-dialog`; Jay, 2026-09-22 — never `Alert.alert` for a confirm/cancel decision, only for a one-button acknowledgement) "Are you sure?" opens after that sheet dismisses — the first consumer of `AlertDialog` in the app. `AlertDialogAction` needs an explicit destructive override (`className="bg-destructive active:bg-destructive/90 dark:bg-destructive/60 text-white"`): its own component always renders the `default` button variant. Confirming calls `archiveProject` (there is no hard delete; "Delete project" is the live label web itself shows over that call) and redirects to `/projects`. Every edit row is read-only (no `onPress`, no chevron) for a non-manager |
| Members | Deleted (COR-123/COR-160 Task 3): `MembersNavPage` and `page:members` no longer exist. The project Settings page's Customize group has a "Members" row instead (**Settings**, above): `external`, opens `${getFrontendUrl()}/projects/<id>/members` in the in-app browser — inviting, roles, and groups all move to web |
| Browser | `BrowserPage` (`page:browser`; Jay, 2026-09-23). **Header:** `PageHeader fillTitle` — hamburger · address field · `···`; `fillTitle` gives the title all the width the buttons leave instead of the centred third. Address field: `h-10 rounded-full bg-secondary px-4` around a transparent `Input` (16pt Roobert, placeholder "Search or enter URL or port") — **text only, no globe, no spinner** (Jay, 2026-09-23). No buttons in the header. **Page:** the site fills the page to the screen's bottom edge on both platforms; the toolbar floats over it. iOS insets the scroll (`contentInset.bottom = usePinnedBarInset(44)`) so the site's end can rise above the controls and has the edge-swipe back/forward (`allowsBackForwardNavigationGestures`); Android WebView has no content inset, so a site's last ~120pt rests under the bar. **Toolbar = the project drawer's bottom bar, copied** (avatar · New session): `PinnedBar controlHeight={44} fade={false} className="justify-between px-5"` — **no fade** (Jay, 2026-09-23): the opaque capsules float straight over the site; controls 16pt above the safe area, one control group at each edge. Left: a 44pt `bg-secondary rounded-full` capsule of Back · Forward; right: the same capsule of Reload (`XIcon` "Stop loading" while loading) · Open in browser. Each control is `Button variant="ghost" size="icon" rounded-full`, 20pt glyph; a control with nothing to do is disabled (50% opacity). Loading: `KortixLoader size="large"` centred. Empty: 32pt globe · "Browser" (`large`) · one `muted` line |
| Files | `FilesNavPage` (`/projects/[id]/files`, the drawer's Files pill only — COR-123/COR-160 Task 3 removes the project sheet, which had a Files row too): the read-only git browser. `PageHeader` with the title only, no `···`; a full-width search; a breadcrumb of chips on a 40pt line (never clipped) only inside a folder. Entries show their name without the extension (`package`, not `package.json`; `displayNames` keeps the extension on names that would collide once bare, e.g. `kortix.html` beside `kortix.yaml`; the full name is the file sheet's title) and a filled glyph: a folder in its tone (`lib/files/folder-tone.ts`: one of the six `THEME.accent`s by a stable hash of the name, case and leading dot ignored); a file by its type (`lib/files/file-icon.ts` → `components/files/file-icons.tsx`: one glyph per web language — TSX, TS, JS, JSX, HTML, CSS, Vue — plus PY, RS, C, C++, SQL, MD, TXT, CSV, INI, PNG, JPG, SVG, PDF, DOC, XLS, PPT, ZIP; the Kortix symbol in the theme's colour at 0.72× the glyph size on `kortix.*`, since the symbol fills its box and the glyphs do not; the git glyph on `.git*`), filled, in the muted foreground — filled reads lighter than the app's bold outline. List: `SettingsGroup`s "Folders" and "Files" of `SettingsRow`s (22pt glyph · name · size as value). The grid is the default view (Jay, 2026-09-22). Grid: 2-up `bg-card` tiles — 36pt glyph, `Text variant="small"` name, `text-xs` muted size on a file, no "Folder" line. Inside a folder the title is the folder's name and the hamburger stays — **no Go back in its place, on any page** (Jay, 2026-09-22); a crumb goes up, and Android back goes up one folder (registered after ProjectScreen's handler), never project home. **No press animation** (Jay, 2026-09-22): rows and tiles use the pressed fill only. All controls live in a `PinnedBar` at the bottom, the drawer's bar: version pill (`GitBranchIcon` · ref, opens the Version sheet, a `SettingsGroup` of picker rows) at the left, then sort and download (`DownloadSimpleIcon`) as `secondary icon` buttons with the list/grid toggle between them: the app's `Tabs` (`TabsList` as a 40pt `bg-secondary` pill, two icon `TabsTrigger`s, the active one on the page background), never two icon buttons. No refresh button: a pull on the list refetches the files and branches (`RefreshControl`, tint `mutedForeground`), as on every other list. A file opens in a `KortixBottomSheetModal` at full height with `enableContentPanningGesture={false}` and the page background: the file name as the title, `CopyContentButton` as `titleTrailing` once the text has loaded, `FilePreview` as the body under `FilePreviewBottomInsetContext`, and a `PinnedBar` with Download · History (two equal `secondary` cells) — the recent-files sheet's layout. History pushes in (`sheet-push`) as a `SettingsGroup` of commit rows, and a commit pushes its `PatchDiffView` in after it; Back walks back. Never a `Modal`, never a hand-animated sheet |
| Review | `ReviewPage` (`page:review`): web's Review Center without bulk select, keyboard layer, or per-session grouping. `PageHeader` with `rightActions` = one `icon` ghost button and `onAdd`, so the control row reads history · `+` · `···` (Jay, 2026-09-21): `ClockCounterClockwiseIcon` "Versions" → a sheet that lists the project's branches (`BranchRow`; title "Versions"; scrolls, at most 80% of the screen; the branches load when the sheet first opens), then `onAdd` ("Open a change request") → `OpenCRSheet` at 88% (on success: Needs you segment, list refetch, success toast). Versions is a sheet, not a tab. Segments (Jay, 2026-09-23: no tab list or segmented control — it looked off on Android): in the pinned bottom bar (`PinnedBar`, 40pt, `gap-2 px-4`), three pill `Button`s (`size="default"`, `rounded-full`, `gap-1`) — the active one `variant="default"`, the others `ghost` — Needs you (plus its count, "Needs you 2"), Waiting, Done; then the `+` (`secondary` `icon`) at the right. `accessibilityRole` tablist / tab with `selected`; a change fires `haptics.selection()`. Rows (Jay, 2026-09-23): one `SettingsGroup` of `SettingsRow`s in a `px-4` column, the app's list (never `ListRow`) — `leading` = kind icon (`components/review/review-icons.ts`) in the item's tone (`reviewItemTone`: an open item takes web's kind colour — change blue, approval orange, output purple, decision yellow, batch green; a finished item takes its outcome — approved or done green, rejected red, changes requested orange, dismissed muted) · `label` = title · `description` = one line `summary · agent · age · risk` · chevron. Empty segment: one muted line, no icon, no button. A failed background refetch keeps the list. The item model and its mapping come from `@kortix/sdk` (`mapApiReviewItem`, `reviewSegmentForStatus`, `reviewItemTarget`); copy and rules are `lib/review/review-meta.ts`, the verdict call is `lib/review/review-verdict.ts` (both under `bun test`). `ReviewDetailSheet`: `PickerSheet`'s layout, at most 90% of the screen; title, kind and risk `Badge`s, agent; one body per kind (change: what changed, impact, conflicts, checks, requested changes, `PatchDiffView`; approval: each call's arguments, or "You do not have access" when the API withholds them; decision: options as `SettingsRow`s, a tap answers; output: note, text preview, external preview row; batch: children with `value`). Verdicts only while `status === 'needs_you'`, in the sheet's pinned footer (`BottomSheetFooter`, sheet background, 12pt above, safe-area below; the scroll content pads by its height), so they stay on screen over a long change. Two verdicts share one row, 50/50, the primary on the right: a change is Request changes · Merge (**no Close on a change**, Jay, 2026-09-21), an approval is Deny · Approve. Three verdicts stack as full-width `lg` pills, first `default`, Request changes `secondary`, the rest `ghost`. Request changes swaps the pills for a `SheetTextInput` + Cancel / Send. Merge a change and Approve or Deny a connector call confirm in an `AlertDialog` that opens after the sheet has closed. A Change Request or a connector call never reaches `/act`: `cr:` ids merge / close / request changes, `call:` ids go to `resolveApproval` |
| Response text | Assistant markdown body is **16pt / 26pt line** (`TYPE.body`, `lib/markdown/markdown-layout.ts`; Jay, 2026-09-22: one step above web's 15px `.kortix-markdown`). The ordered-list gutter and display-math margins derive from it. Inline code (14pt) and code blocks (13pt) are unchanged |
| Sandbox unreachable bar | `SandboxHealthPill` (`components/session/SandboxHealthPill.tsx`), directly above the chat input while the sandbox does not answer. **Drawn exactly as the composer card** (Jay, 2026-09-23): `px-4` edge, `rounded-3xl border border-border bg-background p-2`, no shadow. Row: orange ping dot · `Text variant="muted"` "Unreachable · {elapsed}" (in `px-2`, on the composer's text inset) · Switch as the composer's pill (`Button variant="secondary" size="sm" rounded-full`, 14pt icon). 8pt (`pb-2`) above the composer |
| Scroll to bottom | `ScrollToBottomButton` (`components/session/SessionPage.tsx`): `Button variant="secondary" size="icon"` `rounded-full border border-border` on every platform (Jay, 2026-09-22: a border; no native Liquid Glass, which cannot carry one), 20pt `CaretDownIcon`, label "Scroll to bottom". Shown once the reader is more than 120pt from the end. **Bottom-right, directly above the chat input** (Jay, 2026-09-22): `right: 16` (the project edge, `px-4`), `bottom: 32` — 8pt above the 24pt fade over the list's bottom, which would paint over it if lower. Opacity + scale 0.97 → 1 |
| Thread top fade | `FloatingMenuButton fade` (the thread only: project home and the connecting state do not scroll). A header strip of the page background behind the button, `pointerEvents="none"`: solid from the screen edge to the bottom of the button (`insets.top + 8 + 40`), then a 24pt `expo-linear-gradient` (alpha 1 → 0) below it — the gradient starts under the header row, never inside it (Jay, 2026-09-21). The strip ends at `insets.top + FLOATING_MENU_CLEARANCE` (72pt: 8 offset + 40 button + 24 fade). The list's top inset is the same clearance, so the first turn and the pinned newest turn rest below the fade and are never dimmed; turns that scroll above it fade out, then are hidden behind the header row and the status bar. Mirrors the 24pt fade above the chat input |
| Never | A greeting sentence, the project name, starter chips, recent-session lists, cards, widgets, subtitles |
| New chat, no messages | `SessionPage`'s `FreshSessionHero` renders the same `ProjectHero` (same size, same keyboard scale) |
| Session connecting (COR-146) | `SessionConnecting` (`components/session/`), driven by `ProjectScreen`'s `ensureAndOpen` loop (`/start` polling, `MAX_WAIT_MS` 4 min). **Honest, not a bare spinner.** A 3-step checklist (`lib/session/connect-progress.ts`, tested — pure, no invented progress): Sandbox created (the `/start` sandbox row is `active` with a resolved `external_id`), Project files ready (the loop's `probeSandboxHealth` proxy probe answered without `unreachable` — the same check that surfaces a `boot_error`, so a clean answer is the best signal the repo materialized), Starting the agent (`/start` returned `stage: 'ready'` with the OpenCode pin). A done step: a check (`CheckIcon`) in `THEME.accent.green`, muted label. The current step: `KortixLoader size="small"` (never `animate-spin` an icon) + a live "0:24" elapsed timer (`formatElapsed`, mm:ss) next to the label. Files-ready and agent-starting often complete on the same poll tick (one probe clears both) — that is the real signal shape, not a bug. **First message bubble:** when this connect attempt started from a project-home send with text, that text shows at once as a right-aligned `UserMessageBubble` (same component and text style as the thread's own user bubble), thread-style, above the checklist — not shown for a blank "New session" or a session reopened from the list (never fetched specially for this screen). **Muted line** under the checklist: "Usually under a minute. You can leave; we'll notify you when it replies." only when this device currently has push permission granted (`usePushNotificationsRegistered`, `hooks/`: a passive `getPermissionsAsync` read, never a prompt — the app has no global push-registration store, `usePushNotifications` only registers from the Notifications settings screen); otherwise "Usually under a minute." alone. **Cancel, always available**, both in the checklist and in the inline failure state (below): a `PinnedBar` `Button variant="secondary" size="lg"` "Cancel" pinned to the bottom (the Button takes `rounded-full` only; a `flex-1` wrapper `View` gives it the bar's width) (`ProjectScreen.handleCancelConnect`). It stops the client-side connect loop and returns to project home at once — never blocked on a network call. A session THIS screen just created (a fresh "New session" or a project-home send, tracked by `freshSessionIdRef`) is **deleted** server-side (`deleteProjectSession`, the call the actions sheet's Delete makes; one retry after 2 s; no confirm dialog — the session is disposable), so no stopped session keeps the cancelled prompt and a re-send of the restored draft cannot run it twice. Both session lists (`projectSessions`, `projectSessionsPaged`) are invalidated after it settles. A session reopened from the sessions list is never deleted or stopped — it may hold real history the user still wants, so Cancel there is client-side only. The typed text of a cancelled dashboard send is restored into the project-home composer: `ProjectHome`'s `takeInitialDraft` prop, read once (lazy `useState` init) and cleared, seeded by `ProjectScreen` bumping `homeKey` to force a remount. A Cancel during the view route's push transition keeps the text: `handleViewCovered` (the push's `transitionEnd`) bumps `homeKey` only while the store is off project home (`isHomeRef`), so a push that ends after Cancel returned home does not remount home a second time. The elapsed timer starts when a connect starts — at an entry point, or when the connecting view first shows without one (an opened thread whose sandbox has not switched in) — never at screen mount. Runtime failure (a `boot_error`, a terminal `/start` error, or the 4-minute timeout) still renders the existing inline error state instead of the checklist — message + detail + Restart (web parity, `InlineSessionError`) — with Cancel still pinned below it |

## 6. Project sidebar

The left drawer on `/projects/[id]` (`components/session/ProjectLeftDrawer.tsx`).
It opens from every project page: the hamburger, or an edge swipe (80pt edge,
`swipeEnabled` while the project screen is focused) on any project route.
A swipe closes it (no close button). Every action calls `onClose()` before it
navigates, except the switcher row, which opens a sheet over the drawer
(COR-124/COR-157 Task 4) — only a pick inside that sheet closes it. Navigation
from the drawer never deepens the project stack beyond one screen over home:

| Action | Project home on top | A covering route (`view`, `sessions`, `files`, `account`) on top |
| --- | --- | --- |
| Search / Files / avatar | push that route | replace the top route with it; nothing when it is already on top |
| Review | tab-store `navigateToPage('page:review')` — leaving the home state pushes or replaces the covering route with `view`, the same mechanism as any other page | same |
| Session row | tab store update → home pushes `view` | `view`: the row of the session on screen only closes the drawer; another row updates the store and the view swaps content. `sessions`/`files`/`account`: store update, the route replaces itself with `view` (`useCoveringRoute`) |
| New session | go to project home | go to project home + `popTo('index')` |
| Switcher row → a picked project | `router.replace` to it (`ProjectSwitcherSheet`, see below); the drawer closes with the sheet | same |
| Search / Files / avatar with sub-pages on top | — | the drawer cannot open on a sub-page (no hamburger, edge swipe off). If it is open anyway: pop back to the route when it is the one under the sub-pages, else reset to `[index, route]` (`drawerRouteMove` `pop-to` / `reset`) |

```
┌──────────────────────────────────────────┐  full screen width
│ [■] Project name  ⌄                      │  switcher row, opens the sheet below
│     Account name                          │
│   ⌕ Search                                │  box edge 16pt, icon edge 28pt
│   ▢ Files                                 │
│   ✓ Review                           (3)  │  trailing count pill, hidden at 0
│  Sessions                                 │  muted section label
│  ◌ Session title                          │  scrolls; every session
│  ● Session title                          │
│  Previous chats                           │
│ ░░░░░░░░░░░░░░░░ fade ░░░░░░░░░░░░░░░░░░ │
│ [+ New session]                   (photo) │  pinned, 16pt above safe area
└──────────────────────────────────────────┘
```

| Element | Value |
| --- | --- |
| Drawer | `react-native-drawer-layout`, `drawerType="slide"`, width `100%`, transparent overlay, no shadow (`ProjectScreen`) |
| Drawer close | Faster than open (Jay, 2026-09-22): `closeSpringConfig={DRAWER_CLOSE_SPRING}` — `stiffness: 400, damping: 40, mass: 1`, critically damped, no overshoot: 90% of the travel in ~195ms, settled in ~330ms. Hamburger, session row, and swipe-release closes all use it; a swipe keeps its velocity. Open keeps the library spring (`1000 / 500 / 3`). The prop comes from `patches/react-native-drawer-layout+4.2.10.patch` (the library hardcodes one spring); without the patch `tsc` fails on the prop — run `npx patch-package`, then restart Metro with `--clear` |
| Surface | `bg-chrome-background` (the web `--sidebar` token), `insets.top` top padding. **One straight left line at 20pt** (Jay, 2026-09-16): the switcher row, the Search / Files / Review icons, the "Sessions" label, the session status marks, and Previous chats all start at 20pt — the switcher row, like every other row, is `px-3` inside a `px-2` column |
| Switcher row (COR-124/COR-157, replaces the old logo-only header) | Minimal (Jay, 2026-09-23): **no project tile**. One button edge to edge — a `px-2 pb-1` column holding a `Pressable` `flex-row items-center gap-3 rounded-2xl px-3 py-2 active:bg-accent`, `hitSlop={4}`, label "Switch project, {project}, {account}"; children are `pointerEvents="none"`, so every part presses it. Leading: the Kortix symbol (`KortixLogo variant="symbol"`, 32pt, theme-coloured) in a 32pt (`w-8`) slot — the height of the two text lines (20pt + 17pt line boxes), so it spans both. Then a `min-w-0 flex-1` column: the project name (16pt / 20pt line, `font-roobert-medium`, one line) over the account name (13pt / 17pt line, `muted`, one line). **No caret** (Jay, 2026-09-23). Tap → the switcher sheet (`ProjectSwitcherSheet`, mounted in `ProjectScreen`) |
| Nav rows | `NavPill` rows in a `px-2 pb-2` column, no gap: `Pressable` `flex-row items-center gap-3 rounded-full px-4 py-2.5 active:bg-foreground/5`, icon `size={18}` `text-foreground` on the 20pt line, label `Text` `font-medium flex-1`, one line, an optional trailing node. Order: Search (`MagnifyingGlassIcon` → the Sessions page, `navigateProjectRoute('sessions', { autoFocusSearch: '1' })` — the field is focused on arrival; this replaces the old plain "Sessions" row, since Search is now the only drawer path to it), Files (`FoldersIcon` → `files`, unchanged), Review (`SealCheckIcon`, the same glyph as the Review tab icon, → `useTabStore.getState().navigateToPage('page:review')`; a trailing pill — `bg-kortix-blue/15` / `text-kortix-blue`, the "needs-you" blue, not web's amber — shows `reviewNeedsYouCount` when it is greater than 0, hidden at 0; the count is `countReviewItemsBySegment(...).needs_you`, computed once in `ProjectScreen` and passed down, the same source the deleted project sheet's Review row badge used). The old "All projects" row is gone — the switcher row's `+`/chip flow replaces it |
| Sessions label | `Text variant="muted"` "Sessions", `px-3 pt-3 pb-1` inside a `px-2 -mx-1` wrapper (the row column's own inset), above the list, outside its scroll and its top fade |
| Double-tap guard | Search, Files, Review, and the avatar navigate once: the first tap sets `navigatingRef`, later taps do nothing. The guard resets when the drawer's visibility flips — `useDrawerProgress()` crosses 0.01 (fully closed, or visible again), read with `useAnimatedReaction`. No timer. The drawer content stays mounted while closed, so mount cannot reset it. The switcher row does not use this guard — it opens a sheet, not a route |
| Row style | Menu rows, not free-standing buttons: no resting fill, no border; the pressed `bg-accent` box and the open session's resting `bg-accent` box sit on the parent's `px-4` edge with the same `rounded-xl` radius |
| Session list | `ScrollView` under the nav rows, no indicator, `paddingTop` 4. Rows in a `px-2` column (status marks on the 20pt line): `Pressable` `flex-row items-center gap-3 rounded-xl px-3 py-2 active:bg-accent`. Open session: the row of the session on screen (an open thread or a connecting session, `shownProjectSessionId`) is `bg-accent` at rest with `accessibilityState={{ selected: true }}`, same font weight; tapping it only closes the drawer (`drawerSessionRowMove`), so the thread is not remounted or reconnected. Row = `SessionStatusMark` (the same marks as the Sessions page, see §7) · title (`Text` default, one line, `sessionDisplayTitle`). Label "{title}, {status}" (`sessionStatusLabel`); hint "Long press for session actions". **Long press opens `SessionActionsSheet`** (COR-140 Task 5) over the drawer — the same sheet the Sessions page's long press and the thread's `···` open, through `onSessionActions` (`openSessionActions` on `ProjectRouteValue`); the drawer does not close for it, the switcher row's exception. Tap is unchanged. Rows are **every session of the project** (Jay, 2026-09-21 — no 20-row cap), newest `sessionLastActivityAt` first, from `useProjectSessionsPaged`: a `useInfiniteQuery` over the API's keyset pages (`listProjectSessionsPage`, 50 rows, `x-next-cursor`). The list is an `Animated.FlatList` (virtualised): `onEndReached` at 0.6 loads the next page (`shouldLoadMoreSessions`: one page at a time, never during a pull), a `KortixLoader size="small"` `py-4` shows under the rows while it loads, then Previous chats. Pull to refresh: `RefreshControl` (tint `mutedForeground`) refetches the loaded pages; a background poll never shows the spinner. The Search row opens the same list, with its search field already focused |
| Sub-agent sessions (COR-162) | A sub-agent session (the one relation defined in §5 "Sub-agent": `metadata.spawned_by_session`, read through the SDK's `sessionParentId`) whose coordinator is in the loaded list renders as a **sub-agent row nested directly under its coordinator**, not sorted into its own place by activity — the same composition web uses (sort first, group by coordinator second). `flattenSessionGroups` (`lib/session/session-list.ts`, wraps `groupSessionsByCoordinator`) turns the sorted, flat `recentSessions` list into coordinator-then-children row order for the `FlatList`. A nested row: `marginLeft: 16` indent, a muted 12pt `ArrowElbowDownRightIcon` (`@/lib/icons`) before the `SessionStatusMark`, accessibility label "{title}, sub-agent session, {status}". A child whose coordinator has not loaded yet (its page hasn't arrived — the list loads 50 rows at a time) stays top-level and re-nests automatically once it does, since grouping is recomputed from whatever is loaded on every render. Web nests only one level (a grandchild whose immediate parent is itself a child disappears there); `groupSessionsByCoordinator` here resolves every session to its topmost loaded ancestor instead, so a deeper chain still renders, flattened under its real root, rather than vanishing |
| Loading | `KortixLoader size="small"` centred, `py-8` |
| Empty / error (COR-146) | Which of the two shows comes from `sessionListState` (`lib/session/session-pages.ts`, shared with the Sessions page, §7, tested with `bun test`): **empty** — the query succeeded with zero sessions — `Text variant="muted"` "No sessions yet", `px-3 py-2` (aligned with a row's title). **error** — the query failed and no session survived (a failed background poll or pull-refresh with rows already loaded stays `rows`, never `error`) — centred `items-center gap-2 px-3 py-6`: `Text variant="small"` (one line, `leading-5`) "Couldn't load sessions", `Text variant="muted"` "Kortix didn't respond. Your sessions are safe.", then `Button variant="secondary" size="sm"` "Try again" (`haptics.tap()` → `refetch()`) |
| Previous chats | `LegacyChatsSection` after the rows, `mt-2`, inside the scroll content. In a `px-2` column; its header and chat rows are `px-3 py-2`, so they start on the 20pt line (§2). Renders only when the account has pre-OpenCode chats |
| Session list top fade | A 24pt `expo-linear-gradient` of the drawer surface (`THEME.*.chromeBackground`, alpha 1 → 0) over the top of the list, `pointerEvents="none"`, so rows fade out under the nav pills instead of a hard edge. Its opacity follows the scroll offset (Reanimated, UI thread): 0 at rest, 1 after 24pt of scroll, so the first row is never dimmed |
| Bottom bar | Absolute at the bottom, full width, `pointerEvents="box-none"`. Background: an `expo-linear-gradient` fade of the drawer surface (`THEME.*.chromeBackground`) — alpha 0 → 0.85 → 1 at locations 0 / 0.45 / 1, `pointerEvents="none"` — from 36pt above the controls to the screen edge (the same fade as the floating tab bar). No solid fill. Controls: a `flex-row justify-between px-5` row — **avatar left, New session pill right** (Jay, 2026-09-23), each on the 20pt line. The avatar is `PlanRingAvatar` (`components/settings/PlanRingAvatar.tsx`), 44pt: a 2pt diagonal gradient ring coloured by the active plan family (`planTier`, `lib/billing/plan-tier.ts`, tested): the API's plan label is the family — Free, Team, Enterprise (`PLAN_FAMILY_LABELS`); an older API's tier names map to their family, and every grandfathered paid tier, "Enterprise (Legacy)" included, is Team. Then a 2pt gap of the drawer surface, a 36pt photo. Paid rings are metals (Jay, 2026-09-23), five stops banded light · shadow · highlight · shadow · light at 0 / 0.3 / 0.5 / 0.72 / 1: Free muted grey 60% → 25% (no banding); each metal has a dark and a light set, the light set darker so no band vanishes into the 95.7% light drawer. Team **silver** (achromatic; dark L 92 · 58 · 97 · 50 · 80, light L 78 · 46 · 90 · 38 · 66); Enterprise **champagne gold** (Jay rejected the saturated yellow → orange gold as brassy; one hue, the Kortix yellow's 48; dark s 55 · 45 · 60 · 45 · 50 / L 82 · 52 · 92 · 44 · 72, light s 50 · 45 · 55 · 45 · 48 / L 66 · 38 · 82 · 32 · 56). No known plan: the 44pt photo alone. Label "Account, {plan} plan" whose bottom is `insets.bottom + 16` (16pt above the home indicator). The list's bottom padding is that offset + 44 + 16, so its last row rests above the controls and earlier rows fade out under them |
| New session | `Button size="lg" className="rounded-full"` — the default (primary) variant, 44pt, `NavigationArrowIcon` flipped horizontally (`scaleX: -1`), tip up-right (web's New session glyph, `size={20}`) + "New session", content width. `haptics.tap()` → `onClose()` → `onNewSession()` (ProjectScreen `returnHome`: reset the store and pop a covering route, so project home's composer starts the session) |
| Avatar | `ProfilePicture size={11}` (44pt, the `lg` pill height) from `useProfileEditor()` — the Account page's photo and name; initial when there is no photo. `Pressable rounded-full active:opacity-70`, 2pt hit slop, label "Account". No name or email. → `/projects/[id]/account` through `navigateProjectRoute` (push over home, or replace a covering route) |
| Haptics | `haptics.tap()` on every button and pill. Session rows: one `haptics.tap()`, fired by ProjectScreen (`handleOpenProjectSession`, or the close-only path for the open session's row); the drawer adds none |

### Switcher sheet (COR-124/COR-157 Task 4)

`components/projects/ProjectSwitcherSheet.tsx` — one project/account switcher,
opened from the drawer's switcher row above and from the Projects tab's
account control (below). Both call sites mount it closed and drive it with
`open`. **A sheet driven by `open` never calls `dismiss()` before it has
presented** (`sheetOpenMove`, `lib/ui/sheet-open.ts`; the device bug of
2026-09-23): gorhom 5.2.x's `dismiss()` on a never-presented modal leaves its
status at DISMISSING, and the next `present()` mounts a portal that refuses to
render — the sheet never appears, and later taps change nothing. The sheet
tracks whether it is on screen (set on present, cleared in `onDismiss`) and
dismisses only then. `NewAccountSheet` and `NewProjectSheet` follow the same
rule. `KortixBottomSheetModal` at **full height** (`snapPoints={['100%']}`, Jay,
2026-09-23), `enableDynamicSizing={false}`. Title row: close X at the far left,
**the picked account's name as the title** (Jay, 2026-09-23; "Projects" until
it resolves), and `titleTrailing` a `+` icon `Button` → `NewProjectSheet` preset
to the picked account. **The tab list sits at the bottom** in a `PinnedBar`, the
project drawer's bottom bar (content scrolls under a fade of the sheet colour,
`useSheetBackground`): **the root tab bar's own capsule** (Jay, 2026-09-23),
`FloatingTabCapsule` from `components/navigation/FloatingTabBar.tsx` — the same
60pt capsule, sliding `bg-secondary` thumb, 20pt icon over 12px label, light
`bg-background` + `LIGHT_SHADOW` / dark `bg-card` + border — with the same two
items: Account (`UserIcon`) on the left · Projects (`FolderIcon`) on the right
(Jay, 2026-09-23), centred. `FloatingTabBar` draws the same component. The list pads its end with
`usePinnedBarInset(FLOATING_BAR_HEIGHT)`. The sheet opens on the Projects tab.

| Element | Value |
| --- | --- |
| Account tab | One `SettingsGroup` of account rows: 28pt `Avatar variant="custom"` leading, the account name, `checked` on the picked account (`right={null}`), then a separate one-row group "New account" (`PlusIcon`) → the existing `NewAccountSheet`. Always shown, even with one account. Tapping an account picks it (local state, no store write, no navigation), fires `onAccountSelect` when the caller passed one (the Projects tab does, so its own list follows; the drawer omits it — there the account is written only when a project is picked), and switches to the Projects tab |
| Search | `SheetTextInput` "Search projects", shown only when the picked account has more than 6 projects (`SWITCHER_SEARCH_THRESHOLD`, `lib/projects/switcher.ts`). Case-insensitive, trimmed, matches the project name |
| Project rows (Projects tab) | One untitled `SettingsGroup` (the header already names the account): 28pt tile leading (first letter; the current project's tile `bg-foreground`/`text-background`, others `bg-secondary`/`text-muted-foreground`, `rounded-lg` — the same inverted-fill pattern as `AccountPage`'s project row, §1), name as the label, `checked` on the current project, `right={null}` (no chevron otherwise). Tapping the current project (`checked`) only dismisses the sheet — never a `router.replace` to where it already is |
| Loading / empty / error | `KortixLoader size="small"` while the picked account is still resolving (`chipAccountId == null`, e.g. the sheet opened before `selectedAccountId` arrived) or its projects are loading — never the empty state then, so a user who has projects is never shown "No projects yet"; empty account: `Text variant="muted"` "No projects yet" + `Button size="lg" rounded-full` "Create project"; fetch error: "Couldn't load projects" + `Button variant="secondary" size="sm"` "Try again" |
| Picking a project | Sets `useCurrentAccountStore`'s selected account to the project's account, fires `onProjectOpen` (the drawer closes with it; the Projects tab has none), dismisses this sheet, `router.replace` to the project — the same navigation the Projects tab's own row tap uses |
| New account / New project | Both are a real second `BottomSheetModal`, so this sheet dismisses itself first (`go`: dismiss, 160ms, then present the other) — a `dismiss()` from `go` must not read as the whole switcher closing, so `handleDismiss` swallows one dismiss per `go` call. Cancelling either child (X or pan down) presents this sheet again, so the user lands where they were and the parent's `open` matches the screen (`handleChildClosed`; a child that created something sets `childFinishedRef`, so its late `onDismiss` does nothing). New account returns to this sheet, on the fresh account's (empty) project list, its chip selected. New project finishes the flow exactly like a project row tap |
| Pure helpers | `lib/projects/switcher.ts` (bun-tested): `switcherChips` (accounts in server order + a trailing "New account" sentinel), `filterProjectsByQuery`, `shouldShowProjectSearch`, `projectHref` (the typed-router `Href` for a project id — expo-router's typed routes need the dynamic segment as `params`, not interpolated into a path string) |

### Projects tab account control (R1 regression fix)

`app/(tabs)/projects.tsx`'s header lost its account switcher trigger at some
point before this task, leaving `accountSheetOpen` state with nothing to set
it. Restored (Jay, 2026-09-23): next to the logomark, an account name +
14pt `CaretUpDownIcon` `Pressable` (`Text variant="small"`). **Always
enabled**, even with one account — the switcher's chip row always ends in
"New account", so there is always somewhere to go. Opens the same
`ProjectSwitcherSheet` as the drawer, with `onAccountSelect={setSelectedAccountId}`
so the tab's own project list switches the moment a chip is tapped (this was
the R1 regression: a chip tap never wrote `useCurrentAccountStore`, so the
list stayed on the old account) and no `onProjectOpen` (the tab has no outer
chrome to close); picking a project runs the sheet's own `router.replace`.

Intentionally absent — do not add back:

- The old plain "Sessions" nav row: Search opens the same page, already focused.
- The user card footer (name, email, plan, update dot). The avatar is the only account entry.
- A New session row at the top. New session lives only in the bottom bar.
- The sandbox-scoped Projects tree, and any collapsible section header or chevron over Sessions.
- Row descriptions or timestamps on the nav rows (Review's count pill is the one exception, and only while items wait).

## 7. Project sessions page

`/projects/[id]/sessions` (`components/session/ProjectSessionsPage.tsx`),
opened by the sidebar's Sessions row (pushed over project home, or replacing
the covering route). It lists every
session of the project. Title, status, grouping, search, and relative time
come from `lib/session/session-list.ts` (unit-tested).

| Element | Value |
| --- | --- |
| Header | `SettingsHeader title="Sessions" largeTitle gutter="project" onOpenMenu`: the hamburger (opens the project drawer) alone in the control row, no Go back; the title "Sessions" below it as `Text variant="h3"` (`accessibilityRole="header"`) in an `h-10 px-4 pb-1` row, above the search field |
| Search | `SearchListHeader` (16pt `px-4`, its project default), placeholder and label "Search sessions". Shown only after loading and only when the project has at least one session; when the list becomes empty the query is cleared. Trimmed, case-insensitive match on the display title (`filterSessionsByTitle`). Clear button label "Clear search". `autoFocusSearch` (COR-124/COR-157 Task 4) — the `?autoFocusSearch=1` param the drawer's Search row navigates with — auto-focuses this field (`inputProps={{ autoFocus }}`); a session row's own opening of this page, or a deep link, arrives without it |
| Groups | Today · Yesterday · This week (2 to 7 local calendar days before today) · Older, by `sessionLastActivityAt`, newest first (`groupSessionsByActivity`). Empty groups are omitted. Headers show only when more than one group has sessions (`showHeaders`): `Text variant="muted"`, `mb-2 px-4`, role header, 18pt above every header but the first |
| Data | `useProjectSessionsPaged` — the drawer's paged list (50 rows a page). `onEndReached` at 0.6 loads the next page, with a `KortixLoader size="small"` footer while it loads. Search and groups work on the rows loaded so far; a search with few matches leaves the list short, so it reaches its end and keeps loading pages until the matches fill the screen or the list ends. `useProjectSessions` (one page, flat) stays for lookups only |
| List | `FlatList`, **one item per group** (Jay, 2026-09-22): a `SettingsGroup` of `SettingsRow`s, the settings screens' layout, so the page follows `SettingsGroup`'s look (row tiles, 2pt gaps) with no copy of it. The group's `title` shows only when more than one group has sessions; 18pt between groups (the settings screens' gap). No indicator, `paddingHorizontal: 16` (the project edge), top padding 4, bottom padding `usePinnedBarInset(44)` so the last row rests above the New session button, keyboard dismisses on drag. A group is not virtualised row by row; pages stay 50 rows |
| Row surface | `SettingsGroup`'s. Never a hand-rolled `bg-card` row with `rounded-t-2xl` / `Separator` (the first build: it missed the 2026-09-22 tile redesign) |
| Row | `SettingsRow`: `leading` = `SessionStatusMark` · `label` = title (`sessionDisplayTitle`, one line) · `value` = time (`shortRelative`: now, 5m, 2h, 3d, 2mo, 1y) · `right={null}` (no chevron: the time holds the right edge). `onLongPress` + `longPressLabel="Session actions"` + `accessibilityLabel` / `accessibilityHint` are `SettingsRow` props added for this row (a screen reader gets the long press as a named action). The clock re-renders every 60s and on each refetch. No description |
| Sub-agent sessions (COR-162) | Within each activity-day group, `groupSessionsByCoordinator` (`lib/session/session-list.ts`) nests a sub-agent session (the §5 "Sub-agent" relation) whose coordinator is IN THAT SAME GROUP as a sub-agent row right after its coordinator, same composition as the drawer (§6) and web (sort/bucket first, coordinator-group second). A nested row's `leading` composes a muted 12pt `ArrowElbowDownRightIcon` beside the `SessionStatusMark`, indenting the label past the usual leading slot — the row tile itself stays full width, one `SettingsRow` per session, never a shared or fused tile. Accessibility label adds ", sub-agent session". Search still finds a sub-agent row (it filters the flat list before grouping); if its coordinator doesn't match the search or a different day bucket, the child renders top-level rather than disappearing |
| New session | **Pinned at the bottom right, the project drawer's button** (Jay, 2026-09-22): `PinnedBar controlHeight={44} className="justify-end px-5"` over a fade of the page background; `Button size="lg" rounded-full`, `NavigationArrowIcon` 20pt flipped (`scaleX: -1`) + "New session". `haptics.tap()` → `newSession()` from `useProjectRoute` (ProjectScreen's `returnHome`: reset the store, pop this covering route), so project home's composer starts the session. The list scrolls under it. Hidden while the page loads |
| Tap | Opens the session once per page (`useCoveringRoute`'s `leavingRef`). The view route replaces this page (`StackActions.replace`), so the stack stays one screen over project home. The page fires no haptic; ProjectScreen's `handleOpenProjectSession` fires one `haptics.tap()`. If the store opens a session without a tap (notification, deep link), the page replaces itself with the view the same way |
| Long press | `haptics.medium()`, then `SessionActionsSheet` (`components/session/SessionActionsSheet.tsx`, COR-140 Task 5) — one sheet shared with the thread's `···` and the drawer's session-row long press, opened through `openSessionActions` on `ProjectRouteValue` (mounted once by `ProjectScreen`, alongside `PageContextMenuSheet`). `KortixBottomSheetModal` directly, never the `Sheet` wrapper. `SheetTitleRow title={session title} hideClose`: **no X button**, the title centred; a swipe down or a tap outside closes it. `enableDynamicSizing` + `snapPoints={['100%']}` + `topInset`: it opens at its content height and **drags up to full height**. **Board 11b layout** (Jay, 2026-09-23): one untitled `SettingsGroup` on the 16pt edge (`px-4`, the title row's inset) — Rename (`Pencil`) · Share (`Share`, only when `can_manage_sharing !== false`) · Restart sandbox (`RotateCcw`) · Stop (`Square`, only while running) — then, 18pt below (the settings screens' group gap), a second untitled `SettingsGroup` holding **Delete session alone**, destructive (`Trash2`, `haptics.warning()`). Restart sandbox, Stop, and Delete session need `can_manage_lifecycle !== false`; the Delete group renders nothing without it. Chevron rule: Rename and Share keep the chevron; Restart sandbox, Stop, and Delete session pass `right={null}` |
| Rename and Share | **Both push inside the sheet — one sheet for the whole flow** (Jay, 2026-09-22). Share was a second sheet that opened after the first had closed, with an X and no way back; it now follows Rename. The sheet's body is one `BottomSheetScrollView` (dynamic sizing needs a single scrollable child, and Share's member list can be taller than the screen). **Rename snaps the sheet to full height** (`snapToPosition('100%')` on push; Jay, 2026-09-22): the field sits at the top, clear of the keyboard, and the sheet does not resize as the keyboard moves; Back snaps to the content height again (`snapToIndex(0)`). Share stays at its content height. Rename: pushed inside the sheet (`components/kortix/sheet-push.tsx`, the app's in-sheet push): the actions swap for `SessionRenameForm` with `PUSH_IN`; the title row becomes "Rename session" with `SheetBackButton` in the leading slot, and **Back returns to the actions** (`POP_IN`, only after a push). No second sheet, no close-then-open. The form (`components/session/SessionRenameForm.tsx`): `SheetTextInput` — the one text field for sheets; never a hand-styled `BottomSheetTextInput` with a border (the old sheet: icon tile, description, bordered field, `ActivityIndicator`, all removed) — auto-focused, max 120, placeholder = the automatic title, then primary `Button size="lg" rounded-full` "Save" ("Saving…" while it runs). An unchanged name closes the sheet; an empty name reverts to the automatic title. Failure: `haptics.warning()` + `toast.error` "Unable to rename the session. Try again." |
| Sheet follow-ups | Only Delete follows the sheet: its `AlertDialog` opens after `SessionActionsSheet` has dismissed — never two overlays. Rename and Share are views of the sheet itself. Restart sandbox and Stop close the sheet and run at once: `toast.success` "Session restarting" / "Session stopped", or `toast.error` "Unable to restart the session. Try again." / "Unable to stop the session. Try again.". A repeat of the same action on the same session is ignored while one runs. Share failures are `toast.error` |
| Share | `SessionShareForm` (`components/session/SessionShareForm.tsx`), pushed into `SessionActionsSheet`: title row "Share session" with `SheetBackButton` (Back returns to the actions; no X). 16pt sides (`px-4`, the title row's inset), 16pt between blocks. One `SettingsGroup` of picker rows (icon · label · check, `right={null}`): Only you (`LockIcon`) · Whole project (`GlobeIcon`) · Specific people (`UsersIcon`) — web's `SESSION_SHARING_COPY` labels, no descriptions, no header icon tile. Specific people adds a "People" group (`Text variant="muted"`, `mb-2 px-4`) of `Avatar variant="custom" size={28}` · email (" (you)" on the viewer) · check; a tap toggles with `haptics.selection()`. Members shared with at open sort first and the order then stays fixed, so a row never moves under the finger. Loading: `KortixLoader size="small"`; none: `Text variant="muted"` "No other members yet". Primary `Button size="lg" rounded-full` "Save" ("Saving…" while it runs), disabled while Specific people has no pick — no warning line. The form mounts when pushed: it seeds from the session once, pins the session id, and fetches members only while it shows. Save closes the sheet; failure: `haptics.warning()` + `toast.error` "Unable to update sharing. Try again." |
| Delete dialog | `AlertDialog`, title "Delete session", description `Delete “{title}”? Its sandbox is destroyed. This cannot be undone.` Footer: `AlertDialogCancel asChild` secondary `size="lg"` pill "Cancel" · destructive `size="lg"` pill "Delete session" ("Deleting…" while it runs). Both disabled while the delete runs, and the dialog stays open until it settles. Failure: `haptics.warning()`, the description becomes "Unable to delete. Check your connection and try again." in `text-destructive`, dialog stays open. Success: the session's tab closes, `haptics.success()`, `toast.success` "Session deleted", dialog closes. The title stays in the description while the dialog animates closed |
| Loading | `KortixLoader` centred in the page, no search field |
| Empty / no matches / error | Centred `Text variant="muted"` (`px-8`, `text-center`): "No sessions yet"; "No matching sessions" when the search hides every row; "Unable to load sessions. Pull to refresh." when the load failed and nothing is cached. Loading/error/empty come from the same `sessionListState` helper the drawer uses (§6, COR-146) — a failed background poll or pull-refresh with rows already loaded never shows the error copy, only a fresh load with nothing cached does |
| Pull to refresh | `RefreshControl` (tint `mutedForeground`) refetches the project's sessions |
| Accessibility | Row label "{title}, {status}, {spoken time}", e.g. "Fix login, Running, 5 minutes ago" (`spokenRelative`: "5m" would read as "5 meters"). Hint "Opens the session". Actions: activate, and longpress labelled "Session actions" (opens the sheet). The mark is hidden from screen readers |

Status marks (`components/session/SessionStatusMark.tsx`), shared with the
project sidebar. Each state differs in shape, not colour alone:

| Display status | Source statuses | Mark |
| --- | --- | --- |
| `running` | running | Filled dot, `size-2.5`, `bg-kortix-green` |
| `stopped` | stopped, completed | Hollow ring, `size-2.5`, `border-[1.5px] border-muted-foreground` |
| `starting` | queued, branching, provisioning | Ring `size-2.5` `border-[1.5px] border-kortix-yellow` with a `size-1` `bg-kortix-yellow` centre dot, opacity pulse 1 → 0.35 (800ms, ease-in-out, repeats). Static under reduced motion |
| `failed` | failed | Diamond: `size-2 rotate-45 rounded-[1px] bg-destructive` |
| `needs-you` | pending review (not reachable on mobile yet) | `size-2` `bg-kortix-blue` dot inside a `size-4` `bg-kortix-blue/20` halo |

## 8. Session transcript: the show card

A `show` / `show-user` output is **one row per file, never an embedded viewer**
(COR-107; Jay, 2026-09-22 — option B of the prototype). The transcript used to
hold a 420pt page or a scroller, which fought the transcript for the same
gesture and showed a clipped half of the payload.

| Property | Value |
| --- | --- |
| Row | **The app's own list row** (Jay, 2026-09-22): one `SettingsGroup` of `SettingsRow`s, not a bespoke card — leading slot · label (the file name) · `description` (the kind) · the chevron `onPress` brings. `ShowResultRow` (`tool/shared/show-helpers.tsx`) is that row. One per carousel item; an output with neither a path nor a URL keeps the inline renderer, since there is nothing to open |
| Leading | The image itself for an image — **never an SVG** (Jay, 2026-09-22: a vector in a 22pt slot is noise) — 22pt squared (radius 6) in the row's icon slot — the sandbox read happens only then (`useSandboxImage`); a direct image URL loads straight. **Every other kind is its type glyph** in the same slot. Rendering a page or a document to a still is work a transcript should not do (`showRowModel`, tested) |
| SVG | **Never previewed anywhere on mobile** (Jay, 2026-09-22; `lib/files/svg-policy.ts` `isSvgName`, tested): no still in a row, no drawing in the file sheet. An SVG reads as its markup (`getFilePreviewType` → `TEXT`), so the sheet's title-row **Copy** puts the markup on the clipboard and **Download** hands the real file to the device. The `SvgXml` renderer that briefly existed is gone |
| Label | `showRowModel`: the file name, else the title, else the domain. Second line: the kind — Page, Image, PDF, Slides, Spreadsheet, Document, Markdown, Code, Text — or the domain for a link |
| Tap | `useShowOpenInTab`: an HTML file or a localhost URL opens the Browser tab, a safe external URL the browser, anything else the file sheet (§ File preview). No Refresh, no Full screen, no second Preview control — the sheet owns those |
| Streaming | While the input streams, the header's loader row stays (`bodyKind: 'loading'`) |
| Failed | A dead reference keeps its quiet line — "Preview unavailable — {label}", plus Open link for a safe URL — never a blank row |
| Running app | A localhost preview is the SAME row — and so is the card under a MESSAGE (`SandboxPreviewCard`, rendered by `text-part.tsx` for every `localhost:PORT` an assistant message mentions; Jay, 2026-09-22). The card under a message sits `pt-3` (12pt) below the text it follows — the row has no margin of its own (Jay, 2026-09-22). One component draws all three: `ResultRow` (`tool/shared/result-row.tsx`; its tile and glyph sizes are Jay's, 2026-09-22 — do not change them) (`ServicePreviewRow`, `tool/shared/navigation.tsx`; Jay, 2026-09-22): `MonitorIcon` on the tile — **never the globe** — "App preview" (or the given title), `localhost:3000` under it, chevron; the whole row opens the Browser tab. It replaced a five-part block: a mono URL strip with a globe, a grey viewport box, an outline button repeating the label, an external-link icon button and an Open button. The token-bearing proxy URL is never shown |
| Panel / detail | The activity sheet's detail body and the (desktop) panel keep the full renderer: `ShowCarousel`, `ShowContentRenderer`, the localhost viewport |
| File preview | **One sheet for every file open** (`components/files/FilePreviewSheet`; Jay, 2026-09-22): Recent files' sheet, shared by a `show` row, a read row, a skill document and a generated image. `KortixBottomSheetModal` at `['100%']`, page-coloured surface, X at the far left, the file name centred, Copy at the far right (text files). Body: `FilePreview`; a PDF, Office file, archive or media (`previewsInline`) shows its file card and fetches nothing. Bottom: `PinnedBar` — Download, plus "Add to chat" only where a composer can take the file. **`FileViewer`, the full-screen modal, is never opened from a tool row again** — it remains on the Files page, the project detail page and the composer's mention tap, which need its editor |
| SVG | Its own preview type (`FilePreviewType.SVG`; Jay, 2026-09-22): it loads as TEXT, so the sheet's title-row **Copy** copies the markup, and it draws through `react-native-svg`'s `SvgXml` (already a dependency — no new native build), centred, pinch to 4×. Markup `SvgXml` cannot parse falls back to the text itself, never a blank box |

## 9. Colour and theme

- Every screen follows the resolved colour scheme (NativeWind `useColorScheme`).
  No screen is "always dark".
- Colours come from tokens (`bg-card`, `text-muted-foreground`, `THEME.*`).
  See `CLAUDE.md` → Color for the hex/rgba bans and `withAlpha`.
- Skia and other native renderers cannot parse `hsl(0 0% 100%)`; pass
  `withAlpha(token, 1)` (comma form) to them.

## 10. Checklist before handing off a screen

- [ ] Settings screen uses `SettingsPage` / `SettingsGroup` / `SettingsRow`
- [ ] No uppercase or letter-spaced group titles
- [ ] No row descriptions
- [ ] Chevrons `strokeWidth={2.75}`, full-width separators, borderless `rounded-2xl` cards
- [ ] Buttons follow section 2 (no sizing classes)
- [ ] Checked in light and dark mode
- [ ] `npx tsc --noEmit -p .` shows no new errors in the files you touched

## 11. Toasts

`useToast()` (`components/kortix/toast-provider.tsx`) is the only toast. **`sonner-native` renders them** (https://sonner-native.netlify.app; Jay, 2026-09-22): the library owns the stack, the swipe, the timers, the layout and the motion, so the app does not reimplement any of it. Web's toast (`apps/web/src/components/ui/toast.tsx`, Sonner) stays the behaviour spec. The app owns three things only: the API seam (`toast-provider.tsx`), the look (`toast.tsx`), and the rules in `lib/ui/toast-model.ts` (durations, haptics, message helpers — pure and tested). No screen imports `sonner-native` directly.

| Property | Value |
| --- | --- |
| API | `toast.success / error / info / warning / loading(message, { description?, duration?, id?, action? })` → the toast's id. `toast.promise(promiseOrFn, { loading, success, error? })` — a loading toast that turns into the success or error toast **in place**, returns the promise; no `error` = no error toast (web's `loadingToast`). `toast.dismiss(id?)` — one, or all |
| When | Success of an action that leaves the page in place → `success`; a failure outside a dialog → `error`. Never `Alert.alert`. A failure inside a dialog stays in the dialog |
| Position | `position="top-center"`, `gap={8}` between toasts, `offset={insets.top + 8}` — the library measures `offset` from the SCREEN edge, not the safe area, so the inset is added here or the toast sits under the status bar / notch (Jay, 2026-09-22) |
| Card | The app's floating-surface rule, the floating tab bar's (§6; Jay, 2026-09-22 — the bordered light card was rejected): light `background` lifted by `LIGHT_SHADOW` (`0px 6px 24px` foreground at 0.12, the one token in `FloatingTabBar`), **no border**; dark `card` + hairline `border`, no shadow (a shadow is invisible on black). Radius **12** (`rounded-xl`: `--radius` is 10, so `lg` = 10 and `xl` = 12; anything larger read as a pill — Jay, 2026-09-22), `paddingHorizontal: 16`, `paddingVertical: 12`, 12pt between icon and text. The row is **top-aligned** (`alignItems: 'flex-start'`); the icon and the close button carry a 1pt top margin so the glyph sits on the FIRST text line — a description or an action used to drag it to the middle of the card (Jay, 2026-09-22). Styles are plain objects from `THEME`, never a colour literal: the library renders its own `View`/`Text`, which NativeWind never patches (same reason as `SheetTextInput`) |
| Icon | 20pt, **one family: every type is `weight="fill"`** (Jay, 2026-09-22 — mixed fill and outline read as two icon sets), in its brand tone: success `CheckCircleIcon` green · error `XCircleIcon` red · info `InfoIcon` blue (not the warning circle web uses — it read as a warning) · warning `WarningIcon` yellow · loading `KortixLoader size="small"` (never a spun icon), passed through `<Toaster icons={…}>` |
| Text | Message Roobert 16/22 in `foreground`; description Roobert 14/19 in `mutedForeground` |
| Action | `action: { label, onPress }` → the library's action button in the app's tokens: `secondary` fill, radius 10 (`rounded-lg`, one step inside the card), 14pt Roobert Medium label |
| Close | **Only on a toast that never leaves on its own** — loading, or `duration: Infinity` (`toastKeepsCloseButton`, tested; Jay, 2026-09-22). A timed toast is dismissed by its countdown or a swipe, so the X is noise. When shown it is the library's close button, **transparent — no fill, no border** (Jay, 2026-09-22: a filled one read as a second button beside the action), the X in the library's secondary text colour, 10pt hit slop, top-aligned with the icon |
| Duration | 4 s (`TOAST_DEFAULT_DURATION_MS`, Sonner's default); loading: until updated or dismissed; `duration` overrides. `Infinity` becomes the library's largest finite duration, since it takes no `Infinity`. `pauseWhenPageIsHidden` stops the countdown while the app is backgrounded |
| Stack | `visibleToasts={3}`, newest in front; a fourth pushes the oldest out. A tap expands the stack (the library's own behaviour) |
| Swipe | `swipeToDismissDirection="up"` |
| Haptics | success → success, error → error, warning → warning; info and loading are silent (`toastHaptic`, `lib/haptics.ts`, gated by the Sounds setting). An update in place (`{ id }`) does not buzz again |

## 12. Tool permission requests

A pending permission (opencode's `permission.asked` — bash, edit, write, read,
webfetch, mcp, a doom-loop repeat) is now pinned above the composer, not left
to a scroll position under the blocked tool row (COR-137 Task 7; Paper board
20). Missing it left the agent waiting forever.

| Property | Value |
| --- | --- |
| Where | `PermissionPromptCard` (`components/session/PermissionPromptCard.tsx`) renders in the composer's top slot (`SessionPage`'s `inputSlot`) — the same slot the queue panel uses, above the queue when both are pending |
| Which one | `pinnedPermission` (`lib/session/permission-prompt.ts`) — the oldest pending permission, same rule as the question prompt's `pendingQuestions[0]` |
| Card | `rounded-xl border border-kortix-orange/25 bg-background p-3 gap-2` (the tinted-border alert web's card uses, `session-permission-prompt.tsx`): a `text-kortix-orange` "Permission needed" label with `ShieldWarningIcon`, a title line (`permissionPromptTitle` — a question-style line per permission type, e.g. "Run a command in the sandbox?" for bash), the concrete command/pattern in a mono `bg-secondary` block (`permissionPromptDetail` — the request's `patterns`, falling back to a metadata title), two `size="lg"` pills Deny (`secondary`) · Allow once (`default`), then a quiet `ghost` `size="sm"` text button "Always allow" |
| Reply | Reuses `SessionPage`'s existing `handlePermissionReply` (`replyToPermission` + `removePermission` on success; `toast.error` on failure, prompt stays for a retry) — the same handler the inline row under the tool call already used |
| Inline row | `PermissionPromptInline` (`components/session/tool/tool-part-renderer.tsx`) keeps only a quiet "Waiting for your permission" line under the blocked tool call — its Deny / Allow always / Allow once controls moved to the pinned card, so a permission is never actionable from two places at once |
| Reconnect / reopen | `GET {sandboxUrl}/permission` is read once when a session opens (`SessionPage`'s self-heal effect) and once after an SSE reconnect gap (`hydratePermissionsAfterGap`, `lib/opencode/event-stream.ts`), mirroring the existing `/question` self-heal — a `permission.asked` frame sent while the app or the stream was down is not lost |

## 13. Connector hand-off in chat

When the agent's `kortix-connectors_call` tool hits an unconnected app or a
call awaiting approval, the transcript itself carries the remedy — no
markdown link interception (web's mechanism: `SetupLinkButton` intercepts a
`/connect/<token>` link the agent pastes into prose); mobile reads the same
fact straight off the tool part instead (COR-158, connector remainder — the
model-provider half is **5. Project home**'s Connect provider sheet).

| Property | Value |
| --- | --- |
| Detection | `lib/session/connector-handoff.ts` (`bun test`), read by `ConnectorCallTool` (`components/session/tool/tools/connector-tools.tsx`): `connectorConnectNeed` — a `status: 'denied'` payload carrying `connect_url` (the API's `connector_not_connected` remedy, `apps/api/src/connectors/principal-access.ts`); `connectorApprovalNeed` — `status: 'pending_approval'` with an `execution_id`. A call is never both |
| Standalone row | A call either need matches (`connectorHandoffCallIds`, any `kortix-connectors_call` spelling the tool registry resolves) is a **standalone transcript row**, never part of a burst: `standaloneCallIdsFor(permissions, sessionId, parts)` (`lib/session/turn-body.ts`, tested) adds its call id, as it does for a pending permission. Inside a burst the row would render in the activity sheet — outside `ConnectorHandoffContext`, and a Connect there would stack the auth sheet on the activity sheet. Guard: `ActivitySheetHost` also gets the context; a Connect tapped inside the activity sheet dismisses it first and opens `ConnectorAuthSheet` from its `onDismiss`. Never two overlays |
| Connect row | `ConnectorConnectRow` (`components/session/tool/tools/connector-connect-row.tsx`), mounted under the tool card when `connectorConnectNeed` fires. `ResultRow`-styled (borrows `RESULT_ROW_TILE`, never changes it): a 40pt tile (the connector's `iconUrl` from the live `useConnectors` query, else `PlugIcon`), the app name, a muted "Needs connecting" / success-tinted "Connected" line (read live from `useConnectors`, not the one-time denial), and a `Button size="sm"` "Connect" when not yet connected |
| Connect sheet | `ConnectorAuthSheet` (`components/session/ConnectorAuthSheet.tsx`) — a sibling of `ConnectProviderSheet`, not a generalization of it (different async shape per attempt vs. one static URL), sharing its visual shell and its `useHandoffDismiss` close-then-open primitive (`components/session/handoff-sheet.ts`). One instance lives in `SessionPage`, reached by every `ConnectorConnectRow` through `ConnectorHandoffContext` (`components/session/tool/shared/connector-handoff-context.ts`) — never two overlays. Title "Connect {Provider}", body "Sign in on kortix.com. You come back to this chat when it's done.", Continue ↗ / Not now, same two-56pt-tile layout as `ConnectProviderSheet` (app logo or `PlugIcon` left, Kortix symbol right) |
| Continue | Tries the project's own Pipedream connect first (`pipedreamConnect` + `WebBrowser.openAuthSessionAsync` to a `kortix://` return, `pipedreamFinalize` to confirm — the same round trip `ConnectorsPage.tsx` uses); falls back to the tool's own `connect_url` (the public `/connect/<token>` page, no redirect support) via `openBrowserAsync` only when the project-scoped connect can't start. Either way, `useConnectors`' query is invalidated and a `{Provider} connected` / `{Provider} not connected` toast follows (`connectorHandoffToast`) |
| Pending approval | `ConnectorApprovalPrompt` (local to `connector-tools.tsx`), inline under the tool row rather than pinned like `PermissionPromptCard` (**12**, above) — a connector approval has no session-wide pending list the way permissions do. **Pending is live** (`connectorApprovalState`, tested): the project review list (`useReviewItems`, `reviewKeys.list` — the query `ReviewPage` reads; the API lists a connector call there as `call:<execution_id>` only while it waits; polled every 8 s while the answer can still change). In the list → Approve/Deny. Gone from a list fetched after the call settled → "Resolved" (decided on web, on the Review page, or before a remount). A list fetched before the call settled proves nothing → still pending. Not loaded yet → nothing renders (no Approve/Deny flash for a decided call). List request failed → pending from the tool output. A decision made here reads "Approved"/"Denied". The card names what it approves: the `connector.action` ref in mono, then up to 4 call arguments (`key` muted · value, each cut to 80 characters), then the summary and instructions. Same tinted-border shape as the permission card (orange border, `ShieldWarningIcon`, Deny/Approve pills), forces the row `defaultOpen`. The decision reuses `resolveApproval` from `@kortix/sdk` — the same call `ReviewPage`'s `useReviewVerdict` makes — and invalidates `reviewKeys.list`, also after a failed attempt (the call may have been decided elsewhere). The tool row's own "Needs approval" trigger label is the call's output and does not change |

## 14. Question prompt

The agent's question (opencode `question.asked`) replaces the chat input while
it is pending (`QuestionPrompt`, `components/session/QuestionPrompt.tsx`;
Jay, 2026-09-23 — the old compact chip card with header icon, tab pills,
radio dots, and pencil row read as sloppy).

| Property | Value |
| --- | --- |
| Card | The `Composer` card exactly: `rounded-3xl border border-border bg-background p-2` inside `px-4 pb-3 pt-1` |
| Question | Roobert Medium 16/24; multi-choice adds "Select all that apply" in 14pt `muted-foreground` |
| Options | Plain rows, `px-3 py-2.5`, radius 16, label 16pt, description 14pt muted (≤ 2 lines). Picked or pressed = `secondary` fill. No radio or checkbox glyphs; only a picked multi-choice row shows a 16pt check. List scrolls past 260pt |
| Own answer | The composer's text field (16pt Roobert, no border), placeholder "Or type your own answer" (or "Type your answer" when there are no options). Hidden when `custom: false` |
| Control row | Composer row: `secondary` `sm` pill "Skip" (rejects the request) · ghost "Back" from step 2 · flex · "2 of 3" muted · round send (`icon-md`, fills `primary` when there is an answer) |
| Flow | One question per step. A single-choice tap answers and moves on; multi-choice toggles and send moves on; typed text wins over a single-choice pick. The last step sends all answers. Rules and tests: `lib/session/question-prompt.ts` |
