import { AccountPage } from '@/components/settings/AccountPage';

/** The Account tab: the app's one settings page (components/settings/AccountPage). */
export default function AccountTab() {
  return <AccountPage presentation="tab" />;
}
