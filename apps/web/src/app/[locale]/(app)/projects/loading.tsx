/**
 * Navigation boundary for `/projects`. It gives Next.js a prefetch target for
 * this dynamic route. Blank on purpose: the selector paints its own skeleton
 * under the same header one frame later.
 */
export default function ProjectsLoading() {
  return null;
}
