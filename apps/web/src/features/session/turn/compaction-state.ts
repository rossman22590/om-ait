/**
 * Moved to `@kortix/sdk` (`packages/sdk/src/core/turns/segments/compaction-state.ts`)
 * so web and mobile render the transcript from one implementation. This file
 * keeps the old import path working for existing web call sites.
 */
export { compactionTurnInfo } from '@kortix/sdk';
export type { CompactionTurnInfo } from '@kortix/sdk';
