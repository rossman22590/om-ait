/**
 * Moved to `@kortix/sdk` (`packages/sdk/src/core/turns/segments/step-label.ts`)
 * so web and mobile render the transcript from one implementation. This file
 * keeps the old import path working for existing web call sites.
 */
export { PLUMBING_TOOLS, stepLabel } from '@kortix/sdk';
export type { StepLabel, StepTier } from '@kortix/sdk';
