/**
 * Moved to `@kortix/sdk` (`packages/sdk/src/core/turns/segments/turn-busy-visibility.ts`)
 * with the working-turn logic that depends on it. This file keeps the old
 * import path working for existing web call sites.
 */
export { showTurnBusyIndicator } from '@kortix/sdk';
