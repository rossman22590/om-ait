/**
 * Moved to `@kortix/sdk` (`packages/sdk/src/core/turns/segments/group-steps.ts`)
 * so web and mobile render the transcript from one implementation. This file
 * keeps the old import path working for existing web call sites.
 */
export { groupSteps } from '@kortix/sdk';
export type { Step } from '@kortix/sdk';
