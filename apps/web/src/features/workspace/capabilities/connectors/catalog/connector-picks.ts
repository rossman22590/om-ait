// The picks live in `@kortix/shared` so the API orders a category's top slice
// exactly as this app orders the grid behind its "View all".
export {
  CATEGORY_PICKS,
  UNPINNED_RANK,
  pinRank,
  sortByPicks,
  PICKED_SECTION_KEYS,
  CURATED_SECTIONS,
} from '@kortix/shared/connector-sections';
