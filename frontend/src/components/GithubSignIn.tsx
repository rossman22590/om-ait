'use client';

interface GitHubSignInProps {
  returnUrl?: string;
}

// GitHub sign-in is temporarily disabled
export default function GitHubSignIn({ returnUrl }: GitHubSignInProps) {
  // Return null to hide the component completely
  return null;
}